# Project Context

## BANK — Migración v4 (payer learning) APLICADA en producción; QA controlada — 2026-07-21

**FASE BANK-PAYER-MEMORY-V4-APPLY-AND-CONTROLLED-QA-001 (continuación de la fase de abajo, sesión interrumpida por límite de uso; retomada y auditada):** handoff audit completo del working tree (limpio de código sin commitear — todo lo de Cursor ya estaba en el commit `b98566b`; solo quedaba ruido QA no relacionado: `.playwright-mcp/*`, `review-*.png`, `qa-*.png`, `skills-lock.json`, 2 archivos borrados y 3 capturas `qa-out/*` modificadas, ninguno tocado).

- **Migración v4 verificada APLICADA en producción**, no solo creada: `list_migrations` la registra (`20260721170547_bank_reconciliation_confirm_rpc_v4_payer_learning`); `pg_get_functiondef` en vivo confirma que el cuerpo de `confirm_bank_reconciliation_v1` coincide exactamente con el aprendizaje de pagador de esta fase; único overload (8 parámetros); grants correctos (`postgres`/`service_role` únicamente); `reverse_bank_reconciliation_v1` sin cambios. El SQL realmente ejecutado (columna `statements` de `schema_migrations`) trae un `COMMENT ON FUNCTION` que dice literalmente "...FASE BANK-PAYER-MEMORY-V4-APPLY-AND-CONTROLLED-QA-001 — APLICADA" — o sea, la aplicación fue intencional y autorizada bajo esta fase, no accidental.
- **Discrepancia de documentación (no de código)**: el archivo de migración local commiteado, `PROJECT_CONTEXT.md`/`TASKS.md` anteriores, el doc de arquitectura y el test `bank-confirm-rpc-v4-payer-learning-schema-contract.test.ts` seguían diciendo "NO APLICADA/requiere autorización" — mismo patrón que ya existía con v3 (su archivo tampoco se edita post-apply; el estado real vive en la narrativa, no en el header del `.sql`). Corregido agregando nuevas entradas (esta y la del doc de arquitectura), sin tocar el archivo de migración ni el test — coherente con la convención ya usada en v3.
- **Snapshot post-apply, solo lectura**: 1020 movimientos, 11 sugerencias, 2 links, 0 allocations, 15 eventos, **0 payer identities, 0 client_payer_links** (el escritor —`confirmCanonicalSuggestion` con `p_metadata.payer`— vive en el commit local `b98566b`, sin push; hasta el deploy ninguna confirmación real puede poblar estas tablas). Los 2 links existentes (Samysol SA USD 318,18; Botica del Señor SRL UYU 10.004; confirmados 2026-07-21 ~00:52 UTC, `metadata` vacío) son operaciones reales previas a esta fase — no son residuo de QA sin rollback.
- **CHECK constraints reales** de `bank_payer_identities`/`client_payer_links` (fingerprint_strength, status, source, usual_currency) verificados contra `pg_constraint`: coinciden exactamente con lo que la RPC v4 asume — sin riesgo de violación.
- **Gates**: `tsc --noEmit` 0 errores; `vitest run` 432 archivos/5001 tests passed, 1 skipped, 2 todo, 100% verde; ESLint sin errores nuevos en los archivos de esta fase (312 problemas preexistentes en `scripts/`/`temp-audits/`/`skills/`, ajenos); `npm run build` OK (todas las rutas nuevas compilan, incluida `/api/copilot/bank-reconciliation/manual-draft` y `/api/copilot/clients/[companyId]/payer-memory`).
- **Código de Cursor auditado y validado sin correcciones funcionales**: manual draft, payer-identity helpers/repository, endpoint de Cliente 360, wiring de `confirm-canonical-suggestion.server.ts` (siempre envía `p_metadata` con `payer` cuando hay señal durable), cambios de navegación (Ingresos→Conciliación) y tests actualizados — todo coherente, sin cabos sueltos. Único hallazgo menor: comentario obsoleto en `payer-identity-repository.server.ts` que referencia un archivo `payer-identity-learning.server.ts` que nunca se creó (la lógica quedó inline en la RPC) — deuda de documentación, no de código.
- **Veredicto**: `READY_FOR_BANK_PAYER_MEMORY_CONTROLLED_REAL_QA` — falta únicamente push + ejercitar el escritor con una confirmación real (movimiento con nombre de pagador parseable) para verificar que `bank_payer_identities`/`client_payer_links` se pueblan atómicamente end-to-end.
- **Sin push.** Sin confirmaciones productivas ejecutadas por esta auditoría. Sin backfill. Sin payer identities de prueba creadas.

## BANK — Conciliación simple + memoria de pagadores (local, sin aplicar) — 2026-07-21

**FASE BANK-SIMPLE-RECONCILIATION-AND-PAYER-MEMORY-001 (local, commit pendiente, sin push):** continúa tras corte de sesión. No reinicia; preserva cambios heredados.

- **Navegación**: Importar · Movimientos · Conciliación · Historial. "Ingresos" renombrado a Conciliación; URLs `ingresos`/`reconciliation`/`conciliacion` normalizan preservando `movementId`.
- **Movimientos**: entrada → "Revisar conciliación"; salida → "Vincular con pago programado"; sin toggle directo a `matched`. Historial sin KPIs operativos.
- **Migración v4** `20260725120000_bank_reconciliation_confirm_rpc_v4_payer_learning.sql` **CREADA, NO APLICADA**: aprendizaje atómico en la misma RPC que confirma (firma v3); sin UNIQUE total en `client_payer_links` (respeta índice parcial); idempotencia no re-incrementa; conflicto → `conflicted`.
- **Manual draft**: `POST /api/copilot/bank-reconciliation/manual-draft` + UI "Buscar cliente y recibo"; Motor B queda como ayuda visual colapsada.
- **Cliente 360** (Cobranza): sección read-only "Pagos y cuentas utilizadas"; correcciones deshabilitadas (fase posterior).
- **Helpers**: `lib/bank/canonical/payer-identity.ts` (+ repository read-only). Producción sin cambios DDL/DML.

## BANK — v3 aplicada en producción + import PDF Santander corregido (demo-ready) — 2026-07-21

**FASE BANK-V3-APPLY-PDF-IMPORT-FIX-AND-DEMO-READY-001 (local, commit sin push):** urgencia de demo el mismo día. Dos entregables independientes en una sola fase.

- **v3 aplicada**: `20260723120000_bank_reconciliation_confirm_rpc_v3.sql` aplicada en producción. Hallazgo de despliegue: `CREATE OR REPLACE FUNCTION` no reemplaza una función cuando cambia la lista de parámetros — la v3 (8 parámetros) coexistía como sobrecarga junto a la v2 (7 parámetros), y toda llamada existente (modo `suggested`) fallaba con `42725: function ... is not unique`. Corregido con `20260723120100_bank_reconciliation_confirm_rpc_v3_drop_legacy_overload.sql` (`DROP FUNCTION` de la firma vieja), aplicada en el mismo despliegue. QA rollback-tested (escenarios A-K, workspace real) confirmó **cero cambios productivos netos** (snapshot antes/después idéntico). Reprodujo además un bug latente pre-existente en v2 (no introducido por la v3): `record "v_receipt" is not assigned yet` con `p_receipt_id=NULL` en conexión fría (quirk de cacheo de planes PL/pgSQL). Mitigado en la app (recibo obligatorio en `confirm-canonical-suggestion.server.ts` y en el botón de confirmación manual del drawer), sin tocar la RPC ya verificada — documentado como gap conocido de la RPC.
- **Import PDF Santander corregido**: reproducción contra los dos extractos reales de julio 2026 mostró que el pipeline lineal (no coordenadas X/Y) sí parseaba ambos extractos, pero **perdía en silencio el último movimiento de cada uno** — un marcador de salto de página de `pdf-parse` (`-- N of M --`) quedaba fusionado con la línea "Saldo final" (sin fecha siguiente que cortara el bloque), y `isBalanceRow()` lo descartaba por contener "saldo final". Corregido en dos capas (retirar el marcador como ruido + cortar el bloque también en saldo inicial/final). La nueva validación de saldo por fila detectó además un segundo bug real: `Math.abs()` ocultaba saldos negativos legítimos (la cuenta USD entra en negativo un momento del mes). Tras ambos fixes, los dos extractos reales reconcilian a la centésima. Agregados (aditivos, sin romper el productor Excel): `operation_group_key` (agrupa principal+comisión sin fusionar, verificado con los 3 ejemplos reales de la fase), identidad estructurada de pagador, `balance_validation`, `dedup_fingerprint`. El `external_id` real de deduplicación productiva **no se tocó** (evita invalidar dedup de extractos ya importados). Ver `docs/architecture/santander-pdf-parser.md` para el detalle completo.
- **QA real en browser**: login + upload de los dos PDF reales vía UI → preview 2/2 archivos, 0 errores, 0 omitidos, 98 movimientos, balances correctos — sin confirmar productivamente (solo preview). 4 tabs confirmados (Importar/Movimientos/Ingresos/Historial), sin tab Conciliación independiente. Hallazgo aparte (dato ya importado antes del fix, no de esta fase): movimientos productivos existentes en Ingresos ya muestran el marcador `-- N of M --` contaminando su descripción — limpieza retroactiva queda fuera de alcance.
- **Tests nuevos**: fixture sintético reproduce la geometría real (encabezado partido, fecha/referencia partidas, marcador de página) y los 3 pares principal+comisión con los nombres reales dados en el enunciado (Zeta/Micaela/Petrovic). 12 tests nuevos, todos verdes.
- **Gates**: tsc 0 errores, ESLint limpio, `vitest run` completo verde (incluye ~576 tests en lib/bank-movements + lib/treasury), build pendiente de confirmación final. Sin push, sin confirmaciones productivas reales ejecutadas.

## BANK — Migración v3 corregida antes de aplicar (defecto de CHECK evitado) — 2026-07-21

**FASE BANK-CANONICAL-CONFIRM-RPC-V3-MIGRATION-APPLY-001 (detenida en preflight) + BANK-CONFIRM-RPC-V3-MIGRATION-CORRECTION-001 (corrección local, commit sin push):** el intento de aplicar en producción la migración v3 (`p_metadata` para `confirm_bank_reconciliation_v1`) se detuvo antes de ejecutar ningún DDL — la revisión final del SQL encontró que el INSERT a `bank_movement_reconciliation_links` derivaba `method='manual_reviewed'` de `p_metadata->>'mode'`, valor que **no pertenece** al CHECK real de esa columna en producción (`bank_movement_reconciliation_links_method_check`, verificado en vivo: admite únicamente `'manual'|'suggested_confirmed'`). Habría violado la constraint apenas se usara el modo `manual_reviewed` — exactamente el caso que la fase existe para habilitar.
- **Producción NUNCA fue tocada**: toda la interacción con `erzdifkvvailxnwdukzf` durante el intento de aplicación fue de solo lectura (`execute_sql` sobre `pg_proc`/`information_schema`/`pg_constraint`/`list_migrations`); v2 sigue siendo la versión activa de la RPC.
- **Corrección**: se editó el mismo archivo `supabase/migrations/20260723120000_bank_reconciliation_confirm_rpc_v3.sql` **in place** (nunca se creó una v4, porque el archivo jamás llegó a aplicarse) — `method` es ahora siempre el literal `'suggested_confirmed'`, en ambos modos. La distinción `suggested`/`manual_reviewed` sigue viviendo exclusivamente en `reconciliation_events.metadata.mode`, sin afectar la semántica financiera del link.
- **Gap de test cerrado**: los tests anteriores de la v3 eran puramente textuales y nunca habrían detectado esta violación de constraint. Se agregó un fixture (`REAL_METHOD_CHECK_ALLOWED_VALUES`) tomado del CHECK real capturado en vivo, más tests que verifican que el INSERT solo puede producir valores de ese conjunto — verificado que fallan contra la versión original defectuosa y pasan contra la corregida.
- **Nuevos tests de seguridad de metadata**: `canonical-confirm-reject-api.test.ts` confirma que el schema Zod descarta silenciosamente cualquier clave no declarada (`workspaceId`, `actorId`, `movementId`, `amount`, `currency`, `role`, `serviceRole`, `method`, `metadata`) — la defensa real contra metadata maliciosa vive en ese límite, no en la RPC.
- **Adapter auditado, sin cambios**: `confirmCanonicalSuggestion.server.ts` ya cumplía el contrato requerido (workspace/actor derivados server-side, metadata construida con whitelist fija, nunca envía `method`) — no se encontró desalineamiento real.
- **Gates**: tsc 0 errores, ESLint limpio, `vitest run` completo 4933 passed / 1 skipped / 2 todo (40+ tests nuevos/actualizados de esta corrección), build OK. Sin push, sin migración aplicada, sin confirmaciones reales.

## BANK — Selección manual revisada de cliente/recibo (drawer de Ingresos) — 2026-07-20

**FASE BANK-MANUAL-CANONICAL-MATCH-SELECTION-001 (local, commit sin push):** habilita conciliar manualmente cuando la sugerencia automática no alcanza — bloqueante real hoy (5 casos pendientes en producción, 0 confianza Alta).
- **Hallazgo de auditoría clave**: `confirm_bank_reconciliation_v1` NUNCA exigió que el recibo coincidiera con `proposed_receipt_id` de la sugerencia, y NUNCA conoció el concepto de "cliente" (no recibe `client_id`). Esa restricción vivía 100% en el adapter TypeScript. **No hizo falta una RPC nueva** — solo se agregó `p_metadata jsonb DEFAULT '{}'::jsonb` (migración v3, **creada, NO aplicada**) para poder auditar la decisión (modo, selección final vs. propuesta, motivo) vía `reconciliation_events.metadata` (columna ya existente, nunca usada hasta ahora). El modo "suggested" llama la RPC sin ese parámetro — sigue funcionando contra la v2 ya en producción sin requerir esta migración.
- **Dos modos, un solo contrato financiero**: `mode: "suggested"` (comportamiento idéntico a antes) y `mode: "manual_reviewed"` (cliente/recibo/facturas elegidos explícitamente, motivo obligatorio 3-500 caracteres, revalidación server-side completa: cliente real del workspace, recibo perteneciente a ESE cliente, facturas del cliente seleccionado). Ambos terminan en la misma `confirm_bank_reconciliation_v1` — nunca un writer paralelo.
- **Endpoint extendido** (no uno nuevo): `POST /api/copilot/bank-reconciliation/[suggestionId]/confirm` con body ampliado. **2 endpoints de búsqueda nuevos, solo lectura**: `clients-search` (paginado, nunca el portfolio completo) y `receipts-search` (recibos+facturas de un cliente, con flag `used` cruzado contra links activos).
- **UI**: sección "Otra coincidencia" en el drawer (colapsada, nunca mezclada visualmente con "Confirmar conciliación"), buscador de cliente debounced, recibos con diferencia vs. movimiento, motivo con 5 opciones + "Otro". Cambiar cliente/recibo limpia selecciones downstream.
- **Explícitamente NO implementado**: casos sin ninguna suggestion (la RPC ya soporta `p_suggestion_id=NULL` estructuralmente, pero el flujo seguro completo queda documentado como gap), agrupación visual de recibos en 3 niveles (implementada como lista simple con diferencia mostrada), filtros cliente/período dedicados.
- **Gates**: tsc 0 errores, ESLint limpio, `vitest run` completo 4920 passed / 1 skipped / 2 todo (incluye ~50 tests nuevos: adapter modo manual_reviewed, schema-contract v3, contrato de los 2 endpoints de búsqueda, contrato UI del selector manual), build OK. Sin push, sin migración aplicada, sin confirmaciones reales.

## BANK — Ingresos como única bandeja operativa (Conciliación absorbida) — 2026-07-20

**FASE BANK-UNIFIED-INCOME-RECONCILIATION-WORKSPACE-001 (local, commit sin push, tras QA controlada en producción del commit `83fa402`):** decisión funcional definitiva — la pestaña Ingresos pasa a ser la única bandeja diaria para identificar cliente y conciliar. La pestaña Conciliación independiente (agregada en BANK-CANONICAL-CONFIRM-UI-001, ya en producción) queda retirada de la navegación.
- **Motivo**: la tarea diaria estaba dividida entre Movimientos/Ingresos/Conciliación, obligando a buscar el mismo ingreso en varios lugares.
- **Tabs finales**: Importar · Movimientos · Ingresos (★) · Historial. Deep links antiguos (`?tab=reconciliation`/`?tab=conciliacion`) normalizan a Ingresos preservando `movementId`.
- **Componentes retirados** (sin consumidores tras el refactor, lógica extraída a piezas reutilizables, no borrada): `bank-canonical-reconciliation-panel.tsx`, `bank-income-panel.tsx`. Nuevos: `bank-income-workspace.tsx` (bandeja unificada), `canonical-evidence-ui.tsx` (evidencia/drawer/confirm/reject compartidos), `bank-history-panel.tsx` (Historial + decisiones recientes).
- **Estados derivados** (`lib/bank/canonical/income-workspace.ts`, puro, 16 tests): sin_identificar/cliente_sugerido/con_coincidencia/requiere_revision/conciliado/sugerencia_rechazada/ignorado — independiente de la confianza humana (un caso Baja con cliente+recibo concretos y sin conflicto es "con coincidencia").
- **Sin N+1**: nuevo modo `?workspace=income&movementIds=...` en el endpoint canónico existente (mismo `/api/copilot/bank-movements/canonical-suggestions`, no un endpoint nuevo) devuelve estado+evidencia para todo el subconjunto operativo en un solo request.
- **Motor B** conservado como identificación preliminar (nunca compite con la evidencia canónica, nunca completa una conciliación). **Sin motor nuevo, sin writer paralelo** — confirmar/rechazar siguen siendo exclusivamente los endpoints canónicos de la fase anterior.
- **Historial** ahora también muestra conciliaciones confirmadas/rechazadas recientes; botón "Revertir" presente pero deshabilitado (fuera de alcance).
- Sin cambios de scoring/thresholds, sin aprendizaje de pagador, sin reversión real, sin migraciones nuevas.
- **Gates**: tsc 0 errores, ESLint limpio, `vitest run` completo 4883 passed / 1 skipped / 2 todo (incluye ~37 tests nuevos/reescritos de esta fase), build OK. Sin push.

## BANK — Confirmación/rechazo real de conciliaciones canónicas (bandeja diaria) — 2026-07-22

**FASE BANK-CANONICAL-CONFIRM-UI-001 (local, commit sin push):** primera UI de escritura real sobre el motor canónico (Motor D). Hasta ahora la pestaña Conciliación era 100% lectura; esta fase habilita Confirmar y Rechazar.
- **Confirmar** (`POST /api/copilot/bank-reconciliation/[suggestionId]/confirm`): Zod-validado, RBAC `requireCopilotModuleWriteAccess("bank_movements")`, workspace/actor derivados server-side (nunca del body). Delega en `confirmCanonicalSuggestion()` (`lib/bank/canonical/confirm-canonical-suggestion.server.ts`), que revalida `expectedMovementId`/`expectedReceiptId` contra la sugerencia real y cada factura de la asignación contra las candidatas recalculadas (misma fuente que la evidencia mostrada en el drawer), antes de llamar `confirm_bank_reconciliation_v1` (v2, ya aplicada en producción) — nunca escribe directo, nunca usa Motor C.
- **Rechazar** (`POST /api/copilot/bank-reconciliation/[suggestionId]/reject`): mismo patrón de RBAC. Usa `reject_bank_suggestion_v1` (confirmado reusable sin cambios: soporta `suggestion_scope='operational'`, nunca toca `bank_movements`, así que el movimiento sigue disponible para una futura sugerencia — semánticamente distinto de "ignorar movimiento"). Revalidación propia más estricta que la RPC: solo acepta `operational` desde este endpoint.
- **UI**: confirmación rápida (≤2 clicks) solo si confianza Alta y sin conflicto de pagador; confianza Media/Baja o conflicto exige abrir el drawer de evidencia (selección explícita de facturas, nunca auto-asignadas). Estado optimista por `suggestionId`, `already_confirmed`/`already_rejected` tratados como éxito idempotente, avance automático al siguiente pendiente, sin recarga de página. Nuevo contador "Conciliados hoy" (huso Montevideo fijo UTC−3).
- **Ingresos → Conciliación**: botón "Revisar conciliación" quando el motor ya tiene una sugerencia operativa para ese movimiento — solo navega (vía `?movementId=` en el endpoint de lectura), no reactiva Motor B como flujo de conciliación.
- **Explícitamente NO implementado**: reversión (ni botón ni endpoint — fase futura BANK-CANONICAL-REVERSE-UI-001) y aprendizaje de pagador (sin escrituras a `bank_payer_identities`/`client_payer_links`; copy: "La identificación automática de pagadores se habilitará en una fase posterior").
- **Docs**: `docs/architecture/bank-reconciliation-canonical-engine.md` actualizado (matriz de writers, plan de fases, límites conocidos).
- **Gates**: tsc 0 errores, ESLint limpio en archivos tocados, `vitest run` completo 4860 passed / 1 skipped (pg local) / 2 todo (incluye 32 tests nuevos de esta fase: adapters confirm/reject, contrato de ruteo, enlace Ingresos→Conciliación, contador), build de producción OK, `git diff --check` sin errores reales. Sin push, sin confirmaciones/reversiones reales ejecutadas contra producción.

## BANK — Corrección del contrato canónico confirm/reverse (`status='reversed'`) — 2026-07-20

**FASE BANK-CANONICAL-CONFIRM-CONTRACT-CORRECTION-001 (local, commit sin push):** auditoría exacta del hallazgo bloqueante de la fase anterior (`bank_movements.status IN ('ignored','reversed')` en `confirm_bank_reconciliation_v1`, referenciando un valor que el CHECK de esa columna nunca admitió).
- **Conclusión con evidencia**: es un bug de **lógica de RPC**, no de esquema. El CHECK real de `bank_movements.status` (`pending|suggested|matched|ignored|needs_review`) coincide exactamente con el tipo TypeScript `BankMovementStatus` — ambos ya eran coherentes entre sí. Ningún writer, en ningún lugar del codebase, escribe jamás `'reversed'` en esa columna (ni la propia RPC, ni `reverse_bank_reconciliation_v1`, que nunca toca `bank_movements`). La comparación en el `IN (...)` era una rama siempre-falsa, no un fallo de ejecución — **no rompía nada en producción**. Se descartó explícitamente "ampliar el CHECK" como solución (el enunciado de la fase advertía contra esa suposición) porque la evidencia apuntaba a lo contrario.
- **Gap real distinto encontrado**: la v1 de `confirm_bank_reconciliation_v1` no validaba `suggestion_scope` al confirmar por `p_suggestion_id` — podía confirmar una sugerencia `historical_review` o `matched_audit` igual que una `operational`, contradiciendo la separación operativa/histórica ya establecida.
- **Corrección** (migración `20260722130000_bank_reconciliation_confirm_rpc_v2.sql`, **creada, NO aplicada**): (1) retira la rama `'reversed'` inalcanzable (cero cambio de comportamiento real); (2) agrega `AND suggestion_scope='operational'` obligatorio. Misma firma, mismos grants (`service_role`-only), sin tocar ningún `CHECK`/tabla/dato existente. `reverse_bank_reconciliation_v1` auditada de nuevo: sin cambios necesarios.
- **Máquina de estados** documentada por entidad (sugerencia/link/allocation/movimiento/evento) en `docs/architecture/bank-reconciliation-canonical-engine.md`. Modelo A confirmado para `bank_reconciliation_suggestions.status='reversed'` (ya era correcto).
- **Sin implementación de UI de confirmación real ni aprendizaje de pagador** — ambos siguen explícitamente fuera de alcance (Fases 2 y 3). Sin confirmaciones/reversiones ejecutadas contra producción, sin migraciones aplicadas.
- **Gates**: ver cierre de la fase. Sin push.

## BANK — Motor canónico de conciliación (Motor D) — routing + guards — 2026-07-20

**FASE BANK-RECONCILIATION-CANONICAL-ENGINE-001 (local, commit sin push):** decisión de arquitectura autorizada tras la auditoría previa (BANK-DAILY-RECONCILIATION-UX-AND-FLOW-001): **Motor D** (`bank_reconciliation_suggestions` + `bank_payer_identities`/`client_payer_links` + `confirm_/reverse_bank_reconciliation_v1`) es el **único** motor canónico de conciliación de ingresos. No se crea un segundo motor.
- **Auditoría exhaustiva de writers**: matriz completa de quién escribe qué tabla, con destino de cada motor (A/B/C/D) — ver `docs/architecture/bank-reconciliation-canonical-engine.md`. Hallazgo crítico: Motor C ("Conciliación detallada") escribía `bank_movement_reconciliation_links` por un repositorio propio, **bypaseando** la RPC transaccional endurecida (sin `payment_allocations`, sin `reconciliation_events`, sin locks). Motor A matcheaba contra Tesorería (`planned_cash_obligations`), no contra clientes/recibos/facturas — se usaba ambiguamente como "Conciliar".
- **Contrato de la RPC auditado línea por línea** (`confirm_/reverse_bank_reconciliation_v1`): responde las 10 preguntas de negocio sobre movimiento↔recibo↔factura, pagos parciales, idempotencia. Inconsistencia encontrada: la RPC rechaza `status='reversed'` pero esa columna no admite ese valor — a corregir antes de Fase 2.
- **Cambios de código**: tabs reordenados (Importar·Movimientos·Ingresos·**Conciliación ★**·Historial); Conciliación ahora monta `BankCanonicalReconciliationPanel` (nuevo, 100% lectura de `/api/copilot/bank-movements/canonical-suggestions`, evidencia cliente/recibo/facturas candidatas/pagador/confianza Alta-Media-Baja-Sin sugerencia); Motor A renombrado ("Vincular con pago programado") y reubicado dentro de Movimientos como sección secundaria colapsada; Motor C retirado como escritor en toda capa (UI sin botones de escritura + rutas POST/DELETE devuelven `410 LEGACY_WRITE_RETIRED` server-side, no solo ocultas en UI).
- **Sin escrituras nuevas ejecutadas**: no se generaron sugerencias contra producción, no se confirmó/revirtió nada real. Confirmar/Rechazar reales quedan para Fase 2 (BANK-CANONICAL-CONFIRM-UI), autorización separada.
- **Docs**: `docs/architecture/bank-reconciliation-canonical-engine.md` (nuevo, matriz completa) + `docs/technical/bank-reconciliation-rollout.md` (Etapa 2 agregada).
- **Gates**: ver cierre de la fase para resultados de tsc/eslint/tests/build. Sin push.

## VENTAS — Confirmación definitiva: vendedor a nivel documento, no por línea — 2026-07-20

**FASE SALES-SELLER-DOCUMENT-LEVEL-CONFIRMATION-001 (local, commit sin push):** confirmación de negocio cierra la duda abierta por `DIV-CONT-011` (auditoría de si Zeta trae número de línea estructurado). Regla definitiva: **el vendedor se asigna al comprobante/ticket completo, nunca a una línea o servicio individual.** Ejecutivo = responsable del cliente; Vendedor = responsable del comprobante; Línea = detalle comercial sin vendedor independiente; Cobro = entidad separada.
- **Sin cambios de modelo**: se mantiene `sales_document_salespersons` (`UNIQUE(workspace_id, document_id)`, ya en producción); se descarta explícitamente crear `sales_document_line_sellers`, `document_line_key`, fingerprint de línea, migración/endpoint/auditoría por línea. No existía ningún código de línea a revertir (verificado por `git status`).
- **DIV-CONT-011 cerrada** en `docs/vendors/z/KNOWN-DIVERGENCES.md` como "no requerida" (la falta de número de línea en Zeta ya no bloquea nada, porque no se modela por línea). Nueva sección "Ejecutivo vs Vendedor" agregada a `docs/domains/ventas.md`.
- **UI ya cumplía el contrato** (implementado en fases previas `SALES-DOCUMENT-SELLER-INLINE-UX-AND-IDENTITY-FIX-001` / `DEPLOY-QA-001`): selector único por `document_id` en Detalle, líneas hermanas agrupadas. Único ajuste de texto: "↳ mismo comprobante" → **"Incluido en este comprobante"** (coincide con el ejemplo de negocio) en `ventas-detalle-tab.tsx` + test de contrato actualizado.
- **Métricas ya correctas**: `buildSellerSalesSummary` opera sobre `CanonicalSaleDocument` (uno por documento, `grossAmount` = total de cabecera), nunca itera líneas para atribuir venta — un comprobante de 5 líneas cuenta 1 operación, nunca 5. Se agregó test explícito con el escenario A-3032 (5 líneas, $68.320 → 1 operación, nunca $341.600) en `seller-sales-summary.test.ts`.
- **Gates**: `tsc --noEmit` 0 errores, `vitest run` completo 4780 passed / 1 skipped (pg local) / 2 todo, incluye 41 tests dirigidos a vendedor/ejecutivo. Sin push.

## BANK — Retiro de "Revisión bancaria" (flujo simple en Banco) — 2026-07-19

**FASE BANK-SIMPLE-RECONCILIATION-RESTORE-001 (local, commit sin push):** se elimina de la experiencia la pantalla independiente `/copilot/revision-bancaria` y sus APIs `/api/copilot/bank-review/*`; el flujo vuelve al módulo **Banco** (`/copilot/movimientos-bancarios`), que ya provee importación (Excel/PDF preview+confirm, dedup), lista de movimientos, conciliación N:M (drawer/reconcile/links/ignore) y matching ingreso→cliente.
- **Eliminado del código**: item nav "Revisión bancaria" (+ import ClipboardCheck), `app/copilot/revision-bancaria`, 5 rutas `app/api/copilot/bank-review/**`, `components/copilot/bank-review/**` (3), `lib/bank/review/**` (view/service/actions + 3 tests), entrada RBAC `/api/copilot/bank-review` del module-map.
- **Conservado inactivo (sin rollback DB)**: tablas/`suggestion_scope`/RPC `review/reject/note`/migraciones aplicadas/eventos; las 11 suggestions + 13 events + reviewed_at/reviewed_by/rejected_reason del QA quedan almacenados pero **no visibles**; motor `lib/bank/intelligence/**` inactivo. Sin migración destructiva, sin borrar datos, sin reclasificar.
- Test `bank-review-retired.test.ts` (7): nav/rutas/componentes/lib eliminados, Banco intacto, infra/migraciones no borradas. tsc/eslint/Banco+intelligence (387)/RBAC coverage verdes. **HEAD e741f99 pusheado; este cambio es local sin push.** Próximo: QA simple de Banco.

## BANK — Review actions (revisar/nota/rechazar) — 2026-07-19

**FASE BANK-HISTORICAL-REVIEW-ACTIONS-001 (local, commit sin push):** acciones humanas NO financieras sobre sugerencias de revisión bancaria.
- **Ciclo de vida Modelo A**: "revisada" usa `reviewed_at`/`reviewed_by` (NO agrega `status='reviewed'`; status queda `generated`); "rechazada" = `status='rejected'` + `rejected_reason`. Estado derivado: rejected > reviewed > pending.
- **Migración `20260721120000_bank_review_actions.sql` CREADA, NO APLICADA** (aditiva): amplía `event_type` (+`suggestion_reviewed`/`suggestion_note_added`/`suggestion_rejected`), 3 RPC transaccionales SECURITY INVOKER service_role-only (`review_/reject_/add_bank_suggestion_note_v1`) + helper actor + índice pendientes. Requiere autorización.
- **Atomicidad** suggestion+event en la RPC; **idempotencia** (already_reviewed/rejected/recorded); concurrencia por UPDATE condicionado; scope/estado validados server-side; append-only eventos; sin confirm/reverse, sin links/allocations, sin tocar movimientos/recibos/facturas.
- **API** POST `/api/copilot/bank-review/[id]/{review,reject,notes}` con `requireCopilotModuleWriteAccess("bank_movements")` (read-only→403). **UI** drawer con acciones por scope + composers reason/nota + re-fetch + badges Pendiente/Revisada/Rechazada + filtro revisión. **Contador Pendientes** = activo + `reviewed_at IS NULL`.
- Tests: schema-contract (7 nuevos) + view (reviewState/filter) + service (pending) + actions (10). Gates verdes. Confirmar conciliación operativa: fuera de alcance (RPC financiera futura).

## BANK — Historical shadow persist contract (`suggestion_scope`) — 2026-07-19

**FASE BANK-HISTORICAL-SHADOW-PERSIST-POLICY-001 (local, commit sin push):** contrato estructurado para persistir sugerencias históricas separadas de las operativas.
- **Columna `suggestion_scope`** (`operational` | `historical_review` | `matched_audit`), no boolean (para extender matched_audit). Antes la distinción vivía solo en `warnings` JSON.
- **Migración `20260720120000_bank_suggestion_scope.sql` CREADA, NO APLICADA** (aditiva: columna default `operational`, CHECK, reemplaza `brs_active_uidx`→`brs_active_scope_uidx` con scope, índice `brs_ws_scope_status_idx`; RLS/grants sin cambio). Requiere autorización.
- **Idempotencia por ámbito**: operativo e histórico nunca se sobrescriben mutuamente. Consultas explícitas `listOperationalSuggestions()`/`listHistoricalReviewSuggestions()`. Eventos con `metadata.suggestionScope`.
- **Runner `persistHistoricalForReview`** (flag server-side, default false): requiere `includeHistoricalForShadow=true`+`persist=true`+`dryRun=false`+IDs; solo persiste `historical_review`; omite post-corte/matched; nunca AUTO/cron/UI. `includeHistoricalForShadow` sin flag sigue dry-run only.
- Auditoría: sin consumidores de suggestions/events fuera de `lib/bank/intelligence/**` (no tasks/notif/dashboards/Hoy). Las 5 sugerencias existentes → `operational`; las 2 matched históricas no se reclasifican (tratamiento separado).
- Tests: bank intelligence 134 (runner 19, eligibility 19, schema-contract 12, repositories-scope 3). Doc nuevo `docs/architecture/bank-historical-review-contract.md`. **Cero escrituras/DDL en prod esta fase.**

## BANK — Shadow historical scope (audit-only) — 2026-07-19

**FASE BANK-SHADOW-HISTORICAL-SCOPE-001 (local, commit sin push):** modo `includeHistoricalForShadow=true` (server-side, default false) en `isShadowEligibleMovement()`. Permite analizar movimientos `[2026-01-01, 2026-07-01)` como **historical-audit** (`historicalAudit=true`, `auditOnly=true`, warning `HISTORICAL_SHADOW_AUDIT`, nunca AUTO). **Dry-run only** (`persist=true` → `HISTORICAL_PERSIST_NOT_ALLOWED`) y **exige IDs explícitos** (sin IDs → `HISTORICAL_SCOPE_REQUIRES_IDS`); `< 2026-01-01` siempre excluido (`MOVEMENT_BEFORE_GLOBAL_FLOOR`). Ambos cortes intactos: global `2026-01-01`, bancario `2026-07-01` (no modificado). Los 424 pending históricos no se procesan automáticamente.
- Flujos separados: operativo / matched-audit / historical-audit. Guardas fallan antes de cargar/escribir.
- Tests: eligibility 19 + runner 12 (guards + historical dry-run). Docs actualizados. **Cero escrituras** en esta fase (solo código + tests).

## BANK — Shadow matched-eligibility policy — 2026-07-17

**FASE BANK-SHADOW-MATCHED-POLICY-001 (local, commit sin push):** política única `isShadowEligibleMovement()` (`lib/bank/intelligence/server/eligibility.ts`) aplicada idéntica en todos los caminos (ID único / lista / auto). Por defecto excluye `matched`, `ignored`, `reversed`, egreso, link canónico activo, fuera de workspace y anterior al corte `2026-07-01`. `matched` solo con `includeMatchedForAudit=true` (server-side, default false) → **audit-only**: `auditOnly=true`, warning `MATCHED_MOVEMENT_AUDIT`, nunca AUTO, **no persiste**, no modifica nada (dry-run en esta fase).
- Runner integra el gate antes de construir propuestas; audit-only excluido de persistencia aun con `persist=true`.
- Las 2 sugerencias `matched` históricas de la primera persistencia (`49bd2ddc…`, `1c611fcd…`) se **conservan** como `generated` (datos legítimos previos, no error); no se borran/rechazan/supersedan.
- Tests: `eligibility.test.ts` (11) + runner matched-policy (3). Suite bank intelligence 109 verdes.
- Docs: `bank-shadow-server.md`, `bank-reconciliation-rollout.md` actualizados.

## BANK — Expanded shadow dry-run — 2026-07-17

**FASE BANK-EXPANDED-SHADOW-DRY-RUN-001 (prod read-only, sin push):** muestra explícita de 20 movementIds. AUTO=0, REVIEW=65%, UNIDENTIFIED=35%, ties=4 (degradados), colisiones reales en batch=0 (no simuladas), payerIdentity=0, conf avg 27.5 / max 50. Conteos idénticos 950/365/602/0. writeAttempts=0. HEAD `3a851db` sin commit. Veredicto: **GO_FOR_CONTROLLED_SHADOW_PERSIST_REVIEW**.
- Artefacto local: `.agents/qa-shadow-expanded-dry-run-001.json` (no commit).

## BANK — Shadow correction (ambigüedad/colisión) — 2026-07-17

**FASE BANK-SHADOW-CORRECTION-001 (local, sin push):** motor deja de usar `.find()` arbitrario en recibos exactos. Empate → `proposedReceiptId=null` + `MULTIPLE_STRONG_CANDIDATES` + `tiedCandidates`; colisión inter-batch → `RECEIPT_CANDIDATE_COLLISION`. Dry-run default. Sin SQL remoto / persist / push.
- Revalidación 5 IDs OK → expanded dry-run ejecutado.

## BANK — Shadow server implementation — 2026-07-17

**FASE BANK-SHADOW-SERVER-IMPLEMENTATION-001 (local, sin push):** capa server-side en `lib/bank/intelligence/server/` — lectura workspace-scoped, motor puro → `ShadowProposal`, dry-run por defecto, shadow persist opcional solo en `bank_reconciliation_suggestions` + `reconciliation_events`. Runner con alcance obligatorio (`movementId`|`movementIds`|`limit`≤25). Sin RPC financiera, sin links/allocations, sin cron/UI, sin runner contra producción.
- Docs: `docs/architecture/bank-shadow-server.md`, rollout actualizado.
- Migraciones bancarias: aplicadas e inmutables (sin SQL remoto en esta fase).

## BANK — Validación post-migración inteligencia bancaria — 2026-07-17

**Estado producción (`erzdifkvvailxnwdukzf`):** migraciones `20260719120000`, `20260719120100`, `20260719120200` **aplicadas**. Inmutables: no editar/reaplicar/revertir; correcciones solo con migración nueva.
- Fixtures: 0 residuos `BANK_TEST` / UUIDs locales `aaaaaaaa`/`bbbbbbbb` en tablas protegidas.
- Conteos vivos: `bank_movements=951`, `proto_receipts=365`, `proto_invoices=602`, `bank_movement_reconciliation_links=0`; tablas nuevas en 0 filas vivas (actividad temporal de batería C1–C9 / D1–D8 rollbackeada).
- RPC: `confirm/reverse_bank_reconciliation_v1` sin EXECUTE para anon/authenticated/PUBLIC; EXECUTE para service_role+postgres.
- RLS activa en las 5 tablas nuevas; `reconciliation_events` append-only a nivel policy (sin UPDATE/DELETE policies).
- Concurrencia 25–28: **NO ejecutada** (sin dos conexiones simultáneas reales en este cierre).
- Push: **NO**.

## FASE 9D — Ventas netas + comercial por cliente — 2026-07-16 (CIERRE LOCAL / MIGRATION APPLY BLOCKED)

**Objetivo:** ventas netas canónicas + comercial por cliente (no por factura) + paridad Finanzas.

- **KPI:** `netSalesByCurrency` = emitidas − NC (puede ser negativo). Removido `Math.max(0, …)` del cálculo de `issuedInPeriodNet` / `issuedNet` / `netSales` en Finanzas, Cartera y trends. Clamps de cobranza/tasas se mantienen.
- **Comercial canónico desde 2026-07-01:** `sales_client_salespersons` vigente en `issue_date` → Sin asignar. `sales_document_salespersons` legado, no entra a analytics; UI Detalle sin selector por factura.
- **Migración:** `20260716160000_sales_client_salespersons_fase9d.sql` revisada (aditiva, RLS, historial). **Aplicación bloqueada** en esta sesión: sin `DATABASE_URL` / `SUPABASE_ACCESS_TOKEN` / MCP Supabase. Tabla ausente (`PGRST205`).
- **Checks:** ver cierre de tarea (tsc / eslint / diff-check). Sin Vitest/build/Playwright/push.

## FASE 8 — Bandeja única de tareas inteligentes — 2026-07-15 (CIERRE LOCAL)


**Objetivo implementado localmente:** `/copilot/tareas-diarias` pasa a una única pantalla `Tareas` que combina tareas persistidas y recomendaciones virtuales determinísticas.
- **Arquitectura**: nuevo feed server-side `/api/copilot/tasks/feed` con `UnifiedTaskItem`, dedupe por `stableKey`/`task_key`, score determinístico, tabs/KPIs unificados y acciones de recomendación (`claim`, `start`, `dismiss`, `materialize`) sin migraciones.
- **Recomendaciones**: reglas por módulos habilitados para Cobranza, Clientes, Alertas y Datos usando fuentes read-only existentes, permisos server-side y sin modificar fórmulas financieras/deuda/aging.
- **UI**: se retiró la experiencia visible Cuaderno/Todas; la pantalla usa `UnifiedTasksPanel`, botón único `Nueva tarea`, tabs Prioridad/Para hoy/Atrasadas/Recomendadas/En progreso/Completadas y admin-only Todas/Sin asignar.
- **Integraciones**: Hoy consume el feed unificado; `RelatedTasksCard` muestra tareas y recomendaciones relacionadas para Cliente 360/Cobranza/Alertas y permite tomar recomendaciones.
- **Checks**: `tsc --noEmit` OK, ESLint dirigido a archivos FASE 8 OK, Vitest completo 2/2 OK (369 archivos, 4412 tests), `npm run build` OK, `git diff --check` OK (solo warnings LF→CRLF). `npx eslint` global falla por errores preexistentes fuera de scope en `.vercel`/backup de skills.

## FASE 6A — Increment 5 Global Visual Sweep — 2026-07-15 (CIERRE LOCAL)

**Objetivo cerrado:** barrido visual final de superficies Copilot restantes sin iniciar FASE 6B ni tocar lógica financiera/RBAC/RLS.
- **UI/DS-Core**: estados empty/loading/error normalizados en Tareas, Alertas y Banco; tokens semánticos aplicados en Finanzas, Dashboard, Manual, Helpdesk, Admin, Tesorería y Cliente 360; acciones destructivas de Admin diferenciadas visualmente.
- **Accesibilidad**: tabs de Cliente 360 y Alertas con roles/aria; modales Reportes/Admin con `role="dialog"`/`aria-modal`; severidad de Alertas con nombre accesible no dependiente solo de color.
- **QA**: build producción OK; smoke Playwright autenticado 4/4 OK; UI QA global con 16 rutas × 5 viewports × light/dark, sin screenshots en Git. Banco requirió retries aislados por latencia local headless, todos 200 con warnings visuales menores.
- **Checks**: `tsc --noEmit` OK, ESLint dirigido OK, guardrail visual OK, `vitest run` 357/357 archivos y 4302/4302 tests OK, `npm run build` OK, `git diff --check` OK (solo warnings LF→CRLF).

## FASE 2 — CANONICAL COLLECTIONS SEMANTICS — 2026-07-14 (CIERRE LOCAL)

**Objetivo cerrado:** separar definitivamente `Cobrado aplicado` de `Cobrado registrado`.
- **API canónica**: `lib/financial/canonical/collections-snapshot.ts` agrega `buildCanonicalCollectionsSnapshot`, contratos explícitos `appliedCollectionsAtCutoff`, `registeredCollectionsInPeriod`, `appliedCollectionRate`, `receiptCountInPeriod` y diagnósticos (`missing_*`, `invalid_*`, `negative_applied_collections`, etc.).
- **Compatibilidad**: `generateFinancialConsistencyReport` conserva aliases legacy (`totalCollected`, `collectedInPeriod`) pero expone campos explícitos nuevos. No se modificó deuda, aging, caja, Banco ni movimientos bancarios.
- **Consumidores migrados**: Reportes de cobranza (`Cobrado registrado`), Finanzas detalle (`Cobrado aplicado`), Hoy (`Cobros registrados acumulados`), Cliente 360 (`Cobrado aplicado según Zeta`) y Dashboard (`Ventas vs Cobrado aplicado`).
- **Limitación Zeta documentada**: sin mapping certificado `recibo ↔ factura`; no hay FIFO ni distribución inventada. Aplicado se lee desde ventas/saldo; registrado desde `proto_receipts.receipt_date`.
- **Diff real read-only**: `scripts/audit-canonical-collections-diff.ts` procesó 593 facturas y 356 recibos. Julio parcial y junio completo muestran diferencias aplicado vs registrado clasificadas como `DATA_QUALITY` por `missing_invoice_currency: 4`; sin `IMPLEMENTATION_DEFECT`.
- **Checks**: `tsc --noEmit` OK, ESLint dirigido OK, tests específicos OK, suite completa Vitest 4210/4210 OK, `npm run build` OK, `git diff --check` OK. Smoke visual sin screenshots: Reportes/Dashboard labels OK; Hoy/Finanzas shell OK; sin 500, sin consola severa, sin overflow. Cliente 360 shell OK; ficha no expuso link durante smoke.

## COPILOT-BANK-MOVEMENTS-AND-DAILY-TASKS-SPRINT-B-001 — 2026-07-09 (CRUD MANUAL USABLE)

**Primera versión usable manual sobre las tablas ya migradas (sin push):**
- **APIs de escritura**: `POST /api/copilot/bank-movements`, `PATCH/DELETE /api/copilot/bank-movements/[id]` (+ GET single); `POST /api/copilot/daily-tasks`, `PATCH/DELETE /api/copilot/daily-tasks/[id]`. Todas con `requireCopilotModuleWriteAccess`, validación Zod (`parseAndValidateJsonBody`), workspace-scoped (`.eq("workspace_id", tenantCompanyId)`), y `workspace_id` impuesto por servidor (schema con `rejectWorkspaceId`).
- **Contratos**: `lib/bank-movements/bank-movements-api.ts` y `lib/daily-tasks/daily-tasks-api.ts` — schemas Zod + builders puros `buildBankMovementInsert/Patch`, `buildDailyTaskInsert/Patch`. Conciliar (status='matched') sella `matched_at/matched_by`; completar tarea (status='done') sella `completed_at/completed_by`; reabrir los limpia.
- **UI Banco** (`bank-movements-page-client.tsx`): tab Movimientos operativo — cards (Pendientes/Entradas del mes/Salidas del mes/Revisados), tabla con Agregar/Editar/Conciliar/Ignorar/Reabrir/Eliminar, form inline, empty state. Importar y Conciliación siguen como placeholders (parser/matching fuera de scope).
- **UI Tareas** (`daily-tasks-page-client.tsx`): filtros Pendientes/Vencidas/Completadas/Todas, checklist con completar/reabrir, Nueva tarea, editar, eliminar, empty state.
- **Fuera de scope Sprint B**: upload de extracto, parser PDF/CSV/XLSX, matching automático, cron, generación automática de tareas. Suggestions GET queda read-only.
- **Checks**: tsc 0, suite 3853 passed (2 "failed" = timeouts de render PDF por contención con build concurrente, verdes en aislamiento), build OK, manual encoding OK. QA smoke DB net-zero (insert+rollback validó constraints/trigger, 0 filas persistidas). RLS sin cambios.

## COPILOT-BANK-MOVEMENTS-AND-DAILY-TASKS-SPRINT-A-001 — 2026-07-09 (SCAFFOLD ENTREGADO)

**Base estructural de dos módulos nuevos — sin parser, sin matching automático, sin cron:**
- **Migración PREVIEW (NO aplicada)**: `supabase/migrations/20260709000000_bank_movements_daily_tasks.sql` — tablas `bank_statement_imports`, `bank_movements`, `bank_movement_match_suggestions`, `daily_tasks`. RLS workspace-scoped (patrón Helpdesk/Tesorería, sin USING(true)). Espera aprobación de Andrés para aplicar en Supabase.
- **RBAC**: module keys `bank_movements` y `daily_tasks` en `MODULE_KEYS`, presets por rol (superadmin admin/admin; usuario none/write; cobranza none/write; tesoreria write/write; contador read/write), mapa API y labels de admin panel.
- **Rutas**: `/copilot/movimientos-bancarios` (cards + tabs Importar/Movimientos/Conciliación/Historial) y `/copilot/tareas-diarias` (cards + tabs Mis tareas/Por módulo/Completadas). Guard server-side `isModuleAccessDenied`.
- **APIs GET base**: `/api/copilot/bank-movements`, `/bank-movements/imports`, `/bank-movements/[id]/suggestions`, `/daily-tasks` — `requireCopilotModuleAccess`, workspace-scoped, fallback controlado `{ok:true,data:[],meta:{migration_pending:true}}` si las tablas no existen aún. `daily-tasks` filtra por permisos de módulo del usuario.
- **Importador viejo**: `TreasuryAdvancedToolsPanel` removido del render de Tesorería (tab Movimientos) y reemplazado por CTA a Movimientos bancarios. La lógica reusable NO se borró: `treasury-santander-import-panel.tsx`, parsers `lib/treasury/santander-*`, matching `santander-import-reconciliation.ts` + `client-transfer-alias-matching.ts`, tabla `bank_reconciliation_movements` y APIs `treasury/bank-reconciliation-movements/*` siguen intactos para migrar en Sprint B/C.
- **Manual**: sección `importador-santander` reemplazada por `movimientos-bancarios` + nueva `tareas-diarias`; bullets de Tesorería y navegación actualizados.
- **Checks**: tsc 0 errores, 3834 tests verdes (1 test de nav actualizado para derivar de MODULE_KEYS), build OK, lint-rls sin findings nuevos (31 preexistentes).
- **Pendiente Sprint B/C/D**: aplicar migración tras aprobación, upload + parser (reusar parsers Santander existentes), motor de sugerencias, confirmación de conciliación, generación de tareas diarias, migrar/retirar tabla vieja `bank_reconciliation_movements`.

## Resumen del proyecto
- SaaS financiero B2B (Summer87 Copilot) — Next.js 15 App Router + TypeScript + Supabase + Vercel.
- Integración ERP ZetaSoftware (credenciales estáticas env vars, 5 pipelines sync activos).
- Multi-tenant con workspace_company_id + RLS. 1 workspace productivo (Summer87, ~183 clientes Zeta).
- 3795/3795 tests Vitest. Deploy en Vercel. Auth magic link OTP.

## TREASURY-OVERDUE-COMMITMENTS-IN-MONTH-PROJECTION-AUDIT-001 — 2026-07-03 (IMPLEMENTADO)

**Auditoría y fix UX Tesorería — 1 archivo modificado, sin migración DB:**
- **Causa raíz confirmada (DB)**: USD 5.895,03 = UYU 235.801 / TC 40. Viene de 2 pagos manuales atrasados: `Pago Bps` (UYU 226.632) + `Vacacional Ana` (UYU 9.169), ambos con `due_date: 2026-06-22`, `status: planned`, `source: manual`, `recurring_template_id: null`. Sin conexión con recurrentes eliminados.
- **Tipo de bug**: Caso A — financieramente correcto, copy confuso. El monto es legítimo: pagos impagos de junio siguen abiertos y deben impactar la caja proyectada.
- **Fix**: `treasury-executive-forecast-panel.tsx` — "Compromisos del mes" → "Compromisos abiertos" en 4 lugares + nota "Incl. X vencidos de meses anteriores." (visible solo cuando `overdueTotals > 0`) en todos los bloques USD y moneda original.
- **Tests**: 3795/3795 pasando. tsc 0 errores.
- **Sprint A2 en pausa**: cambios en `copilot-environment-health-strip.tsx`, `currency-display-toggle.tsx`, `hoy-executive-calendar-section.tsx` preservados, no commiteados.

## COPILOT-UX-FEEDBACK-SPRINT-A-001 — 2026-07-02 (IMPLEMENTADO)

**Quick wins UX — 5 archivos modificados, sin migración DB:**
- **Default moneda**: `DEFAULT_CURRENCY_DISPLAY_MODE` → `"native"` (antes `"usd_equivalent"`). Usuarios nuevos ven Moneda original. Usuarios con `localStorage` previo mantienen su preferencia.
- **Copy "Vista USD" → "Todo en USD"**: `currency-display-toggle.tsx` + `currency-display-mode.ts`. Banner: `"Todo en USD · UYU convertidos con TC {rate}"`. Toggle: `"Todo en USD"` / `"Moneda original"`.
- **Banner accionable**: `UsdEquivalentActiveNotice` incluye botón `"Volver a moneda original"` (desktop) / `"Cambiar"` (mobile) que llama `setMode("native")` directamente.
- **Agenda cobranza removida de Hoy**: `CollectionAgendaHoyCard` eliminada de `hoy-page-view.tsx`. Reemplazada por CTA estático `"Ver agenda en Cobranza →"` → `/copilot/cobranza#cobranza-agenda`. Fetch a `/api/copilot/collection-actions` ya no se dispara desde Hoy.
- **Manual actualizado**: `sections.generated.ts` — "Vista USD" → "Todo en USD" en textos visibles al usuario.
- **Helpdesk tipos/estados**: BLOQUEADO para Sprint B. La migración `20260626000000_helpdesk_tables.sql` tiene `CHECK (type IN (...))` y `CHECK (status IN (...))` — agregar `"feature"`, `"planned"`, `"published"` requiere `ALTER TABLE ... DROP/ADD CONSTRAINT`. Requiere aprobación explícita.
- **Tests**: 3771/3771 pasando. 4 tests de `currency-display-mode.test.ts` actualizados para reflejar nuevo default y copy.

## HELPDESK-FEEDBACK-MODULE-001 — 2026-06-26 (IMPLEMENTADO)

**Mesa de ayuda completa — ruta `/copilot/mesa-de-ayuda`, module key `helpdesk`:**
- **DB**: `helpdesk_tickets`, `helpdesk_comments`, `helpdesk_attachments` con RLS workspace-scoped. Migración `20260626000000_helpdesk_tables.sql` aplicada en Supabase.
- **Storage**: bucket `helpdesk-attachments` (privado, 10 MB/file, PNG/JPG/WebP/PDF). Políticas RLS aplicadas.
- **APIs**: `GET/POST /api/copilot/helpdesk/tickets`, `GET/PATCH /api/copilot/helpdesk/tickets/[id]`, `GET/POST comments`, `GET/POST attachments`, `GET signed URL attachment`.
- **RBAC**: `helpdesk` añadido a `MODULE_KEYS`. Presets: superadmin=admin, todos los demás roles=write (incluyendo readonly). Admin ve todos los tickets del workspace; usuarios solo los suyos.
- **UI**: `HelpdeskPageClient` → lista + formulario + detalle + comentarios + adjuntos. Sidebar ítem en sección Sistema (icono HelpCircle).
- **Manual**: sección "Mesa de ayuda" agregada a `sections.generated.ts`.
- **Tests**: `lib/helpdesk-types.test.ts` (15 casos) + fix en `module-permissions.test.ts`. 3721/3721 verde.
- **tsc**: 0 errores.

## Cobranza Fase B — 2026-06-22
- **B1 API**: `POST /api/copilot/cobranza/registrar-cobro` (orquestación tesorería + evidencia cobranza, RBAC `acciones` write).
- **B2 UI**: Drawer `RegistrarCobroDrawer` en `/copilot/cobranza` — botón global + por cliente, formulario completo, integración con endpoint B1. Tests en `lib/cobranza/registrar-cobro-client.test.ts` (10 casos).

## Architectural Council — 2026-05-17 (IMPLEMENTADO)

**R-01 RESUELTO — Tenant resolver a auth.uid():**
- `supabase/sec03-01-app-users-auth-uid.sql`: columna `auth_user_id` en `app_users`, backfill por email, función actualizada con auth.uid() + fallback email temporal.
- `services/app-user-source.ts`: agregada `getAppUserByAuthUid()`.
- `lib/copilot-api-auth.ts`: JWT path usa auth.uid() primero, fallback email para usuarios pre-migración.
- **APLICADO en Supabase** (2026-05-17): backfill completado, función `copilot_current_workspace_company_id()` actualizada.
- **Pendiente eliminar fallback email** cuando todos los `app_users` tengan `auth_user_id`.

**R-02 RESUELTO — Staleness detection + alertas:**
- `lib/cron/cron-stale-check.ts`: `alertIfStale()` usando `zeta_pipeline_runs` existente.
- `zeta-sync-saldos` y `zeta-sync-vouchers` llaman `alertIfStale` al inicio del run.
- Proveedores: Slack/Discord/Email/Webhook via `lib/alerts/alert-dispatcher.ts` (ZETA-16).
- **Activar**: configurar `SLACK_WEBHOOK_URL` o `CRON_ALERT_WEBHOOK_URL` en Vercel env.

**R-03 RESUELTO — workspace_integrations para credenciales Zeta:**
- `supabase/zeta-17-01-workspace-integrations.sql`: tabla con RLS (service_role only).
- `supabase/zeta-17-02-companies-status.sql`: normaliza columna `companies.status` en repo.
- `lib/integrations/zeta/zeta-config.ts`: `loadZetaServerConfigForWorkspace(workspaceId)` con DB lookup + env fallback.
- `scripts/seed-zeta-integration-current-workspace.mjs`: seed del workspace actual.
- **APLICADO en Supabase** (2026-05-17): zeta-17-01 y zeta-17-02 aplicados. Seed ejecutado y validado (1 row activa, workspace=Summer87).
- **Pendiente de wiring**: pipelines individuales (saldos, vouchers, cuotas) deben pasar `workspaceId` a `loadZetaServerConfigForWorkspace`. Hoy usan env vars.
- **Pendiente**: encriptación de `credentials` (TODO documentado en SQL y en docs/operations/zeta-integrations.md).

**Documentación operacional creada:**
- `docs/operations/cron-reliability.md`
- `docs/operations/zeta-integrations.md`
- `docs/adr-001-zeta-credentials-per-workspace.md`

**Estado de escalabilidad:** las 3 SQL aplicadas en Supabase (2026-05-17). Seed ejecutado. Con `SLACK_WEBHOOK_URL` configurada en Vercel, el sistema está listo para incorporar cliente #2 (onboarding sin re-deploy via `workspace_integrations`). El wiring de credenciales en pipelines es opcional para el primer cliente adicional si usa las mismas credenciales de desarrollador Zeta.

**zeta-completeness-audit — Diagnóstico resuelto (2026-05-17/18):**
- Los 7 `partial` históricos fueron causados por un bug de sustracción asimétrica en `9e5d525`: `legacyUntraceableCount` se restaba del local pero NO del zeta en `computeSeverity`. Esto hacía aparecer drift artificial del 85–95% en registros CCV1.
- Fix en `a9f8911`: `computeSeverity(adjustedZetaCount, operationalLocalCount)` con `adjustedZetaCount = max(0, zetaAccepted - legacy)`. Cuando `adjustedZeta=0` → severity `"ok"` correctamente.
- El scheduled cron del 03:30 UTC aún ejecutó sobre el deployment anterior (promotion lag de Vercel). Validación manual post-deploy (01:48 UTC y 2026-05-18 ~01:13 UTC) confirmó `critical_detected=0, resync_jobs_created=0, status=succeeded`. No se requiere código ni redeploy adicional.

**Deuda técnica residual:** (1) Supabase CLI migrations, (2) ROW_CAP paginación queries financieras, (3) LLM cache, (4) encriptación credentials, (5) wiring `loadZetaServerConfigForWorkspace` en todos los pipelines.

## AUTH-PAGE-GUARDS-HARDENING-001 — 2026-06-24 (IMPLEMENTADO)

**Guards server-side RSC en páginas con datos sensibles — sin cambios de API, DB ni UI:**
- `/copilot/finanzas`: `page.tsx` → RSC con `isModuleAccessDenied("finanzas")`; lógica → `finanzas-client.tsx`.
- `/copilot/reportes`: `page.tsx` → RSC con `isModuleAccessDenied("reportes")`; lógica → `reportes-client.tsx`.
- `/copilot/clientes`: `page.tsx` → RSC con `isModuleAccessDenied("clientes")`; lógica → `clientes-page-client.tsx`.
- `/copilot/clientes/[companyId]`: `page.tsx` → RSC con `isModuleAccessDenied("clientes")`; lógica → `cliente-360-page-client.tsx`.
- Patrón: `export const dynamic = "force-dynamic"` + guard server-side antes de renderizar el client component.
- Eliminado `useCopilotPermissions()` guard en cada uno de los 4 client components (redundante, ahora en server).
- **tsc**: 0 errores. **Tests**: 3623/3623 pasando. Sin tocar APIs, DB, fórmulas ni navegación.

## DOMAIN-CLEANUP-PHASE-1 — 2026-06-23 (IMPLEMENTADO)

**Cambios de labels y estructura — sin nuevas funcionalidades, sin cambios de API ni DB:**
- `copilot-nav-config.tsx`: Acciones oculto del sidebar (`sidebarHidden: true`). Rutas siguen activas.
- `app/copilot/acciones/page.tsx`: Banner informativo "Agenda y acciones integradas en Cobranza" con CTA → `/copilot/cobranza`.
- `hoy/collection-agenda-hoy-card.tsx`: CTA "Ver agenda" → `/copilot/cobranza#cobranza-agenda`.
- `dashboard-page-client.tsx`: Tabla "Clientes con deuda activa" reemplazada por CTA a Clientes y Cartera.
- `treasury-receipts-panel.tsx`: "Cobranza del mes" → "Recibos Zeta (contable)" + CTA → `/copilot/cobranza`.
- **Labels unificados** (17+ archivos): "Total pendiente" → "Deuda actual" donde representa `deuda_activa`. Incluye Finanzas, Cartera, Cobranza, Hoy, Clientes, vistas 360 y drawers.
- `manual/page.tsx`: Flujo diario paso 3: Acciones → Cobranza. Referencias de navegación actualizadas.
- **Build**: OK. **tsc**: 0 errores. **Tests**: pre-existing Jest parse failure (293 suites, no relacionado con estos cambios).

## Estado actual
- Proyecto inicializado con Claude Skills.
- Convención de archivos temporales establecida (`temp-audits/`).
- **ZETA-11 (Tesorería manual Summer87) — Fases 1–3 en repo**. SQL idempotente `supabase/zeta-11-00` … `zeta-11-05` aplicado en Supabase (Summer87 Copilot). APIs `app/api/copilot/treasury/*` (17 rutas) + dominio `lib/treasury/*` con tests. UI `/copilot/tesoreria`: dashboard (KPIs, alertas, proyección), caja manual, conciliación Santander, obligaciones futuras; consume solo APIs treasury reales. Sin tocar ZETA-08/ZETA-09, `/copilot/datos`, integración Zeta ni dashboard home.
- **ZETA-12 (Motor financiero tesorería) — Fase 4 en repo**. Motores puros `treasury-alert-engine`, `treasury-cash-projection`, `treasury-recurring-obligations`, `treasury-insights`; APIs `projection` / `alerts` / `insights` / `recurring-obligations`; SQL `zeta-12-01` plantillas recurrentes; dashboard `/copilot/tesoreria` con KPIs, runway, proyección 30d, riesgos e insights. `zetaCollections` explícitamente vacío hasta cablear cartera. Sin tocar ZETA-08/ZETA-09 ni `/copilot/datos`.
- **Pipeline Recibos Zeta operativo** (enero–mayo 2026 sincronizado, reconciliación 100 %).
- **Pipeline Vendor Payments Zeta listo (ZETA-09-01)**: migración aplicada, smoke estructural OK, persistencia real pendiente de que Zeta devuelva ≥1 recibo de pago a proveedor para el tenant. Endpoint disponible para disparo manual (`POST /api/zeta/sync-vendor-payments`). Sin cron todavía. Sin push.
- **ZETA-10 (Tesorería / egresos / caja / bancos) PAUSADO 2026-05-12 tras audit de discovery (Opción D)**. Discovery live concluido: Zeta no expone egresos reales para este tenant. Ningún endpoint (Movimientos Caja, débitos bancarios, Compras/QueryCompras/Detalladas, Cuotas Proveedor, Saldos Pendientes Proveedor, Recibos Pagos Pendientes, Asientos) devolvió filas en las ventanas probadas. Único endpoint con datos reales: `RESTMovimientosBancariosV1Query` con 168 filas ene–abr 2026, todas `ComprobanteTipo=51` (crédito bancario / ingreso), espejo de los Recibos de Cobro ya cubiertos por ZETA-08. No se implementan tablas, ni pipelines, ni UI. Bloqueado por pendiente de negocio: confirmar con cliente/contador dónde se registran gastos, pagos a proveedores, débitos bancarios, impuestos, sueldos, transferencias y egresos operativos.
- **Motor de Decisiones — Fases 5A–5D COMPLETAS (2026-05-18/19)**. 1430/1430 tests. SQL aplicado en Supabase: `de-11-executive-briefings`, `de-12-strategic-learning`. Fases: 5A operacional (queue, state machine, analytics, automation), 5B predictivo (recovery likelihood, deterioration forecast, SLA stress, operator load), 5C ejecutivo (ExecutiveCommandCenter, 8 reglas determinísticas, TTL 15 min), 5D aprendizaje estratégico (outcome detection, action/operator/automation/forecast effectiveness engines, strategy recommendations, StrategicLearningPanel con 5 tabs, 6 APIs nuevas, TTL 60 min). Totalmente determinístico — sin ML/LLM. Panel en `/copilot/decisiones` colapsado bajo "Aprendizaje estratégico".

## Manual de uso Copilot

**Fuente de verdad (editar directamente, UTF-8):**
- `lib/copilot-manual/sections.generated.ts` — secciones web + PDF
- `lib/copilot-manual/glossary-extra.ts` — términos extra del glosario
- `lib/copilot-manual-content.ts` — TOC, orden web/PDF, module cards (no duplicar copy largo acá)

**UI:** `app/copilot/manual/page.tsx` solo renderiza; no contiene el contenido del manual.

**Validar encoding y estructura (antes de commit que toque el manual):**
```bash
node scripts/generate-copilot-manual-content.mjs --check
# alias equivalente:
node scripts/check-copilot-manual-encoding.mjs
npx vitest run lib/copilot-manual/copilot-manual-encoding.test.ts
```

**Generador legacy retirado:** `scripts/generate-copilot-manual-content.mjs` ya no lee `page.tsx` ni sobrescribe archivos. Flags `--write` / `--generate` fallan a propósito. Default y `--dry-run` solo validan.

## Últimos cambios
- 2026-06-10: **FIX-TOP-FACTURACION-NETA-USD-001 — ranking facturación neta histórica**. `billing_uyu`/`billing_usd` en portfolio = Σ facturas positivas − Σ NC (anuladas excluidas, dedupe operativo, desde 2026-01-01). Dashboard/PDF: labels «Top facturación neta histórica». Validado Dolby USD 3342,04 (antes 18592). Tests 3246/3246. Sin commit.
- 2026-06-10: **UI-PREDEPLOY-001 — ajustes visuales pre-deploy (solo layout)**. Finanzas: 3 niveles (foto actual 4 KPIs hero, actividad comercial agrupada, nivel 3 colapsable). Dashboard: eliminada tabla KPI duplicada en resumen ejecutivo. Hoy: cards más compactas, caja detallada y proyección fin de mes colapsadas por defecto. Sin cambios de cálculo, copy financiero ni navegación. Tests 3240/3240, `tsc` OK. Sin commit.
- 2026-06-10: **ZETA-SHADOW-RECONCILE FASE 1 — 8 shadows cerrados (Summer87)**. Módulo `lib/integrations/zeta/zeta-saldos-shadow-reconciliation.ts`: cierre automático cuando shadow balance>0, CCV1 emparejado único y balance≤0.005, con guard de cuotas. Feature flag `ZETA_SALDOS_SHADOW_RECONCILE=1` (opt-out `0`). Hook preventivo en `zeta-saldos-pipeline.ts` (`maybeCloseShadowSupersededByCcv1`). Scripts: `reconcile-zeta-saldos-shadows-dry-run.ts`, `reconcile-zeta-saldos-shadows-apply.ts` (snapshot rollback en `tmp/shadow-reconcile-snapshot-2026-06-10.json`). Apply ejecutado: 8/8 cerrados; `pendingAtCutoff` antes/después UYU 599.425 / USD 8.152,06 (Δ=0). Shadows restantes: 31 (28 `ccv1_still_open`, 3 `ambiguous_fallback`) → backlog FASE 2 en `tmp/shadow-reconcile-fase2-backlog-2026-06-10.json`. Audits post-apply: live-pending OK=56/saldo_mal=0, debt-rollup 10/10, sync-health PASS. Sin cambios a dedupe/cartera/dashboard/Hoy/aging. Sin commit.
- 2026-06-10: **UX-TRUST-001 — auditoría global de copy ejecutivo**. Reescritura de textos defensivos en Hoy, Dashboard, Cartera, Tesorería, Finanzas, Clientes, Reportes y manual: lenguaje de confianza (actual, vigente, registrado, al corte) sin cambios de cálculo. Constantes en `copilot-financial-ux-copy`, `copilot-hoy-ui-contract`, `copilot-financial-metrics-contract`. Tests 3240/3240. Sin commit.
- 2026-06-10: **UX-FINANZAS-001 — copy y consistencia Panorama financiero**. Sin cambios de fórmulas de ventas/cobros por período. Labels unificados: «Cobros registrados» (recibos por fecha) vs «Cobrado aplicado» (desglose Cartera). Resumen ejecutivo reordenado; comparación «Ventas vs cobros registrados» + «Diferencia operativa del mes». Deuda vencida en riesgo de cobranza alineada a `sumPortfolioOverdueDebt` (card = tabla por moneda). Manual Finanzas actualizado. Tests 3240/3240, `tsc` OK. Sin commit.
- 2026-06-10: **UI-POLISH-002B — Tesorería sin duplicación de caja**. Cards hero UYU/USD solo en header global; tab Caja muestra composición operativa (`tesoreria-cash-detail.tsx`: saldo cargado, cobros Zeta, manuales, edición saldo inicial, últimos movimientos) + registro rápido + resumen pagos. Filtros Cobranza/Movimientos/Pagos próximos preservados. Botones principales normalizados a `CopilotButton`; modales conservan `CopilotGhostButton`/`CopilotPrimaryButton`. `tsc`, build y 34 tests treasury OK. Sin commit.
- 2026-06-10: **TECH-DEBT-002 — middleware → proxy (Next.js 16)**. `middleware.ts` migrado a `proxy.ts` (`export async function proxy`); mismo matcher y lógica auth/RBAC/Zeta sin cambios. Test: `lib/proxy-convention.test.ts`. Build sin warning de convención middleware deprecada.
- 2026-06-10: **CLEANUP-LEGACY-004 — Deuda menor post-navigation**. `RELEASE_NOTES_LOCAL.md` actualizado (Operacional/Rutas fuera del sidebar; tabla de redirects). Eliminado `lib/copilot-ui-state.ts` (keys `rutas:*` sin consumidores). Registro `copilot-surface-status` sin superficies retiradas. Manual: copy «Operacional» → «Alertas» en FAQ.
- 2026-06-10: **CLEANUP-LEGACY-001/002/003 — Rutas legacy cerradas y UI huérfana retirada**. Páginas UI removidas del App Router; bookmarks con redirects permanentes en `next.config.ts` (insights → dashboard, gestion-ia → agentes, rutas/personalizacion → hoy, operacional → alertas). Deep links y CTAs repointados a `/copilot/alertas` y `/copilot/hoy`. Eliminados componentes UI sin imports activos: `components/copilot/rutas/*`, `components/copilot/operacional/*`, `rutas-flow-back-link.tsx`. APIs y motores en `lib/` intactos: `/api/copilot/rutas-hub`, `/api/copilot/rutas-snapshot`, `/api/operacional/*`, `lib/operacional/*`, `lib/copilot-rutas-*`.
- 2026-06-05: **FASE 4 — RBAC en APIs Copilot (P1-003)**. Helper `requireCopilotModuleAccess` / `requireCopilotModuleWriteAccess` en `lib/auth/copilot-module-api-auth.ts` (permisos desde `app_users` + `app_user_permissions`). 94 rutas críticas protegidas (treasury, reports, data, dashboard PDF, finanzas, cartera, clientes, acciones, manual). Matriz: `docs/security/copilot-api-rbac.md`. Tests: `lib/auth/copilot-module-api-auth.test.ts`.
- 2026-06-05: **FASE 4B — Cierre cobertura RBAC API**. +64 rutas legacy mapeadas (notifications→hoy, decision-engine/decisions/initiatives/outcomes/automation-governance/operational-actions→acciones, operational-* feeds→hoy, real-insights/rutas/executive-briefing→finanzas, intelligence-bundle/llm-briefing/strategic-recommendations→agentes, zeta integrations→datos, transfer-aliases→clientes). Registry + test cobertura: `lib/auth/copilot-api-rbac-registry.ts`, `lib/auth/copilot-api-rbac-coverage.test.ts`. Allowlist final: login/logout (público), admin/* (requireAdminContext), /me + dev-* (tenant only). Solo 3 rutas tenant-only restantes.
- 2026-06-05: **FASE 5 — Cookie sesión firmada (P1-002)**. `copilot_session` ahora es `{userId}:{role}:{companyId}:{credentialVersion}.{hmacSha256Hex}` con `COPILOT_SESSION_SIGNING_SECRET`. Cookies legadas sin firma rechazadas → **todas las sesiones activas se invalidan** al desplegar; usuarios deben re-login. Helpers: `lib/copilot-session-signing.ts`, middleware async con verificación Web Crypto. Tests: `lib/copilot-session-cookie.test.ts`, `lib/copilot-session-logout.test.ts`.
- 2026-06-05: **FASE 3B — Blindaje Manual de uso**. Fuente única: `sections.generated.ts`. Generador reemplazado por validador (`--check` / `--dry-run`, sin escritura). Guard compartido: `scripts/copilot-manual-encoding-guard.mjs`. Tests ampliados en `copilot-manual-encoding.test.ts`.
- 2026-06-05: **FASE 3 — UX confianza financiera**. Dashboard: label "Ventas del período" (chart subtitles + KPI ya canónico), disclaimer visible en modo UYU+USD separado y USD consolidado con TC (`METRIC_SEPARATED_CURRENCY_DISCLAIMER` / `METRIC_USD_CONSOLIDATED_DISCLAIMER`). Manual `sections.generated.ts` restaurado UTF-8 desde commit 8ccfd95 + sección dashboard actualizada. Lint/test anti-mojibake: `scripts/check-copilot-manual-encoding.mjs` + `lib/copilot-manual/copilot-manual-encoding.test.ts`. Validaciones: `tsc --noEmit` OK, tests afectados OK.
- 2026-06-08: **FASE DASHBOARD RESUMEN — `/copilot/dashboard` ejecutivo completo**. Nueva ruta `/copilot/dashboard` con vista ejecutiva tipo "estado del negocio": 6 KPI cards (facturado, cobrado, pendiente, cobranza %, cartera vencida, runway caja), 10 charts (CSS flex bars + SVG paths — sin librería externa), 2 tablas (top deudores, distribución monedas). Archivos: `app/copilot/dashboard/page.tsx` (~720 líneas, "use client"), `lib/copilot-dashboard-summary.ts` (funciones puras de transformación), `lib/copilot-dashboard-summary.test.ts` (unit tests invariantes financieros), `scripts/audit-dashboard-summary-consistency.ts` (script de auditoría), `components/copilot/copilot-nav-config.tsx` (BarChart3 + nav item "Dashboard Resumen"), `package.json` (script `audit:dashboard-summary-consistency`), `lib/copilot-manual/sections.generated.ts` y `lib/copilot-manual-content.ts` (TOC + sección "dashboard"). Decisiones clave: (1) `currencies` es `CurrencyReconciliation[]` — acceso vía `.find(c => c.currencyCode === cur)`; (2) carga de datos con función async inline dentro de `useEffect` (satisface React Compiler `set-state-in-effect`); (3) recarga via `reloadTick` increment. Validaciones: `tsc --noEmit` OK, 3097/3097 tests, ESLint OK, build OK.
- 2026-05-24: **UX — CTAs y navegación cruzada entre módulos Copilot**. Auditoría completa de hrefs y labels en todos los módulos. lertas/page.tsx ctionLabel(): split /tesoreria → section=pagos devuelve "Ver pagos" y /tesoreria general devuelve "Ver Tesorería"; añadidos /operacional → "Ver operacional" y /acciones → "Ver acciones"; removida rama muerta /estado; fallback "Ver" → "Ver detalle". uild-actions.ts: labels treasury "Ver tesorería" → "Ver pagos"; sync_failed label "Ver estado" → "Ver operacional" y fallback href → /copilot/operacional; default "Ver" → "Ver detalle". (Comits anteriores: Nav cleanup + placeholder pages limpiados + datos page simplificada + treasury/cash shape bug + AbortController en Hoy.) TSC clean. Nota: ?section=pagos y ?section=obligations no tienen scroll-to-section en tesorería aún — links navegan correctamente pero sin scroll behavior.
- 2026-05-22: **Tesorería Resumen — fix caja disponible alineada con Hoy**. Root cause: `treasuryCashPositionGet` no incluía `collectedFromClients` (Cartera cobros). Fix: `use-treasury-workspace` ahora llama `financial-reconciliation?mode=all_outstanding` en paralelo, extrae `carteraCollectedToDate` vía `carteraCollectedToDateFromReport`, y fusiona vía `mergeCollectedIntoCashPositions` — idéntico al pipeline de Hoy. Nuevo campo `cashPositionFailed: boolean` en el State: si el fetch falla, el dashboard muestra "No se pudo cargar la posición de caja" en ambos bloques (Caja disponible + Caja después de compromisos) en lugar de UYU 0 falso. Alertas "Caja insuficiente" no se disparan si `cashPositionFailed`. Dashboard: bloque "Caja disponible" ahora muestra línea "Cobros de clientes" cuando > 0. Subtitle actualizado. TypeScript OK, 18/18 tests relacionados OK, build OK. Sin Zeta/schema/Cartera-write.
- 2026-05-22: **Tesorería Fase 2 — Pagos operativos**. Pestaña Pagos con acciones por estado: "Pendiente" → Pagar + Editar + menú; "Vencido" → Pagar + Reprogramar + menú; "Confirmado" → Pagar + Editar + menú; "Pagado/Cancelado" → solo menú. 5 modales propios (sin `window.confirm`): PayModal (amount_final + checkbox register_cash_movement + preview impacto caja), EditModal (title/tipo/monto/moneda/vencimiento/notas), RescheduleModal (nueva fecha + warning si recurrente), CancelModal (confirma sin tocar caja), DeleteModal (warning destructivo). Bug fix: `paidObligation` ahora enruta a `/scheduled-payments/[id]/mark-paid` cuando `registerCashMovement=true` (el endpoint `/paid` no acepta `register_cash_movement`). URL `markPaidScheduledPayment` agregada a `TREASURY_API`. Helper puro `lib/treasury/treasury-obligation-actions.ts` con `getTreasuryPaymentActionsByStatus` / `formatPaymentStatusLabel` / `buildPaymentConfirmationPayload`. 12 tests nuevos, 103/103 treasury, build OK. Sin Zeta/Cartera/facturas/schema/Hoy directo.
- 2026-05-22: **Hoy × Recurrentes pausados — filtro en scheduled-payments**. `pauseRecurringPayment` no cancela instancias ya generadas; el GET `/api/copilot/treasury/scheduled-payments?include_summary=1` ahora carga plantillas inactivas y excluye obligaciones con `recurringTemplateId` (o metadata/instance key) cuyo template tiene `active: false`. Capa `lib/treasury/treasury-scheduled-outflow-eligibility.ts`; `summarizeScheduledOutflows` + listado Hoy usan el mismo filtro. Tests 20 en módulos treasury scheduled. Build OK. Sin Zeta/Cartera/facturas.
- 2026-05-21: **`/copilot/hoy` — pulido UX 3 scopes (solo UI)**. Jerarquía: estado actual (caja estimada / por cobrar / +30 primario; cobrado y manuales secundarios); período con resultado operativo destacado; proyección 30d con alertas deduplicadas (`lib/copilot-hoy-projection-display.ts`); franja atraso “N de M deudores…”, CTA Ver casos. Labels cortos + tooltips. Tests 167 vitest + build OK. Sin cálculos/Zeta/balance_amount/pipelines.
- 2026-05-21: **`/copilot/hoy` × Tesorería — egresos futuros**. Reutiliza `planned_cash_obligations` (sin tabla nueva). Capa `lib/treasury/treasury-scheduled-payments.ts` + API `/api/copilot/treasury/scheduled-payments`. UI Tesorería “Pagos futuros” con resumen UYU/USD. Hoy: sección “Próximos 30 días”, balance proyectado = por cobrar − egresos 30d, alertas sin LLM. Seed opcional `scripts/seed-treasury-scheduled-payments-demo.mjs`. Tests nuevos 94 en módulos tocados. Sin Zeta/sync/schema/balance_amount.
- 2026-05-19: **`/copilot/hoy` — cobrado alineado con Cartera**. `carteraPeriodMetricsFromReport` usa `portfolioResolvedAmount` (Cobrado aplicado), no `collectedInPeriod` bruto. % cobranza = aplicado / facturado neto. Tests 81/81, build OK. Sin sync/schema/cálculos motor.
- 2026-05-19: **`/copilot/hoy` — simplificación CEO (solo UI/copy)**. Título **Hoy en la empresa**; labels período (facturado/cobrado), **Atrasado +30 días**, nota si cobrado > facturado; sección **Clientes con deuda** con tabla expandible (8 → todos en página); eliminado **Casos a revisar hoy** y `hoy-priority-collections-table.tsx`; hero/aviso más neutros; drawer atención con scroll a deudores. Contrato `lib/copilot-hoy-ui-contract.ts`. Tests 81/81, build OK. Sin Zeta/schema/balance_amount/pipelines ni cambio de cálculos.
- 2026-05-19: **`/copilot/hoy` alineado con Cartera (mapping + UX)**. Facturado neto vía `buildCurrencyIndex` (`issuedInPeriodNet`, NC restan); cobrado `collectedInPeriod`; pendiente `pendingAtCutoff`; deudores = `countActiveDebtorClients` (Explorador); atención = subset (`clientQualifiesForAttention`); hero “N deudores / M prioritarios”; drawer todos los deudores con count dinámico y filtros +30; `clientCounts` en pulse. Tests 78/78, build OK. Sin Zeta/schema/balance_amount/pipelines.
- 2026-05-19: **`/copilot/hoy` — semántica Deuda vencida vs Cartera (Opción B + fallback A)**. Card de mora alinea con Aging de Cartera (`sumCarteraAgingOverdue`: buckets 31–60 + 61–90 + +90) cuando la página carga `GET /api/copilot/financial-reconciliation?mode=all_outstanding` en paralelo con `rutas-hub`. Si el total del portfolio coincide con `openingBalance` (fila Anterior) y no hay Aging, label **Deuda arrastrada** + helper “Saldo pendiente anterior al período actual”. Archivos: `lib/copilot-cartera-aging-totals.ts`, `lib/copilot-today-business-pulse.ts`, `app/copilot/hoy/page.tsx`, `components/copilot/hoy/hoy-page-view.tsx`, tests 75/75. Sin tocar sync, schema, `balance_amount`, pipelines.
- 2026-05-21: **Cliente 360 — Ficha premium ejecutiva + helper operacional (Fases 1–9)**. Evolución de `/copilot/clientes` y Ficha Cliente 360 de "visor de tablas" a experiencia ejecutiva accionable. **Nuevos archivos**: `lib/copilot-client-operational-summary.ts` (helper puro sin LLM: `buildClientOperationalSummary` → riskHints, suggestedActions, executiveSummary, timelineEvents, missingDataHints; heurísticas: deuda vencida, recibos recientes 60d, multi-moneda, riesgo alto/medio, contactos faltantes, sync stale); `lib/copilot-client-operational-summary.test.ts` (20 tests: sin deuda, con deuda vencida, sin contactos, multi-moneda, recibos recientes, timeline, acciones no duplicadas). **`lib/copilot-client-360.ts`**: agrega `Client360Contact` type; payload extendido con `contacts[]`, `debt_uyu`, `debt_usd`, `overdue_uyu`, `overdue_usd`, `last_receipt_date`, `last_invoice_date`, `last_sync_at`; query `proto_contacts` en `loadClientCompany360()`; tracking de `debtByCurrency`. **`components/copilot/copilot-client-360-view.tsx`**: reescritura completa — header premium (avatar, nombre, badges: deuda/riesgo/código Zeta/RUT/actividad/sync/multi-moneda), KPI row (8 chips: deuda UYU/USD, vencido UYU/USD, comprobantes, recibos, últ. movimiento, últ. sync), bloque "Lectura de Copilot" (heurísticas con severidad ok/info/warning/critical), quick actions (Ver cartera/alertas/Resumen IA disabled/Re-sincronizar disabled), 6 tabs (Resumen/Estado de cuenta/Comprobantes/Recibos/Contactos/Integración Zeta), timeline operacional en Resumen (eventos de facturas emitidas/vencidas/recibos/sync), ContactsTab con cards + email clickeable + teléfono. **`app/copilot/clientes/page.tsx`**: 4 cards mejoradas (top clientes, con deuda count, riesgo alto con tono rojo, sin contacto con tono amber), nuevos filtros "Riesgo alto" y "Actividad reciente", columna "Contacto" en tabla (Disponible/Sin contacto), acción renombrada "Ver ficha 360". Sin tocar sync Zeta, balance_amount, Supabase schema (excepto query read-only a proto_contacts), RLS, APIs financieras. `tsc --noEmit` exit 0 (fuera de test preexistente); `vitest run` 1802/1804 (2 pre-existentes); build ✓ Compiled successfully.
- 2026-05-19: **Fix sync saldos — vouchers ya no pisa `balance_amount`**. Causa raíz: `zeta-sync-vouchers` (:10) corría después de `zeta-sync-saldos` (:00) y `protoUpdateInvoice` enviaba `balance_amount: 0` del mapper. Fix mínimo: `buildZetaVoucherProtoUpdatePatch()` omite `balance_amount` en updates; creates siguen con 0 hasta saldos. Logs temporales `zeta_balance_write_diag` (`ZETA_BALANCE_WRITE_DIAG=1` o lista vigilancia). Backfill `run-saldos-sync.ts` 183 clientes → **40/40 OK** vs Excel VentasExport-1237 (UYU $196.731 / USD U$S 7.835,41). ACQUAGARDEN 2926 = USD 368,26. Archivos: `zeta-customer-vouchers-mapper.ts`, `zeta-customer-vouchers-pipeline.ts`, `zeta-balance-write-diag.ts`, `zeta-saldos-pipeline.ts`. Sin tocar UI/Cartera/Explorer/RLS/schema.
- 2026-05-19: **Explorador de deuda — búsqueda + auditoría El País**. Causa búsqueda rota: filtro solo en `companyName`; si es `null` la UI muestra `companyId` pero el match fallaba. Fix: `normalizeSearchText` + haystack (`companyName`, display, `companyId`, `zetaClientName` desde facturas en reporte). Pipeline confirmado: `baseClients` → `filtered` → `sorted` → `pageRows`. El País: fila Explorer `pendingByCurrency` = `pendingAtCutoff` cuando es único deudor; cards “Saldo anterior” es portfolio-wide (no por cliente). Archivos: `lib/copilot-search-normalize.ts`, `lib/copilot-debt-explorer-search.ts`, `lib/copilot-cartera-debt-consistency-audit.ts`, `client-debt-explorer.tsx`, `copilot-financial-reconciliation.ts`, `financial-reconciliation/route.ts`. Tests + build OK.
- 2026-05-19: **Motor de Decisiones FASE 5D — Strategic Learning Layer**. 6 engines determinísticos (outcome-detection, action-effectiveness, operator-effectiveness, automation-effectiveness, forecast-calibration, strategy-recommendation). Orchestrator + repository. 6 APIs (strategic-learning GET, action-effectiveness GET, operator-effectiveness GET, automation-effectiveness GET, forecast-calibration GET, detect-outcomes POST). `StrategicLearningPanel` con 5 tabs (recomendaciones/acciones/operadores/automatización/calibración). Integrado en executive-decision-engine (learning recommendations + calibration_score en confidence). SQL de-12 aplicado en Supabase. 38 tests nuevos, 1430/1430 totales. `tsc --noEmit` exit 0, build exit 0. Sin commit/push.
- 2026-05-18: **Motor de Decisiones FASE 5C — Executive Decision Layer + UX Consolidation**. executive-decision-engine (8 reglas), executive-command-center UI, executive-timeline-builder, executive-brief API (TTL 15 min), panel reordering con CollapsibleSection, UX polish en operational cards (priority-based border, expandable details). SQL de-11 aplicado en Supabase. 27 tests nuevos. Sin commit/push.
- 2026-05-17: **Cartera UX — Claridad semántica y transparencia operativa**. 3 archivos modificados, 0 cálculos financieros tocados. `lib/copilot-financial-ux-copy.ts`: 8 nuevas constantes (preSyncBannerTitle/Body, historicalPartialBadge, 4 kpiXxxTooltip). `components/copilot/executive-summary-cards.tsx`: renombrados 4 KPIs ("Facturado en período", "Cobrado registrado", "Saldo pendiente actual", "% cobranza registrada"); componente `KpiInfoButton` (hover+focus tooltip inline); badge "Histórico parcial" (amber) en cards afectadas cuando `isPreSync=true`; prop `isPreSync` nueva. `components/copilot/cartera-shell.tsx`: defaults `draftStart=2026-01-01`; banners dismissibles `PreSyncBanner` (período < 2026-01-01) + `RowCapBanner` (meta.truncated=true) — dismissal keyed por period para auto-reset; componente `HistoricalGapNote` con gap real calculado desde `buildCurrencyIndex`; `structuralGaps` derivado vía `useMemo`. Validaciones: `tsc --noEmit` exit 0; ESLint --max-warnings 0; build ✓ sin regresiones.
- 2026-05-16: **ZETA-16 — Enterprise Sync Production-Grade Hardening**. Fases A–H completas. Estado previo confirmado: FASE A (acknowledged drifts), FASE B (health score 5D), FASE C (daily snapshots + cron 02:00 UTC) ya existían. Implementado en este turno: **FASE D** — `lib/data/zeta-contract-snapshot-repository.ts` (append-only, getLatestSnapshots, getKnownFields); `lib/integrations/zeta/zeta-runtime-contract-guard.ts` (Zod soft-fail, quarantine, unknown field detection, snapshot output; `runContractGuard<T>` genérico). **FASE E** — UI operativa Dead Letter: `app/api/admin/zeta/resync-jobs/route.ts` (GET filtrado por status/workspace/limit), `app/api/admin/zeta/resync-jobs/[id]/route.ts` (GET detalle), `/[id]/retry/route.ts` (POST — solo failed/dead_letter; concurrency guard con `.in("status",[...])`), `/[id]/cancel/route.ts` (POST — solo pending/running; preserva record para auditoría); `app/admin/ops/resync/page.tsx` (Server Component con filtros, StatusSummary, aviso no-destructivo); `resync-jobs-table.tsx` + `resync-job-actions.tsx` (Client Components: RetryJobButton, CancelJobButton, PayloadInspector, expand row para detalle/error/payload). **FASE F** — Alerting: `lib/alerts/types.ts` (AlertEvent, AlertProvider interface, AlertDeliveryResult); providers `email.ts` (ALERT_EMAIL_ENDPOINT o Resend directo), `slack.ts` (Incoming Webhook), `discord.ts` (Embed colored), `webhook.ts` (POST JSON); `alert-dispatcher.ts` (feature-flag: solo envía si env var configurada, parallel delivery, logs estructurados). **FASE G** — `supabase/zeta-16-04-sync-metrics-history.sql` (tabla append-only con duration_ms, rows_fetched/upserted/inserted/updated/skipped/failed, retry_count, error_count, status, drift_count/pct, api_latency_ms; índices por workspace+pipeline+time y run_id); tipos `SyncMetricsHistoryInput/Row` + `ContractSnapshotInput/Row` + `AlertEvent/AlertEventType` en `zeta-enterprise-sync-types.ts`; `lib/data/zeta-sync-metrics-history-repository.ts` (appendSyncMetrics, getSyncMetricsHistory, getSyncMetricsTrend, getMetricsForRun). **FASE H** — Circuit breaker: `lib/integrations/zeta/zeta-circuit-breaker.ts` (CLOSED/OPEN/HALF_OPEN, configurable threshold/windowMs/openDurationMs, singleton registry via `getZetaCircuitBreaker`, `CircuitOpenError`); Advisory locks: `lib/integrations/zeta/zeta-advisory-lock.ts` (djb2 hash determinístico, wraps `zeta_try_advisory_lock` RPC, fail-open si RPC no disponible); `supabase/zeta-16-05-advisory-locks-rpc.sql` (pg_try_advisory_lock + pg_advisory_unlock como SECURITY DEFINER RPCs). Fix: `app/api/cron/zeta-daily-snapshot/route.ts` — TS error pre-existente en `buildSnapshot` tipando `supabase` como `SupabaseClient` en lugar de `ReturnType<typeof createClient>`. `vercel.json` — agrega cron `zeta-daily-snapshot` (0 2 * * *). **Tests**: 21 nuevos verdes — 6 contract-guard, 6 circuit-breaker, 4 alert-dispatcher, 5 health-score pre-existentes confirmados. `tsc --noEmit` exit 0. Restricciones respetadas: sin breaking changes, sin migraciones destructivas, sin inventar endpoints Zeta, sin tocar balances ni contabilidad.
- 2026-05-15: **ZETA-13 — Enterprise Sync Reliability Layer**. Sistema completo de auditoría de completeness, detección de divergencias, checks de integridad y re-sync inteligente. **SQL (4 migraciones nuevas)**: `zeta-13-01-completeness-audits.sql` (tabla `zeta_completeness_audits` — auditorías per-entidad+período+workspace con conteos Zeta vs local, missing_registro_ids[], severity, dedup por hora), `zeta-13-02-sync-divergences.sql` (tabla `zeta_sync_divergences` — divergencias campo a campo con status open/resolved/auto_fixed), `zeta-13-03-integrity-violations.sql` (tabla `zeta_integrity_violations` — violaciones financieras internas con check_name, entity, severity), `zeta-13-04-resync-jobs.sql` (tabla `zeta_resync_jobs` — cola de re-sync por entidad/scope/triggered_by con dry_run). **TypeScript (7 archivos nuevos)**: `lib/integrations/zeta/zeta-enterprise-sync-types.ts` (todos los tipos: CompletenessAuditResult, SyncDivergence, IntegrityViolation, ResyncJobRow, EnterpriseSyncHealth), `lib/integrations/zeta/zeta-completeness-audit.ts` (motor: `runInvoiceCompletenessAudit` llama Zeta para mes/anio, compara contra proto_invoices via zeta_metadata JSONB; `runReceiptCompletenessAudit` comparación precisa por RegistroId via receipt_number ZETA:COB:{id}; `runContactCompletenessAudit` count-only; `getAuditMonths` respeta límite 2026-01), `lib/integrations/zeta/zeta-integrity-checker.ts` (8 checks DB-only: invoice_balance_exceeds_total CRITICAL, invoice_null_currency WARNING, invoice_null_balance_pending WARNING, invoice_duplicate_number CRITICAL, receipt_null_currency WARNING, receipt_negative_amount INFO, installment_orphan_no_invoice WARNING, installment_overdue_invoice_open INFO; `runAllIntegrityChecks` orquesta + persiste via upsertIntegrityViolation), `lib/data/zeta-enterprise-sync-repository.ts` (CRUD completo para las 4 tablas nuevas: insertCompletenessAudit con dedup 23505, upsertSyncDivergence, upsertIntegrityViolation, createResyncJob, updateResyncJob). **APIs nuevas (3 rutas)**: `app/api/cron/zeta-completeness-audit/route.ts` (cron diario 03:30 UTC — audita current+prev month para invoices+receipts por workspace, crea resync_jobs si severity=critical), `app/api/cron/zeta-integrity-check/route.ts` (cron diario 04:00 UTC — corre los 8 checks por workspace, persiste violaciones), `app/api/zeta/resync/route.ts` (POST manual autenticado — re-sync por entidad+período; invoca syncZetaCustomerVouchers/syncZetaCollectionReceipts directamente; crea y actualiza resync_job; dry_run disponible), `app/api/copilot/enterprise-sync-health/route.ts` (GET tenant-auth — retorna últimas auditorías por entidad, violaciones abiertas, jobs recientes, EntityHealthSummary por entidad con drift y violation counts). **vercel.json**: 2 crons nuevos (30 3 * * * + 0 4 * * *). **pipeline names**: COMPLETENESS_AUDIT + INTEGRITY_CHECK en ZETA_PIPELINE_NAMES. **Validación**: `tsc --noEmit` exit 0; 151/151 tests verdes; build ✓ (4 rutas nuevas en output). Sin commit. Sin push. **Limitación conocida**: `runAllIntegrityChecks` con `checkInstallmentOverdueInvoiceOpen` retorna vacío hasta que `zeta-08-01` esté aplicado en Supabase (tabla `proto_invoice_installments` no existe aún).
- 2026-05-15: **FASE 7.3 — Guided Operational Experience**. Rediseño UX de `/copilot/rutas`: flujo secuencial "qué pasa → qué hago → qué sigue", action-first, progressive disclosure. Nuevos componentes: `rutas-operational-hero.tsx` (hero con estado, headline, CTA "Empezar ahora"), `rutas-daily-route-section.tsx` ("Ruta del día" con max 4 pasos, progreso 1/N, checkboxes), `rutas-advanced-details-section.tsx` (wrapper colapsable "Ver detalles avanzados"), `rutas-recent-activity-section.tsx` (timeline humanizado con labels en español). Renaming global: "Briefing ejecutivo" → "Lo más importante hoy"; "Inteligencia operacional" → "Por qué está pasando"; "Prioridad estratégica" → "Qué hacer ahora"; "Comando operativo" → "Pendientes importantes"; "Ejecución guiada" → "Paso a paso"; "Lectura ejecutiva" → "Impacto del problema"; "Continuidad operacional" → "Problemas recurrentes"; "Automatizaciones activas" → "Automatizaciones funcionando". Simplificación visual: `CommandCenterRow` reducido a 2 badges máximo (severidad + estado), removidos badges técnicos (SLA, recurring, reopenCount, urgencyScore). `GuidedWorkflowCard`: progress bar + "Ahora: paso", eliminados badges SLA/urgencia. Focus mode: en `status === "critical"` se ocultan secciones secundarias (`RutasOperationalIntelligenceSection` + `RutasOperationalCommandCenterSection`). Nuevo orden en body: Hero → Lo más importante hoy → Por qué está pasando → Qué hacer ahora → Ruta del día → Paso a paso → Actividad reciente → Detalles avanzados (colapsado con: DayStatus, TreasuryPressure, Automatizaciones, Impacto, Problemas recurrentes, Feed completo, Prioridad, Alertas, Insights, Rutas). Secciones técnicas (governance, memory, narratives, full feed) movidas a advanced. Validación: `tsc --noEmit` exit 0; 151/151 tests verdes; build ✓ (`/api/copilot/intelligence-bundle` confirmado). Sin commit. Sin push.
- 2026-05-15: **FASE 7.2 — Intelligence Bundle + Unified Runtime**. Bundle endpoint `/api/copilot/intelligence-bundle` llama `buildOperationalWorkflows` exactamente una vez y ensambla briefing + intelligence + command center en un único pase. Contexto feed actualizado: bundle-first con fallback a 3 endpoints individuales. Componentes actualizados para usar contexto (sin fetch propio): `RutasExecutiveBriefingSection`, `RutasOperationalIntelligenceSection`, `RutasActiveAutomationsSection`. 22 tests de bundle. Validación completa.
- 2026-05-13: **ZETA-12 — Motor financiero e inteligencia de tesorería (Fase 4)**. Capa incremental sobre ZETA-11: motores puros de alertas, proyección de caja, insights ejecutivos y obligaciones recurrentes; servicios/APIs `app/api/copilot/treasury/{projection,alerts,insights,recurring-obligations}`; SQL `supabase/zeta-12-01-recurring-obligation-templates.sql`; dashboard `/copilot/tesoreria` ampliado (alertas críticas, runway, gráfico 30d, riesgos, insights). Proyección sin cobranzas Zeta (`zetaCollections: []`) hasta integración explícita con cartera. Validación: `tsc --noEmit`, `vitest lib/treasury`, eslint archivos tocados. Sin push.
- 2026-05-13: **ZETA-11 — UI Tesorería `/copilot/tesoreria` (Fase 3)**. Módulo App Router con tabs Dashboard / Caja manual / Conciliación Santander / Obligaciones; barra sticky (rango fecha, moneda, refresh); hook `use-treasury-workspace` (fetch paralelo + mutaciones API); helpers `lib/treasury/treasury-dashboard.ts` (KPIs, alertas, match suggestions, proyección); feedback inline tipo banner. Smoke API treasury validado post-SQL; smoke visual Playwright en `e2e/smoke-copilot.spec.ts`. Validación: `tsc --noEmit`, `vitest lib/treasury`, eslint archivos tocados. Sin commit/push; `stash@{0}` intacto. Audit técnico de descubrimiento read-only (sin migraciones, sin pipelines, sin UI, sin commit). **Endpoints relevantes enumerados** desde `docs/zeta/reference/ZetaSoftware-REST-10-2025.json` (262 endpoints totales, 57 matches en dominios tesorería/caja/bancos/proveedores/movimientos/asientos; 9 operacionales + 10 catálogos). **9 endpoints operacionales probados live** contra `EmpresaCodigo=250218923`: `RESTFacturaProveedorV1QuerySaldosPendientes`, `RESTCuotasV1QueryProveedor`, `RESTRecibosPagosV1QueryPendientes`, `RESTFacturaProveedorV1QueryCompras`, `RESTFacturaProveedorV1Compras`, `RESTFacturaProveedorV1ComprasDetalladas`, `RESTMovimientosCajaV1Query`, `RESTMovimientosBancariosV1Query`, `RESTAsientoV1Lista`. **Resultado live (todos HTTP 200, envelope correcto)**: 8/9 endpoints devuelven `Response[]` vacío en abr-2026, dic-2025 y "now"; **único endpoint con datos reales = `RESTMovimientosBancariosV1Query` con 168 filas ene–abr 2026 ($2.673.088,60 agregado)**. Barrido profundo + verificación de `ComprobanteTipo=52,53,54,55` (débitos bancarios) confirma **0 débitos bancarios en TODO el rango ene-abr 2026**. Los 168 movimientos son 100% `ComprobanteTipo=51` (Crédito Bancario), 100% banco Santander, 98 en cta UYU + 70 en cta USD, `Descripcion="Recibo de Cobro"` predominante, `ConceptoCodigo`/`ClienteProveedor`/`ProveedorNombre` 100% vacíos, `Conciliado="N"`. **Conclusión arquitectónica**: para este tenant los movimientos bancarios visibles son espejo de los Recibos de Cobro ya cubiertos por ZETA-08 (no agregan información nueva), y **no hay egresos reales (pagos a proveedores, débitos, gastos, transferencias, impuestos, sueldos) registrados en Zeta**. Implementar un módulo "Tesorería" hoy sincronizaría 0 filas de egreso, repitiendo el escenario de ZETA-09-01. **Decisión usuario: Opción D — pausar implementación y devolver descubrimiento al stakeholder**. NO se crearon tablas, NO se crearon pipelines, NO se tocó UI, NO commit. `stash@{0}` intacto. **Artefactos read-only generados en `temp-audits/`** (todos untracked, no afectan build): scripts `zeta-10-enumerate-endpoints.mjs`, `zeta-10-extract-bodies.mjs`, `zeta-10-audit-tesoreria.mjs`, `zeta-10-audit-deep-bancos.mjs`, `zeta-10-audit-debits.mjs`; outputs `temp-audits/output/zeta-10-audit-tesoreria-*.json`, `zeta-10-audit-deep-bancos-*.json`, `zeta-10-audit-debits-*.json`. **Recomendación arquitectónica registrada para cuando se reanude (post-confirmación de fuente real de egresos)**: NO mapear a `proto_payments` (mezcla semánticas con CxP / recibos de pago a proveedores); crear `proto_bank_movements` y `proto_cash_movements` independientes, ambas con `workspace_company_id`, `company_id NULL`, `source='zeta'`, `zeta_metadata jsonb`, `direction='credit'|'debit'` derivado de `ComprobanteTipo`, `account_external_id` (CuentaCodigo), `external_registro_id` (RegistroId), `concepto_code/nombre`, `is_conciliated` (Conciliado='S/N'); mantener `proto_payments` como hoy; si se requiere "Flujo de Caja Realizado" unificado, crear vista SQL `proto_treasury_flow_v1` con UNION ALL de receipts + bank/cash movements + payments liquidados.
- 2026-05-12: **ZETA-09-01 — Sync vendor payments Zeta → `proto_payments` (cierre técnico, persistencia real pendiente de datos)**. Implementación end-to-end del pipeline de pagos a proveedores desde la fuente canónica `RESTRecibosPagosV1QueryComprobantes`, siguiendo 1:1 el patrón del módulo Recibos cliente. **Archivos nuevos**: (1) `supabase/zeta-09-01-proto-payments-zeta-sync.sql` (migración idempotente: `company_id DROP NOT NULL`, columnas `currency_code text` + `source text default 'manual'` + `zeta_metadata jsonb`, CHECK `source IN ('manual','zeta')`, CHECK `currency_code IN ('USD','UYU') OR NULL`, 3 índices nuevos `(workspace_company_id, payment_number)`, `(workspace_company_id, source)`, parcial `(workspace_company_id, (zeta_metadata->>'zeta_registro_id')) WHERE source='zeta'`). (2) `lib/integrations/zeta/zeta-vendor-payments-rest-method.ts` (constantes default `RESTRecibosPagosV1QueryComprobantes` + `QueryComprobantesIn`, env-overridable). (3) `lib/integrations/zeta/contracts/zeta-vendor-payments.contract.ts` (tolerancia a varios shapes JSON, extracción `Response[]`, detección `IsLastPage`/`Succeed`, `summarizeZetaVendorPaymentsResponseShape`). (4) `lib/integrations/zeta/zeta-vendor-payments-fetch.ts` (build payload, retry 3x backoff, structured logging `zeta_vendor_payments_payload_shape` + `zeta_vendor_payments_raw_response`, clasificación de error_code). (5) `lib/integrations/zeta/zeta-vendor-payments-mapper.ts` (`mapZetaVendorPaymentToCopilot` → `CopilotVendorPaymentV1`; `mapCopilotVendorPaymentToProtoPaymentInput` con reglas: `payment_number = "ZETA:PAG:${RegistroId}"`, `payment_date` ISO, status `paid|void` desde `Emitido`, `category = ComprobanteNombre`, `vendor_name = ProveedorRazonSocial || ProveedorNombre`, `reference = Serie+Numero`, `currency_code` USD/UYU normalizada desde `MonedaSimbolo/MonedaCodigo`, `company_id=null`, `obligation_id=null`, `source='zeta'`, `zeta_metadata` con ids/códigos útiles, `notes.zeta_vendor_payment_v1.raw_payload` completo; rechazos `invalid_fecha`, `invalid_amount`, `negative_amount`, `pre_operational_date`). (6) `lib/integrations/zeta/zeta-vendor-payments-pipeline.ts` (`syncZetaVendorPayments` con paginación, registro `zeta_sync_runs`/`zeta_sync_state` con `resource_flow="zeta_vendor_payments_v1"`, upsert idempotente por `(workspace_company_id, payment_number)` vía `protoCreate/UpdatePayment` con `allowUnlinkedCompany:true`, structured logging `zeta_vendor_payments_sync` con kinds `page`/`row_skip_mapper_rejected`/`summary`). (7) `app/api/zeta/sync-vendor-payments/route.ts` (POST manual autenticado, Zod body `{mes, anio, proveedorCodigo?, comprobanteCodigo?, monedaCodigo?, localCodigo?}`). (8–10) Tests unitarios `contract.test.ts`, `fetch.test.ts`, `mapper.test.ts` (todos verdes). **Archivos modificados**: `lib/copilot-proto-crud-types.ts` (`PROTO_PAYMENT_STATUSES` agrega `"void"`, `ProtoPaymentInput.company_id: string|null`, agrega `currency_code?`/`source?`/`zeta_metadata?`); `lib/copilot-data-integrity.ts` (`validatePaymentIntegrity(input, options)` con `allowUnlinkedCompany`); `lib/copilot-proto-crud-service.ts` (firmas de `protoCreatePayment`/`protoUpdatePayment` aceptan `options.allowUnlinkedCompany`, persisten `currency_code`/`source`/`zeta_metadata` cuando se proveen; `company_id` se persiste como `null` solo bajo opt-in); `app/copilot/datos/datos-client.tsx` (bloque "Pagos" agrega columnas a la derecha `vendor_name` / `currency_code` / `caja_nombre`, search keys actualizados, nuevo `enrichPaymentRows` que copia `zeta_metadata.caja_nombre` al row top-level cuando existe; otras entidades intactas). **Validaciones del pipeline**: ignora registros sin `RegistroId`, sin fecha válida, anteriores a `COPILOT_OPERATIONAL_START_DATE`, montos ≤0 o negativos. **Aplicación SQL en Supabase** (read-only post-migración vía MCP confirmó): `company_id IS_NULLABLE=YES`; `currency_code text NULL`; `source text NULL DEFAULT 'manual'::text`; `zeta_metadata jsonb NULL`; ambos CHECK creados; 3 índices nuevos creados con definiciones exactas; backfill = no-op porque `proto_payments` tenía 0 filas. **Smoke estructural ejecutado vía runner `temp-audits/smoke-sync-vendor-payments.mts`** (`npx tsx temp-audits/smoke-sync-vendor-payments.mts [mes] [anio]`, importa el mismo `syncZetaVendorPayments` que usa el endpoint con `SUPABASE_SERVICE_ROLE_KEY` + `WORKSPACE_COMPANY_ID` del `.env.local`; equivalente al POST sin pasar por auth Next): 3 ventanas probadas (5/2026, 4/2026, 12/2025) → todas HTTP 200 con `QueryComprobantesOut.Response[]` vacío (`payload_size_bytes:73`). 3 `zeta_sync_runs` registrados (1× bootstrap + 2× incremental), todos `succeeded`, `records_fetched=0`. `zeta_sync_state.zeta_vendor_payments_v1` con `bootstrap_completed=true`, watermark `{"last_sync_at":"2026-05-12T15:51:00.385Z","mes":"12","anio":"2025"}`. **Revisión UI `/copilot/datos → Pagos`**: dev server compila la página (`GET /copilot/datos` 200, ~48.9 KB) y el endpoint dataset (`GET /api/copilot/dataset?active=active` 200, `payments=[]`, `receipts=234`, `invoices=348`, `companies=183`) — el componente accordion mantiene lazy-load (`fetchEntityBlock` solo cuando `expandedEntity != null`); empty state heredado de `CopilotDataTable` muestra "No hay registros para esta vista."; `listProtoPayments` usa `select("*")` por lo que las columnas nuevas viajarán automáticamente cuando existan filas. **Validación final**: `npx tsc --noEmit` exit 0; `npx vitest run` **836/836** verdes (incluye 3 nuevos suites Pagos Zeta); `npx eslint` sobre los 13 archivos tocados → 0 errores, 8 warnings preexistentes en `lib/copilot-proto-crud-service.ts` (no relacionadas con este cambio). **Decisiones arquitectónicas**: (a) `source='zeta'` como columna explícita para auditabilidad. (b) `company_id NULL` para pagos a proveedores (no se crea `proto_vendors` en este pase; los pagos Zeta son a proveedores no a clientes, y replicar el patrón de Recibos unlinked). (c) `allowUnlinkedCompany` como opt-in en `protoCreate/UpdatePayment` (centraliza validación; el flag mantiene la integridad de pagos manuales sin tocar el CRUD existente). (d) Upsert idempotente por `(workspace_company_id, payment_number)` con `payment_number = "ZETA:PAG:${RegistroId}"`. (e) Sin impacto en tesorería/bancos/movimientos de caja (módulos no implementados aún). (f) Sin impacto en `financial-reconciliation` / `cartera` / `installments` / `saldos` / `due_date_confidence` / dashboards / escenarios / insights (verificado por scope del diff). **Limitaciones conocidas y aceptadas**: (1) la empresa actual (`EmpresaCodigo=250218923`) devuelve `Response[]` para las 3 ventanas probadas (puede ser que no haya actividad de pagos a proveedores en Zeta, que el `RolCodigo=1` no tenga acceso al recurso, o que la empresa use otro circuito de pagos). (2) Persistencia real con datos Zeta queda **pendiente** hasta que el endpoint devuelva ≥1 fila — `proto_payments` sigue con 0 filas. (3) Mapper + persistencia están cubiertos por unit tests verdes; cuando Zeta devuelva filas, las reglas (`payment_number="ZETA:PAG:*"`, `source='zeta'`, `company_id=null`, `zeta_metadata`, `notes.raw_payload`) aplicarán por contrato testeado. (4) El smoke runner `temp-audits/smoke-sync-vendor-payments.mts` queda disponible para validar mapper + persistencia con datos reales cuando aparezcan, sin tocar producción. **Estado final: ZETA-09-01 implementado + validación estructural OK + validación real pendiente de datos Zeta**. Commit `e716097` ya creado (no push). `stash@{0}` (ZETA-08-live bucket B) sin tocar.
- 2026-05-12: **ZETA-08 — Pipeline Cuotas end-to-end + due_date real + aging real (Fases 1–9)**. Implementación completa del pipeline `proto_invoice_installments` para reemplazar `due_date = issue_date + 30d` por la fecha real de vencimiento desde `RESTCuotasV1QueryCliente`. **FASE 1 (DB)**: `supabase/zeta-08-01-proto-invoice-installments.sql` crea la tabla con todos los campos pedidos (`workspace_company_id`, `invoice_id` nullable FK con `on delete set null`, `zeta_registro_id`, `cliente_codigo`, `cuota_numero`, `cuota_total`/`cuota_saldo` numeric(18,5), `cuota_vencimiento date NOT NULL`, `moneda_codigo`, `currency_code` CHECK ∈ {UYU,USD,null}, `es_entrega_inicial`, `raw_payload jsonb`, `synced_at`/`created_at`/`updated_at`), UNIQUE `(workspace_company_id, zeta_registro_id, cuota_numero)`, 5 índices (workspace, invoice partial, vencimiento, saldo partial, registro_id), trigger fail-closed `copilot_proto_row_force_workspace`, RLS completa por tenant. `supabase/zeta-08-02-proto-invoices-due-date-source.sql` agrega `proto_invoices.due_date_source` (`synthetic_30d` | `zeta_cuotas_v1`) con backfill defensivo que marca filas existentes con `synthetic_30d` + índice parcial para queries de aging real. **FASE 2 (LINK)**: `lib/integrations/zeta/zeta-installments-link.ts` con `findActiveProtoInvoiceIdByRegistroId` (single) y `findActiveProtoInvoiceIdsByRegistroIds` (batch) — reusa los 3 JSON paths conocidos (identidad explícita → voucher v1 → raw_payload histórico) del módulo `zeta-proto-invoice-registro-match`, devuelve `Map<registroId, invoiceId|null>`. **FASE 3 (PIPELINE)**: `lib/integrations/zeta/zeta-installments-pipeline.ts` orquesta fetch paginado (`fetchZetaInstallments`) → mapper batch (`mapZetaInstallmentsBatch`) → linker batch → upsert idempotente `onConflict='workspace_company_id,zeta_registro_id,cuota_numero'` → actualización condicional de `proto_invoices.due_date` con `min(cuota_vencimiento de cuotas con saldo>0)` y `due_date_source='zeta_cuotas_v1'`. **Reglas duras**: nunca escribe NULL en due_date (regla explícita del usuario), no toca `balance_amount`, raw_payload truncado a 8K chars max, retorna `{rows_fetched, rows_mapped, rows_discarded, rows_upserted, rows_linked, rows_orphan, invoices_due_date_updated, stopped_reason, errors, warnings}`. **FASE 4 (CRON)**: `app/api/cron/zeta-sync-cuotas/route.ts` con auth `CRON_SECRET`, anti-overlap 6h, retry exponencial via `withZetaRetry`, cap configurable `ZETA_CUOTAS_CRON_MAX_CLIENTS_PER_WORKSPACE` (default 200), delay 400ms/page + 600ms/cliente. `vercel.json` agrega schedule `0 */6 * * *`. `ZETA_PIPELINE_NAMES.CUOTAS = "zeta-sync-cuotas"` agregado al registry. **FASE 5 (BACKFILL)**: `app/api/zeta/sync-installments-backfill/route.ts` (POST autenticado con `CRON_SECRET`) procesa UN workspace, acepta `{workspaceCompanyId, clienteCodigos?, maxClients, updateInvoiceDueDate, pageDelayMs, clientDelayMs, maxPagesPerClient, dryRun}` y devuelve summary completo por cliente + totales. Script CLI `scripts/zeta-backfill-installments.mjs` con banderas `--clientes <CSV>` `--max <N>` `--dry-run` `--no-due-date`. **FASE 6 (DUE DATE REAL)**: `ProtoInvoiceInput.due_date_source` agregado al type system. Mapper voucher (`zeta-customer-vouchers-mapper.ts`) y `zetaInvoiceToProtoInput` en saldos pipeline ahora escriben `due_date_source: 'synthetic_30d'` al crear. Protección clave: nuevo helper `buildSaldosDueDatePatch()` en `zeta-saldos-pipeline.ts` lee `due_date_source` antes de cada update de invoice existente y OMITE `due_date`+`due_date_source` del patch cuando el actual es `zeta_cuotas_v1` (evita pisar real con sintético entre crons de saldos y cuotas que corren a distintas frecuencias). **FASE 7 (AGING REAL)**: motor `lib/copilot-financial-reconciliation.ts` extendido. `InvoiceInput.due_date` y `due_date_source` opcionales. Nueva función `resolveAging(inv, nowMs)` decide entre `computeAgingRangeByDueDate` (semántica "days past due", solo si `due_date_source==='zeta_cuotas_v1'` Y `due_date` parseable) y el fallback sintético `computeAgingRange(issue_date)`. `AgingBucket` extendido con `realDueDateCount` + `syntheticDueDateCount`. `CurrencyReconciliation.agingSource` nuevo (`AgingSource = 'real'|'synthetic'|'mixed'|'none'`) derivado per-currency desde el conteo de fuentes. Route `app/api/copilot/financial-reconciliation/route.ts` ahora hace `.select(..., due_date, due_date_source, ...)` y los propaga al motor. **FASE 8 (TESTS)**: 19 tests nuevos verdes (`lib/integrations/zeta/zeta-installments-pipeline.test.ts` 7 casos: linkage match/huérfano, multi-cuotas con MIN due_date, USD/UYU separados, cuotas vencidas, idempotencia 2 corridas, NUNCA null en due_date; `lib/copilot-financial-reconciliation-aging-real.test.ts` 12 casos: vigente, vencido 0-30/31-60/61-90/90+ con due real, fallback synthetic source/null/null due_date, agingSource real/synthetic/mixed/none, multi-currency independiente, multi-cuotas usando min). Suite total 808/808 verdes (789 previos + 19 nuevos). **FASE 9 (VALIDACIÓN)**: `npx tsc --noEmit` exit 0; `npx vitest run` 808/808; `npx eslint` 0 errores nuevos (warnings preexistentes intactos). Docs sincronizados: `docs/vendors/z/KNOWN-DIVERGENCES.md` DIV-CONT-001 marcada MITIGADO POR ZETA-08 con plan implementado, DIV-CONT-006 cambiada a ✅ IMPLEMENTADO (de "DISPONIBLE pendiente implementación"). **Validación live contra Zeta PDF (Acquagarden, El País, cliente con cuotas vencidas) queda pendiente para próximo turno con credenciales y tras correr migraciones + backfill en Supabase real.** NO se tocó (por restricción explícita): aplicaciones recibo↔factura (DIV-CONT-005 sigue bloqueado por Zeta), FX, dashboard UI complejo, scoring IA, automatizaciones.
- 2026-05-11 (tarde): **Zeta Endpoints Revalidation con Colección 10-2025 + DIV-CONT-009 RESUELTO + DIV-CONT-010 documentado**. El usuario subió la colección Postman vigente `docs/zeta/reference/ZetaSoftware-REST-10-2025.json` (262 endpoints, variable oficial `baseUrl = "https://api.zetasoftware.com/rest"`) + guía PDF `Postman-Ejemplo-de-consultas.pdf`. **Hallazgo crítico que invalida el bloqueo reportado por la mañana**: el audit script `temp-audits/audit-zeta-due-date-shape.mjs` (línea 34) leía `process.env.ZETA_BASE_URL` (variable NO definida en `.env.local`) y caía al default `"https://api.zetasoftware.com"` SIN el segmento `/rest`. URL final probada: `.../APIs/RESTCuotasV1QueryCliente` → HTTP 404 (página HTML genérica). Mientras tanto, el código de producción (`lib/integrations/zeta/zeta-config.ts:4`) usa `DEFAULT_BASE = "https://api.zetasoftware.com/rest/APIs"` con el segmento `/rest/` correcto que la colección 10-2025 confirma. **Re-prueba live controlada** (2026-05-11 tarde) con dos scripts nuevos: (1) `temp-audits/audit-zeta-cuotas-path-reprobe.mjs --cliente-codigo 2` probó AMBAS variantes — A) `https://api.zetasoftware.com/APIs/RESTCuotasV1QueryCliente` → 404 con HTML "404 Not found" (reproducción del bug); B) `https://api.zetasoftware.com/rest/APIs/RESTCuotasV1QueryCliente` → **HTTP 200** con `QueryClienteOut.Response[]` con 2 cuotas reales para Acquagarden: `RegistroId="2527", CuotaNumero=1, CuotaSaldo="368.26", CuotaTotal="678.32", CuotaVencimiento="2026-04-10", MonedaCodigo=2 (USD)`. Validación cruzada vs PDF Zeta "Comprobantes Pendientes — ACQUAGARDEN" exacta (A2874 saldo USD 368,26). **DIV-CONT-009 cerrado como RESUELTO** — no era bloqueo Zeta, era bug del audit script. (2) `temp-audits/audit-zeta-2025-reprobe.mjs` probó los demás endpoints solicitados: `RESTQuerySaldoPendienteCliente` → HTTP 404 con JSON real `{"error":{"code":404,"message":"Not Found"}}` (es alias conceptual del PDF, NO existe como endpoint REST; el canónico es `RESTFacturaClienteV4QuerySaldosPendientes` que ya está en uso — **DIV-CONT-010 documentado**); `RESTFacturaClienteV4QuerySaldosPendientes` → HTTP 200, 2 rows para Acquagarden, sample keys 25 campos incluyendo `CondicionCodigo/CondicionNombre` (info no aprovechada hoy, posible enrichment futuro); `RESTRecibosCobranzaV2Load` probado live con `RegistroId=1911` real → HTTP 200 pero **devuelve solo cabecera** (`CajaCodigo, ClienteCodigo, CobradorCodigo, ComprobanteCodigo, Cotizacion, Descripcion, Fecha, LocalCodigo, MonedaCodigo, Notas, Numero, RegistroId, Serie, Total, UsuarioCodigo`) sin arrays de aplicaciones — **DIV-CONT-005 (aplicaciones recibo↔factura) sigue vigente como bloqueo real**; `RESTRecibosCobranzaV2QueryPendientes` (nuevo endpoint del catálogo 2025, body root `QueryPendientesIn`) → HTTP 200, 0 rows para Acquagarden, **endpoint funcional listo para futuro módulo de anticipos**. **Comparación de endpoints en uso vs nueva colección**: los 5 endpoints REST activos en producción (`RESTContactosV3Query`, `RESTComprobantesClienteV1Query`, `RESTFacturaClienteV4QuerySaldosPendientes`, `RESTFacturaClienteV4VentasDetalladas`, `RESTRecibosCobranzaV2QueryComprobantes`) coinciden 1:1 con sus contrapartes en la colección 10-2025 (mismos nombres, mismos wrappers `QueryIn`/`QuerySaldosPendientesIn`/`VentasDetalladasIn`/`QueryComprobantesIn`/`QueryClienteIn`, misma ruta canónica `/rest/APIs/`). **Búsqueda exhaustiva en la colección 10-2025** de variantes del endpoint Cuotas pedidas por el usuario: ninguna de las 6 alternativas (`RESTCuotasV1Query`, `RESTCuotasClientesV1Query`, `RESTFacturaClienteV4QueryCuotas`, `RESTVencimientosV1Query`, `RESTComprobantesCuotasV1Query`, `RESTCuotasV1QueryByCliente`) existe — solo `RESTCuotasV1QueryCliente` y `RESTCuotasV1QueryProveedor` (out-of-scope). **Docs actualizados**: `docs/vendors/z/KNOWN-DIVERGENCES.md` con DIV-CONT-009 ahora marcado RESUELTO + bloque completo de evidencia y lección operativa (todo audit live debe usar `loadZetaServerConfig()` o el mismo `baseUrl` que producción), DIV-CONT-010 nueva entrada para el alias del PDF, DIV-CONT-006 actualizado con "ENDPOINT DISPONIBLE LIVE" en lugar de "EN MIGRACIÓN". `docs/zeta-missing-financial-endpoints.md` con header re-fechado, matriz revisada (Cuotas pasa de 🚫 BLOQUEADO a ✅ DISPONIBLE, alias del PDF documentado como fila aparte, recibos Load probado), sección 2 reescrita completamente con evidencia live + recomendación técnica (migración SQL + cron + backfill — no implementado en este turno por restricción explícita del usuario "solo auditar, probar live, documentar"). `docs/zeta-financial-coverage-audit.md` con nuevo Update box y módulo 9 cambiado de "BLOQUEADO endpoint" a "READY-TO-IMPLEMENT". **Evidencia archivada** en `temp-audits/output/`: `cuotas-reprobe-A-bug-2.json` (404 reproducible), `cuotas-reprobe-B-canon-2.json` (200 con shape correcto), `2025-reprobe-1-saldopendientecliente-ghost.json` (404 del alias), `2025-reprobe-2-saldos-canonico.json` (200 del canónico), `2025-reprobe-3-recibo-load.json` (cabecera sin aplicaciones), `2025-reprobe-4-recibos-pendientes.json` (nuevo endpoint funcional). **Conclusión consolidada respondiendo las 4 preguntas del usuario**: (1) **SÍ existe endpoint REST usable para vencimiento/cuotas reales** — `RESTCuotasV1QueryCliente` validado live. (2) **NO existe endpoint REST usable para aplicación recibo↔factura** — confirmado por re-prueba live de `RESTRecibosCobranzaV2Load`. (3) **`RESTQuerySaldoPendienteCliente` y `RESTFacturaClienteV4QuerySaldosPendientes` son el mismo endpoint conceptual**; el primero es alias del PDF que NO existe como ruta REST; el segundo es el canónico y ya está en uso. (4) **Endpoints realmente bloqueados por Zeta**: aplicaciones recibo↔factura (DIV-CONT-005), estado de cuenta oficial (DIV-CONT-007), saldos a fecha histórica con FechaCorte (DIV-CONT-003 mitigable con snapshots locales), FX precomputadas (DIV-CONT-008 calculable localmente). **Endpoints implementables YA**: pipeline Cuotas (resuelve DIV-CONT-001), futuro módulo de anticipos con `RESTRecibosCobranzaV2QueryPendientes`. **NO se tocó** (por restricción explícita del usuario): DB schema, migraciones, pipelines, UI, cron, sync existente. Solo: audit scripts + docs + recomendaciones técnicas.
- 2026-05-11: **Zeta Missing Financial Endpoints — Discovery + backend Cuotas (FASES 1–6 parciales)**. Auditoría exhaustiva de los **260 endpoints REST oficiales** de Zeta (`docs/zeta/postman/Api ZetaSoftware collection.json`) para certificar qué gaps de la base financiera son implementables HOY y cuáles están bloqueados por API. **Discovery (FASE 1)**: nuevo documento `docs/zeta-missing-financial-endpoints.md` con matriz `Necesidad / Endpoint candidato / Existe doc / Existe código / Probado / Resultado / Acción` para los 5 gaps (Aplicaciones, Vencimiento real, Estado de cuenta, Saldos a fecha, Diferencias de cambio). **Endpoints REST oficiales confirmados disponibles**: `RESTCuotasV1QueryCliente` (con `CuotaVencimiento` REAL + `RegistroId` linkeable con `proto_invoices.zeta_metadata.zeta_customer_voucher_v1.zeta_registro_id`), `RESTCondicionesPagoV1Query` (catálogo), `RESTMonedasCotizacionesV1Query` (cotizaciones por fecha — `CotizacionComercial`/`CotizacionFiscal`), `RESTAsientoV1Lista` + `RESTPlanCuentasV2Query` (alternativa para Estado de Cuenta vía asientos contables). **Endpoints REST oficiales BLOQUEADOS**: tras revisar los 260, **NO existe**: `RESTEstadoCuenta*`, `RESTCuentaCorriente*`, `RESTSaldos*Fecha*`, `RESTDiferenciaCambio*`, ni ningún endpoint que exponga aplicaciones recibo↔factura (la doc oficial 0165 confirma: "este método no asigna formas de pago"). `RESTRecibosCobranzaV2Data/Load` también solo traen cabecera. **Scripts read-only de auditoría live (FASES 2–5)**: 4 nuevos `.mjs` en `temp-audits/`: (1) `audit-zeta-receipt-data-shape.mjs` prueba el endpoint `Data` con 5 recibos reales (pago completo / parcial / USD / UYU / cliente con varias facturas) y busca recursivamente keys "aplicaciones/imputaciones/cancelaciones/detalle" — el script guarda JSON sanitizados en `temp-audits/output/` para auditoría manual. (2) `audit-zeta-due-date-shape.mjs` llama `RESTCuotasV1QueryCliente` con un cliente con deuda > 0, inspecciona shape de retorno (`RegistroId/CuotaVencimiento/CuotaSaldo/CuotaTotal/MonedaCodigo/EsEntregaInicial`), busca keys de vencimiento en payloads ya persistidos en `zeta_metadata`, y compara contra `due_date` sintético. (3) `audit-zeta-account-statement-endpoints.mjs` prueba `RESTAsientoV1Lista`, `RESTBalanceV1Query`, `RESTPlanCuentasV2Query` y `RESTFacturaClienteV4QuerySaldosPendientes` con `FechaHasta` simulada en el pasado para verificar si filtra por corte (no, filtra por emisión). (4) `audit-zeta-exchange-differences-shape.mjs` valida shape de `RESTMonedasCotizacionesV1Query`, busca recursivamente `Cotizacion` en payloads de recibo (`Load`) y factura (`VentaDetallada`) para confirmar que el cálculo local de FX es factible. **Implementación SEGURA backend (FASE 6 parcial)**: contract puro `lib/integrations/zeta/contracts/zeta-installments.contract.ts` (4 shapes soportados con tolerancia camel/Pascal/snake — `QueryClienteOut`/`QueryOut`/array raíz/`Cuotas|Items|Data` interno; filtrado por `RegistroId`+`CuotaNumero`; `countZetaInstallmentsDiscarded()` para que el caller loggee con contexto completo; `summarizeZetaInstallmentsResponseShape()` para logs estructurados). Fetch HTTP `lib/integrations/zeta/zeta-installments-fetch.ts` (`buildQueryClienteInData()` con defaults amplios para vencimiento/saldo y exclusión defensiva de opcionales vacíos — evita 400 ASP.NET binder; `resolveHasMore()` con heurística `rows >= 500` y warning estructurado; `fetchZetaInstallments()` con logging completo `zeta_installments_payload_shape` + `zeta_installments_raw_response`; clasificación de errores `zeta_config/http/shape/timeout/rate_limit/unknown`). Mapper puro `lib/integrations/zeta/zeta-installments-mapper.ts`: `ProtoInstallmentInput` tipa el shape de dominio (zeta_registro_id linkable, cliente_codigo opcional, cuota_numero, cuota_vencimiento `YYYY-MM-DD`, moneda_codigo raw, currency_code mapeado UYU/USD/null, cuota_total, cuota_saldo, es_entrega_inicial boolean nullable, raw_payload preservado); `mapZetaInstallment()` defensivo (acepta numbers/strings JS-native/es-UY `"15.000,00"`/ISO con hora/ISO Z/`DD/MM/YYYY`/`S|N`/booleans/0|1); `mapZetaInstallmentsBatch()` reporta `discards` con `index/reason/sample_keys`. **Tests**: 60 verdes nuevos — 18 en contract (4 shapes, IsLastPage con `true/false/"S"/"N"`, summarize), 31 en mapper (canonical Postman, camelCase, snake_case, parsing es-UY, fechas múltiples formatos, EsEntregaInicial 5 variantes, rechazos null/array/string/0 sentinel/negativos, batch mix válidas+inválidas + sample_keys), 11 en fetch (defaults vencimiento/saldo, opcionales vacíos NO incluidos, numbers casteados desde strings, sentinels 0/NaN rechazados, trim de fechas, constantes method/rootInKey alineadas con Postman). **NO implementado en este turno** (por diseño): migración SQL `proto_invoice_installments`, pipeline orquestador, cron, backfill, actualización de `proto_invoices.due_date` con flag `due_date_source`. Razón: el usuario pidió "no implementar migración hasta confirmar shape" — el audit script debe correr live primero. **Divergencias documentadas en `docs/vendors/z/KNOWN-DIVERGENCES.md`**: DIV-CONT-005 (aplicaciones recibo↔factura — BLOQUEADO Zeta tras revisar 260 endpoints, pedir endpoint dedicado a soporte), DIV-CONT-006 (vencimiento real disponible vía Cuotas — implementación parcial entregada), DIV-CONT-007 (Estado de cuenta oficial BLOQUEADO — evaluable como POC con asientos + plan de cuentas), DIV-CONT-008 (FX differences — calculable localmente pero bloqueado por DIV-CONT-005 para precisión factura-por-factura). **Validación**: `npx tsc --noEmit` exit 0; `npx vitest run` **789/789 verdes** (60 nuevos); `npx eslint` sobre los 6 archivos production+test + 4 audit scripts → 0 errores. **NO se tocó**: sync Zeta existente, cron, DB schema, RLS, mappers existentes (`proto_invoices.due_date` sintético permanece igual hasta validación live), `balance_amount`, fórmulas dashboard/cartera, UI. Próximos pasos documentados en TASKS.md: ejecutar los 4 audit scripts con credenciales reales (objetivo de FASE 6.2), correr Cuotas live para confirmar shape, crear migración + pipeline + cron solo después de validar.
- 2026-05-11: **Zeta Financial Coverage Audit · auditoría exhaustiva base financiera + detector NC seguro**. Documento maestro `docs/zeta-financial-coverage-audit.md` con matriz de 10 módulos (Clientes, Facturas, Saldos Pendientes, Recibos, Vínculos factura-recibo, Notas de Crédito, Estado de Cuenta, Saldos a Fecha, Vencimiento Real, Diferencias de Cambio): existe/tabla/endpoint Zeta/campos presentes/campos faltantes/riesgo/acción. Inventario completo de endpoints REST operativos en código (5): `RESTContactosV3Query`, `RESTComprobantesClienteV1Query`, `RESTFacturaClienteV4QuerySaldosPendientes`, `RESTFacturaClienteV4VentasDetalladas`, `RESTRecibosCobranzaV2QueryComprobantes`. **Hallazgos críticos**: (1) `proto_receipts.invoice_id` siempre `null` — el endpoint `RESTRecibosCobranzaV2QueryComprobantes` no devuelve detalle de aplicaciones, bloqueado por endpoint Zeta no certificado. (2) **NCs no contabilizadas como crédito** — el classifier acepta CFETipos DGI 102/112/122/etc. y el mapper persiste `cfe_tipo` en `zeta_metadata.zeta_customer_voucher_v1`, pero el motor financiero las trata como facturas con total positivo. Causa divergencia visible (caso El País UYU: Zeta cierra $67.222, Copilot $75.884, gap $8.662 = NC A391). (3) `due_date` sintético `+30d` para todas las facturas — bloqueado por endpoint condiciones de pago no integrado. (4) Sin snapshots históricos de `balance_amount`. (5) Sin pipeline de diferencias de cambio. **Implementación SEGURA aplicada**: nuevo helper puro `lib/copilot-zeta-credit-note.ts` con `CFE_NC_TIPOS_DGI` (12 códigos), `readCfeTipoFromZetaMetadata()` (defensivo, tolera camel/snake/string-numérico), `isCreditNoteFromMetadata()`. Opt-in en motor `lib/copilot-financial-reconciliation.ts`: nuevo flag `InvoiceInput.is_credit_note?: boolean` (default `false` → compat total con todos los tests/consumidores existentes). Cuando `true`: la fila NO suma a `issuedInPeriod` ni `invoiceCount`, suma a `creditNoteCount`/`creditNoteAmount`, resta de `totalPending`/`pendingAtCutoff`/`openingBalance` con clamp ≥0 (sin pendiente negativo), excluida de aging. NCs pre-período se acumulan en `creditNotePrePeriodByCurrency` y restan del opening balance reconstruido. `CurrencyReconciliation` expone `creditNoteCount` y `creditNoteAmount` para trazabilidad UI. **Route** `app/api/copilot/financial-reconciliation/route.ts` resuelve el flag desde `r.zeta_metadata` vía `isCreditNoteFromMetadata()` y agrega `credit_notes_detected` al log `report_generated`. **Script de auditoría read-only** `temp-audits/audit-zeta-financial-coverage.mjs`: imprime conteos clientes/facturas/recibos, distribución por moneda, cobertura `balance_amount` (null/0/>0/<0/>total), heurística de `due_date` sintético (% facturas con `due-issue = 30d` exacto), detección de NCs por CFETipo + flag `con_total_positivo`, recibos `invoice_id != null` (espera 0 → confirma gap), cartera real (clientes con deuda > 0, saldo USD/UYU). Argumentos: `--workspace <uuid>` opcional. Paginación interna 1000 filas. **Tests**: 30 tests nuevos en `lib/copilot-zeta-credit-note.test.ts` (catálogo NC core/contingencias, lectura defensiva con metadata null/array/string/camelCase/snake_case/CFETipo, NaN/Infinity, decimales truncados); 7 tests nuevos en `lib/copilot-financial-reconciliation.test.ts` suite "notas de crédito: opt-in is_credit_note" (default false sin cambio, NC reduce pendiente sin entrar a issued, clamp ≥0, opening pre-período con NC pre-período, aislamiento USD/UYU, aging excluye NCs, NC sin moneda válida descartada). **Validación**: `npx tsc --noEmit` exit 0; `npx vitest run` **729/729 verdes** (motor 112/112 con 7 nuevos casos NC; helper NC 30/30); `npx eslint lib/integrations/zeta app/api/copilot app/api/zeta app/api/cron lib/copilot-zeta-credit-note.ts lib/copilot-financial-reconciliation.ts` → 0 errores nuevos (4 warnings preexistentes en otros archivos). **NO se tocó**: sync Zeta, cron, DB schema, RLS, mappers, `balance_amount`, fórmulas dashboard ejecutivo. El detector NC YA está activo end-to-end: motor + route + tests. Comportamiento visible al usuario: clientes con NCs ahora muestran saldo correcto coincidente con Zeta. Próximos pasos documentados: certificar endpoint aplicaciones de recibo (vínculo factura↔recibo), certificar endpoint condiciones de pago (`due_date` real), considerar tabla snapshots `proto_invoice_balance_snapshots`, pipeline cotizaciones como módulo aparte.
- 2026-05-11: **Cartera contable alineada con Zeta (Fase A) · separación de conceptos + receipts + opening balance**. Refactor estructural del modelo `/copilot/cartera` para coincidir con los reportes oficiales Zeta (Comprobantes Pendientes, Análisis de Saldos, Estado de Cuenta) en lugar de mezclar ventas, pendiente y cobranza. **Motor `lib/copilot-financial-reconciliation.ts`**: nuevo tipo `ReceiptInput` (id, company_id, currency_code, amount, receipt_date, status). `CurrencyReconciliation` ampliado con 5 campos canónicos: `issuedInPeriod` (= ventas del rango, alias de `totalInvoiced`), `pendingAtCutoff` (saldo abierto al `Hasta`, INCLUYE facturas pre-`Desde` con `balance_amount > 0` — coincide con "Comprobantes Pendientes" de Zeta a fecha de corte), `collectedInPeriod` (Σ `proto_receipts.amount` con `receipt_date` en rango, NO derivado de `invoiced − pending`), `collectedReceiptCount`, `openingBalance` (saldo anterior reconstruido: `Σ invoices.total_amount pre-Desde − Σ receipts.amount pre-Desde`, mismo concepto que "Saldo anterior" del Estado de Cuenta). `collectionEffectiveness` ahora es `collectedInPeriod / issuedInPeriod` cuando se pasan receipts; fallback legacy `(invoiced − pending) / invoiced` cuando NO se pasan (compat tests/UI antiguas, comportamiento controlado por `input.receipts !== undefined`). Filtro de monedas activas: con receipts incluye monedas con cobros aunque no haya facturas en período; sin receipts mantiene comportamiento legacy. Aliases `totalInvoiced/totalPending/totalCollected` preservados con semántica legacy (totalPending = solo facturas del período, NO incluye pre-período) para no romper consumidores. **Route `app/api/copilot/financial-reconciliation/route.ts`**: nueva query `proto_receipts` (filtrada por `workspace_company_id` + `is_active` + `receipt_date <= periodEnd` cuando aplica, ordenada por `receipt_date`, mismo `INVOICE_LIMIT`), mapeo `toSafeNumber` para `amount`, paso al motor como `input.receipts`. Falla en lectura de receipts degrada silenciosamente con log estructurado (no bloquea el reporte; UI muestra "—" en cobranza en lugar de inventar). Log `report_debug` actualizado con `issued_in_period`, `pending_at_cutoff`, `collected_in_period`, `opening_balance`, `collected_receipt_count` para auditoría server-side. **Helper `lib/copilot-cartera-cards-source.ts`**: `NormalizedCurrencyMetrics` extendido con los 5 campos nuevos (con fallbacks a aliases legacy si el motor no los envía). **Cards `components/copilot/executive-summary-cards.tsx`**: `CurrencyCardMetrics` rediseñado con los 4 conceptos contables explícitos + `collectedDataMissing`. 4 builders separados con copy correcto: `issuedCard` ("Emitido en período {code}"), `collectedCard` (NUEVO: "Cobrado en período {code}", muestra "—" si no hay receipts, ícono `CircleCheckBig`), `pendingCollectionCard` ("Pendiente de cobro al corte {code}"), `effectivenessCard` ("Cobranza efectiva {code}" con microcopy `Cobrado X / Emitido Y` ahora desde los campos contables nuevos). `openingBalanceCard` (NUEVO: "Saldo anterior {code}", se muestra SOLO cuando `openingBalance > 0`, evita poblar la grilla con cards vacías). Orden de cards reordenado por flujo lógico: Emitido → Cobrado → Pendiente → Efectividad → Riesgo → (Saldo anterior si aplica). **9 tests motor nuevos** en `lib/copilot-financial-reconciliation.test.ts` (suite "contrato contable"): `collectedInPeriod` proviene de receipts (no de invoiced−pending); `openingBalance` reconstruido desde ledger; **caso ACQUAGARDEN USD** (anterior 707.26 + factura 678.32 − recibo 339 = pending 1385.58, identidad contable verificada); **caso El País UYU** (opening 58.560 + 2 facturas 8.662 = pending 75.884; divergencia documentada de 8.662 = NC A391 no contabilizada, ver DIV-CONT-002); receipts pre/post-período excluidos del in-period; receipts anulados excluidos; compat legacy sin receipts; effectiveness usa collected/issued con receipts; moneda activa si hay cobros aunque no haya facturas del período. **4 scripts read-only en `temp-audits/`** para validación visual contra PDFs Zeta: `audit-zeta-comprobantes-pendientes.mjs` (cliente/comprobante/total/saldo/vencido para fecha de corte), `audit-zeta-analisis-saldos.mjs` (aging por cliente/moneda/bucket 0-30/31-60/61-90/90+), `audit-zeta-vencimiento-cuotas.mjs` (vencimiento/factura/atraso, con disclaimer DIV-CONT-001 sobre `due_date` sintético), `audit-zeta-estado-cuenta.mjs` (cliente×moneda: anterior + debe + haber + final con desglose de movimientos). Argumentos CLI (`--cutoff`, `--from/--to`, `--client`, `--currency`). **Divergencias documentadas en `docs/vendors/z/KNOWN-DIVERGENCES.md`**: DIV-CONT-001 (`due_date` sintético `issue + 30d`), DIV-CONT-002 (NCs no contabilizadas — caso El País 67.222 vs 75.884), DIV-CONT-003 (`balance_amount` es snapshot vivo del cron 3h, no histórico al `Hasta`), DIV-CONT-004 (`openingBalance` depende de cobertura de recibos pre-período sincronizados). **Validación**: `npx tsc --noEmit` exit 0; `npx vitest run` **692/692 verdes** (motor 105/105 con 9 nuevos casos contables); `npx eslint` archivos modificados → 0 errores nuevos. **NO se tocó**: sync Zeta, DB schema, cron, RLS, mappers, `balance_amount` (sigue siendo el actual del cron de saldos), fórmulas existentes (`totalInvoiced` etc. mantienen valores numéricos idénticos, solo se separa semánticamente). Las cards muestran ahora 4 conceptos diferenciados con copy claro y "—" cuando no aplica en lugar de "$ 0".
- 2026-05-11: **Cards `/copilot/cartera` · helper normalizador de `report.currencies` (testeable)**. Síntoma post-restart: cards en `$0/0%` PERO microcopy `Cobrado $235.326,38 / Emitido` (probó que `totalCollected` llegaba al cliente y los otros campos NO). Server logs confirmaron backend correcto (`report_debug` rev. 2026-05-12T00:21:25Z: USD invoiced 8639.8 / UYU invoiced 659992.38) y un error CRÍTICO PREVIO de HMR (`Rendered more hooks than during the previous render` en `use-financial-reconciliation.ts:174`, causado por el `useRef lastParamsKeyRef` agregado en el commit anterior) que dejó el state del componente desincronizado durante esa sesión de dev. Fix definitivo: nuevo helper `lib/copilot-cartera-cards-source.ts` con tres funciones puras (`readCurrencyMetric`, `buildCurrencyIndex`, `findCurrencyMetrics`) que normaliza `report.currencies` resistente a: array vs objeto indexado (`{USD:{}, UYU:{}}`), camelCase vs snake_case, numéricos number vs string vs es-UY (`"659.992,38"`), claves objeto lowercase (`{usd:{}}`), códigos inválidos y items malformados. Deriva `totalCollected`/`collectionEffectiveness` solo desde el mismo bucket si faltan. **30 tests** que incluyen un caso de regresión replicando literal el shape del log `report_debug`. `executive-summary-cards.tsx` refactorizado: eliminado el bloque `readPath/readNumberField/readCurrencyCode/readEffectivenessField/findCurrency` (~110 LOC) y reemplazado por `buildCurrencyIndex(report.currencies)` memoizado por render. Builders (`pendingCollectionCard/issuedCard/effectivenessCard`) reciben el `Map` ya normalizado. Microcopy de efectividad ahora muestra `Cobrado X / Emitido Y` (antes solo "/ emitido" literal). Aging/staleClients siguen como red de seguridad última con warning dev. Log temporal `[cards currencies runtime]` solo dev/browser para auditoría rápida (`TEMP[2026-05-11]`). Validación: `tsc --noEmit` exit 0; `vitest run` 683/683 (+30 nuevos); ESLint clean.
- 2026-05-11: **Fuente única financiera en motor (`/copilot/cartera`)**. Auditoría confirmó vía script Supabase read-only (`temp-audits/audit-cartera-period-may-2026.mjs`) que para 2026-05-01→11 hay 58 facturas (USD 23 / UYU 35) con shape limpio y valores reales coincidentes con el aging visible (USD invoiced $8.639,80 / pending $5.824,22 / eff 32,6%; UYU invoiced $659.992,38 / pending $424.666,00 / eff 35,7%). El motor calculaba bien `totalInvoiced` — la inconsistencia visual venía de UI rederivando con fallback. Cambios sin tocar sync/cron/DB/RLS/mappers/balance_amount/fórmulas de deuda actual cliente: (1) **`lib/copilot-financial-reconciliation.ts`**: `CurrencyReconciliation` ahora incluye `totalCollected` (`round2(max(0, invoiced − pending))`) y `collectionEffectiveness` (`clamp(collected/invoiced, 0, 1)` o `null` si `invoiced ≤ 0`) calculados en el mismo pase. JSDoc explícito documenta que aging no puede derivar emitido porque excluye facturas cobradas 100%. (2) **`app/api/copilot/financial-reconciliation/route.ts`**: log estructurado `kind: "report_debug"` con shape consolidado del reporte (`currencies[]`, `aging_sums.{UYU,USD}`, `stale_client_count`) sin secretos para auditoría server-side. (3) **`components/copilot/executive-summary-cards.tsx`**: `getCurrencyCardMetrics` simplificado a leer DIRECTO del bucket `report.currencies[code]` cuando existe (fuente primaria sin recálculo). Aging + staleClients quedan como **fallback defensivo SOLO** si el bucket no aparece (red de seguridad ante regresiones futuras); el fallback emite `console.warn` en dev cuando hay deuda en aging pero `currencies` no expone la moneda. `findCurrency` ahora también acepta `totalCollected/collectionEffectiveness` con keys alternativos y deriva los faltantes desde el MISMO bucket (nunca cross-source). Card "Cobranza efectiva" gana microcopy `Cobrado X / emitido` leyendo `collectedAmount` del motor. (4) **Tests motor**: 6 nuevos casos en `lib/copilot-financial-reconciliation.test.ts` (suite "currencies output: paid + partial + pending invoices"): paid invoices incluidas en `totalInvoiced`, aging no define emitido, multi-currency independiente, `collectionEffectiveness=null` cuando invoiced=0, clamp ≥0 para casos patológicos, réplica del subset USD del audit (23 facturas). Validación: `tsc --noEmit` exit 0; `vitest run` 653/653 (motor 96/96 con 6 nuevos); ESLint 0 errores en archivos tocados.
- 2026-05-11: **Fix rangos independientes `/copilot/cartera` · cards consistentes desde la primera carga**. Síntoma: con servidor recién arrancado, cargar directamente 01/05/2026 → 11/05/2026 mostraba counts correctos pero montos mal; al cargar primero 01/01/2026 → 11/05/2026 y luego volver, los montos eran correctos. Tres capas afectadas, todas resueltas sin tocar sync/DB/cron/balance_amount/fórmulas:
  1. `components/copilot/executive-summary-cards.tsx`: `getCurrencyCardMetrics` priorizaba `agingByCurrency` sobre `report.currencies` para pending. Aging excluye facturas cobradas 100%, mientras `currencies` agrega TODAS las facturas del período → cuando un rango tenía cobros parciales/totales, `aging.totalPending < currencies.totalPending` y eso disparaba `emittedAmount = pendingAmount`, subestimando "Emitido". Fix: `currencies` es fuente primaria; `aging`/`staleClients` solo como fallback defensivo si `currencies` viene vacío.
  2. `hooks/use-financial-reconciliation.ts`: el reducer retenía el reporte previo durante un `request` (anti-flicker en refetch). Al confirmar un rango NUEVO, el usuario veía el reporte ANTERIOR durante el fetch → mezcla visual "counts del nuevo + montos del viejo", visualmente igual a un bug de cálculo. Fix: nuevo action `params_changed` + `lastParamsKeyRef` que detecta cambio de `mode|periodStart|periodEnd` y limpia el reporte previo antes del nuevo fetch. El comportamiento anti-flicker se conserva para refetch puro del mismo rango.
  3. `app/api/copilot/financial-reconciliation/route.ts`: ausencia de `export const dynamic = "force-dynamic"` y `revalidate = 0` permitía al Data Cache de Next servir respuestas stale entre rangos. Se agrega ambos. Validación regex `^\d{4}-\d{2}-\d{2}$` de `period_start`/`period_end` con `400 INVALID_PERIOD` y check `start > end → 400 INVALID_PERIOD_RANGE`. Query Supabase agrega `.order("id")` para reproducibilidad del subset bajo `LIMIT 5000`. Log estructurado nuevo `kind: "range_received"` con `from`, `to`, `mode`, `raw_from`, `raw_to`, `workspace_id` (sin secretos) para diagnosticar parámetros recibidos.
  - Regresión: 4 tests nuevos en `lib/copilot-financial-reconciliation.test.ts` ("independent period ranges on same dataset") con dataset realista (enero–mayo, UYU/USD, cobros parciales + totales). Cubren: rango corto correcto desde primera invocación, rango amplio correcto, dos corridas del mismo rango en cualquier orden dan resultados idénticos (motor puro), rango vacío no rompe.
  - Validación: `npx tsc --noEmit` exit 0; `npx vitest run` 647/647 (motor 90/90 con 4 nuevos); `npx eslint` sobre los 6 archivos tocados sin issues.
- 2026-05-11: **Indicador auto-sync Zeta en barra `/copilot/cartera`**. UI-only (cero cambios en cron, sync, DB, balance_amount, motor financiero). Objetivo: que el usuario entienda que los datos se actualizan automáticamente cada 3 h (cron Vercel `0 */3 * * *` → `/api/cron/zeta-sync-saldos`) y distinga eso del botón manual Refrescar. Nuevo `lib/copilot-auto-sync.ts` con tres helpers puros: `ZETA_SALDOS_SYNC_INTERVAL_HOURS=3`, `pickZetaSaldosSyncState(syncStates)` (substring "saldo" case-insensitive en `resource_flow`, fallback `pickMostRecentSync`; cubre `saldos_pendientes` legacy y `factura_cliente_saldos_pendientes` actual), `computeNextZetaSyncAt(now, intervalHours=3)` (próximo múltiplo entero UTC estrictamente futuro), `formatHHmm(date)` (formato 24h es-UY con `—` para inválidos). 16 tests en `lib/copilot-auto-sync.test.ts`. Componente `<AutoSyncIndicator />` agregado dentro de `components/copilot/financial-control-bar.tsx` (oculto en mobile `<sm`, visible alineado a la derecha junto a Refrescar a partir de `sm`): muestra tres líneas — "Auto-sync Zeta · cada 3 h" con `Clock3`, "Última sync: hace Xh" (de `formatRelativeAgeHours(ageHours)` del sync state de saldos) o "Última sync: no disponible", "Próxima aprox.: HH:mm" local. Sin `useState`/`useEffect` en el indicador: cada fetch del reporte refresca el render. `FinancialControlBar` recibe nueva prop opcional `syncStates?: readonly SyncStateSummary[]`; si falta el indicador cae al copy estático. `cartera-shell.tsx` pasa `report?.syncStates` (queda en fallback hasta que el usuario confirme un rango — comportamiento esperado). Tooltip del botón Refrescar refuerza "no reemplaza al cron automático Zeta". Validación: `npx tsc --noEmit` exit 0; `npx vitest run` 643/643; ESLint sin issues en archivos tocados.
- 2026-05-11: **Fix cards superiores `/copilot/cartera` · fuente canónica unificada con Aging/Explorer**. Bug observado: para rango 01/05 → 11/05, Aging mostraba `UYU $ 424.666 / 23 fact.` y `USD U$S 5.824 / 14 fact.`, Explorer mostraba clientes/saldos correctos, pero las cards superiores quedaban en `$ 0,00 / U$S 0,00 / Cobranza efectiva 0%`. Causa: las cards leían `report.currencies` vía `findCurrency()` mientras Aging consume `report.agingByCurrency[code]` y Explorer `report.staleClients[].pendingByCurrency[code]`. Cualquier inconsistencia en `report.currencies` (shape mismatch, bucket vacío, key alterada) dejaba las cards a cero aunque el resto de la página tuviera datos. Fix solo en `components/copilot/executive-summary-cards.tsx` (cero cambios en motor, sync, DB, cron, balance_amount, fórmulas, mappers, Aging, Explorer): nuevo helper `getCurrencyCardMetrics(report, code)` que prioriza las MISMAS fuentes que ya funcionan. `pendingAmount` se deriva de `Σ agingByCurrency[code].amount` (mismo número que muestra Aging), con fallback en cascada a `staleClients.pendingByCurrency[code]` → `currencies.totalPending`. `pendingInvoiceCount` desde `Σ agingByCurrency[code].invoiceCount`. `emittedAmount/emittedInvoiceCount` desde `currencies.totalInvoiced/invoiceCount` con fallback al pending si el bucket viene vacío (mínimo lógico, nunca menor). `collectionEffectiveness = clamp((emitted − pending) / emitted, 0, 1)`. `pendingCollectionCard`, `issuedCard` y `effectivenessCard` consumen el helper. Removido `console.debug("[summary cards]", …)` temporal y el import sin uso `deriveCollectionEffectiveness`. Validación: `npx tsc --noEmit` exit 0; `npx vitest run` 627/627; `npx eslint components/copilot/executive-summary-cards.tsx components/copilot/cartera-shell.tsx` sin issues.
- 2026-05-11: **Cambio de carga inicial `/copilot/cartera` · sin fetch hasta confirmar fechas + sin selector Año**. Frontend/wiring únicamente; no se tocaron sync, DB, cron, `balance_amount` ni fórmulas core. `CarteraShell`: `periodStart/periodEnd` ahora arrancan `null`, `hasConfirmedRange=false`, y `useFinancialReconciliation({ enabled: hasConfirmedRange })` impide cualquier fetch al montar. `handleConfirmDraft()` exige `Desde` + `Hasta` válidas (`YYYY-MM-DD`, `desde <= hasta`) y sólo entonces setea el rango efectivo y habilita el hook. Se elimina el default efectivo/visual basado en período operativo para la carga inicial: al entrar no hay snapshot, no hay queries y no hay KPIs calculados. `FinancialControlBar`: se simplifica a sólo `Desde`, `Hasta`, `Confirmar`, `Refrescar`; se eliminan completamente selector Año, selector global de moneda, estado sync y lógica derivada del año. `Refrescar` queda disabled hasta confirmar un rango. Cartera queda en modo multi-moneda (`selectedCurrency="all"`) y los componentes internos conservan soporte multi-currency. Estado inicial profesional: `EmptyPeriodState` con título "Seleccione un período", texto pedido, placeholders de cards con `—`, aging vacío, tabla vacía y reconciliación vacía; durante loading tras confirmar se usan los mismos placeholders con shimmer para evitar layout shift. Validación: `npx tsc --noEmit` exit 0; `npx vitest run` 627/627; ESLint cartera sin errores (3 warnings preexistentes en `client-debt-explorer`/`explainability-panel`).
- 2026-05-11: **Fix UI/state `/copilot/cartera` · cards tolerantes a shape + botón Confirmar**. Alcance: frontend/wiring solamente (no sync Zeta, DB, cron, balance_amount ni fórmulas core). Cambios:
  1. `components/copilot/executive-summary-cards.tsx`: `findCurrency()` ya no asume exclusivamente `currencyCode/totalPending/totalInvoiced`. Normaliza cada item de `report.currencies` aceptando `currencyCode/currency_code/currency/code` y montos en camel/snake/aliases (`totalPending/total_pending/pendingAmount`, `totalInvoiced/total_invoiced/invoicedAmount`, counts equivalentes), parseados con `toSafeNumber`. Esto corrige el caso donde el report trae datos reales pero la UI cae a `?? 0` por mismatch de keys o montos string. Agregado `console.debug("[summary cards]", { selectedCurrency, currencies, report })` sólo en `NODE_ENV !== "production"` para inspeccionar shape real sin secretos.
  2. `components/copilot/cartera-shell.tsx`: los inputs de fecha arrancan visualmente vacíos (`draftStart/draftEnd = ""`) aunque el reporte inicial mantiene período operativo default (`2026-01-01 → hoy`) para no dejar la pantalla sin datos. Se agregan `confirmedDraftStart/confirmedDraftEnd`, `draftCurrency` y `hasPendingChanges` con comparación normalizada `YYYY-MM-DD`.
  3. `components/copilot/financial-control-bar.tsx`: moneda y año pasan a draft; no aplican hasta Confirmar. `Confirmar` se habilita con cualquier cambio pendiente válido (fecha, moneda o año), se deshabilita si no hay cambios/loading/fechas incompletas/`desde > hasta`; selector Año sólo rellena draft; moneda pendiente muestra punto visual. Fechas parciales muestran mensaje amber; `desde > hasta` muestra error rose.
  - Validación: `npx tsc --noEmit` exit 0; `npx vitest run` 627/627; `npx eslint app/copilot/cartera/page.tsx components/copilot/cartera-shell.tsx components/copilot/executive-summary-cards.tsx components/copilot/financial-control-bar.tsx` sin issues.
- 2026-05-11: **Fix cartera cards en cero · `numeric` Supabase serializado como string**. Cards "Pendiente de cobro UYU/USD", "Emitido UYU/USD" y "Cobranza efectiva" mostraban `$ 0,00 / U$S 0,00 / 0,0%` aunque hubiera 348 facturas reales en el período 2026-01-01 → 2026-05-11 (USD: 163 emitido U$S 101.942,86 / 32 pendientes U$S 11.514,08; UYU: 185 emitido $ 3.624.185,05 / 29 pendientes $ 561.218). **Causa raíz**: `app/api/copilot/financial-reconciliation/route.ts` mapeaba `total_amount`/`balance_amount` con `typeof === "number" ? r : null`. PostgreSQL serializa `numeric`/`decimal` como string en el driver Supabase JS (preserva precisión); el cast convertía todo a null → `safeNum(null) = 0` en el motor → `if (!(totalAmount > 0)) continue` → bucket de moneda vacío → `report.currencies = []` → `findCurrency()` undefined en cards → 0. **Fix mínimo**, solo wiring (cero cambios en motor financial-reconciliation, sync, cron, DB, balance_amount, fórmulas, formatters, mappers, RLS): nuevo helper aislado `lib/copilot-numeric-parse.ts → toSafeNumber(v: unknown): number | null` que acepta number finito + string JS-native (`"1234.56"`) + string es-UY (`"3.624.185,05"`) y rechaza NaN/Infinity/boolean/object/empty. Aplicado en el route (que ahora también contabiliza cuántas filas vienen como string para diagnóstico) y en `scripts/run-post-fase3-validation.ts`. Log estructurado `kind: "report_amounts_all_zero"` (warn) cuando se detecta el patrón (facturas cargadas + totales en cero) con hint señalando el cast como sospecha primaria. 10 tests nuevos (`lib/copilot-numeric-parse.test.ts`) verifican parseo de strings reales del backfill (3624185.05, 101942.86, 11514.08, 561218). Totales: 627 tests verdes, `tsc --noEmit` exit 0, ESLint 0 issues en archivos tocados.
- 2026-05-11: **UX `/copilot/cartera` · confirmación manual de fechas + filtro de moneda global + remoción de "Todo el historial"**. Solo frontend (cero cambios en backend, sync, pipeline, balance_amount, RLS, DB, mappers). Cambios principales:
  1. **`components/copilot/cartera-shell.tsx`**: introduce estado `draftStart/draftEnd` separado del rango efectivo `periodStart/periodEnd`; `selectedCurrency: "all" | "USD" | "UYU"` se propaga a todos los hijos; `mode` queda fijo en `period_only` (sin más selector visible). `handleConfirmDraft()` promueve draft → estado real (que dispara fetch); `handlePeriodChange()` actualiza ambos (uso desde selector Año).
  2. **`components/copilot/financial-control-bar.tsx`**: refactor a control 100% controlado (sin `useEffect`, `useRef`, ni `useState` de fechas dentro del bar → cumple `react-hooks/set-state-in-effect` y `react-hooks/refs`). Eliminado tablist de modo (`period_only` / `all_outstanding`); reemplazado por tablist de moneda "Todas / USD / UYU". Inputs Desde/Hasta editan draft; botón verde "Confirmar" (`Check` icon) promueve a estado real, deshabilitado si `!draftDirty` o `desde > hasta` (mensaje rosa accesible `role="alert"`). Selector Año aplica inmediato (sigue siendo atajo) y sincroniza draft. Enter en inputs también confirma.
  3. **`components/copilot/executive-summary-cards.tsx`**: cuando `selectedCurrency ∈ {USD, UYU}` → 4 cards de la moneda activa (Pendiente de cobro / Emitido / Cobranza efectiva / Clientes en riesgo `<MONEDA>`). Cuando `"all"` → 8 cards (UYU+USD por métrica + stale global + orphan). Wording canónico alineado con dashboard ejecutivo. Nuevo `staleCardForCurrency()` deriva clientes con `pendingByCurrency[currency] > 0` cuyo `status !== ok` y muestra ratio sobre la cartera de la moneda.
  4. **`components/copilot/aging-analytics.tsx`**: cuando hay moneda forzada, oculta los tabs UYU/USD y muestra un badge con la moneda activa; sólo grafica buckets/montos/percentages/counts de esa moneda. Cuando "all", mantiene tabs internos como antes.
  5. **`components/copilot/client-debt-explorer.tsx`**: pre-filter del dataset por moneda activa (`pendingByCurrency[currency] > 0`); oculta la columna de saldo de la moneda opuesta (preferible "solo la columna activa"); badge `<MONEDA>` junto al título; toggle "Sin deuda" se oculta cuando hay filtro de moneda (no aplica conceptualmente); `effectiveSort` derivado en `useMemo` reemplaza el sort original si apunta a una columna oculta (sin `setState` en render); expanded row muestra solo el saldo de la moneda activa.
  6. **`components/copilot/explainability-panel.tsx`**: `buildItems(report, selectedCurrency)` filtra las listas de monedas en `stalePendingByCurrency`, `historical.pendingByCurrency` y `warnedPendingByCurrency` para que las explicaciones no mezclen USD/UYU cuando hay filtro activo.
  - **Validaciones**: `npx tsc --noEmit` exit 0; `npx vitest run` 617/617; `npx eslint app/copilot/cartera/page.tsx components/copilot/{cartera-shell,financial-control-bar,executive-summary-cards,aging-analytics,client-debt-explorer,explainability-panel}.tsx` → 0 errores nuevos, 3 warnings preexistentes (`COLLECTION_PRIORITY_LABELS`, `pendingTotal`, `reduce`) no relacionados.
- 2026-05-11: **Backfill saldos pendientes 2026-01-01 → 2026-05-11 + wording ejecutivo + Cash conversion**. Validación post-fix sobre el período operacional completo: 348 facturas (USD=163, UYU=185), 0 con `status='issued' AND balance_amount=0` (estado consistente, sin falsos pendientes). Cobertura por moneda: USD 32 pendientes / U$S 11.514,08 abiertos / U$S 90.428,78 cobrados; UYU 29 pendientes / $ 561.218 abiertos / $ 3.062.967 cobrados. Cron Vercel cada 3h (`0 */3 * * *`) confirmado; cap efectivo 200 cubre los 183 clientes activos con `Codigo`. **Cambios UI sólo en `components/copilot/copilot-financial-dashboard.tsx`** (cero cambios en cálculos / fórmulas / motor de prioridad / aging engine): (1) wording ejecutivo: `Facturado → Emitido`, `Pendiente → Pendiente de cobro`, `Efectividad → Cobranza efectiva` en `KpiStrip` + headers de Top deudores (`Pendiente → Pendiente de cobro`, `Efect. → Cobranza ef.`). (2) Nueva `<KpiGlossary>` debajo de cada strip con definiciones canónicas (Emitido = total facturado del período; Pendiente de cobro = saldo abierto informado por Zeta; Cobrado = emitido − pendiente; Cobranza efectiva = cobrado / emitido). (3) Nueva `<CashConversionSection>` arriba de los KPIs con tarjeta por moneda (USD/UYU) que muestra frase ejecutiva única: "Emitiste {symbol} {totalInvoiced}, cobraste {symbol} {totalCollected} y queda pendiente {symbol} {totalPending}." Cero mezcla de monedas. Reusa exactamente los campos `totalInvoiced / totalCollected / totalPending` del motor sin recalcular. Script de auditoría: `temp-audits/audit-backfill-saldos-coverage-2026.mjs` (read-only) con 4 reportes: resumen por moneda, desglose status/active, listado issued con balance=0 (0 hoy), y cash conversion ejecutivo. 617/617 tests, `tsc --noEmit` exit 0, eslint sin issues.
- 2026-05-11: **Fix cobertura cron saldos pendientes Zeta** (`app/api/cron/zeta-sync-saldos/route.ts`, `app/api/zeta/sync-saldos-all-clients/route.ts`). **Causa raíz**: el cron `/api/cron/zeta-sync-saldos` procesaba sólo `MAX_CLIENTS_PER_WORKSPACE = 10` clientes por corrida sin `.order()`, mientras el workspace `040321ff-…` tiene **183** clientes activos con `Codigo`. Selección no determinística → 22/23 facturas USD emitidas entre 2026-05-01 y 2026-05-10 quedaron con `balance_amount = 0` (mapper voucher escribe 0 por diseño; saldo lo aplica exclusivamente `RESTFacturaClienteV4QuerySaldosPendientes`). A-2926 sí reflejaba saldo porque su cliente caía por azar en el batch. **Fix mínimo, solo en pipeline (UI/dashboard/fórmulas intocadas)**: (1) cron: `MAX_CLIENTS_PER_WORKSPACE` default 200 (parametrizable por env `ZETA_SALDOS_CRON_MAX_CLIENTS_PER_WORKSPACE`) + `.order("id", { ascending: true })` para selección reproducible + log `clients_cap_reached` cuando el universo real supera el cap. (2) endpoint manual: `max_clients` cap 50→500, default 20→100, mismo `.order("id")`. **Validación antes/después** (`temp-audits/audit-may-usd-balance-mismatch.mjs`, read-only): antes 1 factura USD con balance > 0 + 22 en `balance=0`/`status=issued`; después 14 con saldo real + 9 en `balance=0`/`status=paid` (zero-pass confirmado por Zeta como pagas). A-2926 (control) 678.32, A-2925 427, A-2923 561.20, A-2922 732 → todas correctas. A-2919 sigue ausente (gap separado del voucher pipeline, no de saldos). 1 cliente con `TypeError: fetch failed` transitorio al insertar staging payload (no bug del fix; lo procesa la próxima corrida). 617 tests, `tsc --noEmit` limpio, eslint sin errores nuevos. Re-sync producción ejecutado vía cron local con `CRON_SECRET`: 183 clientes procesados, 52 saldos upserted, 17 min duración. Reporte: `temp-audits/audit-may-usd-balance-mismatch-report.md`.
- 2026-05-10: **UX — diferenciación clara "Cuenta corriente" vs "Por factura" en detalle de cliente** (`components/copilot/copilot-client-account-statement.tsx`). Solo copy/labels/badges; cero cálculos modificados. Cambios: (1) Header reporte: `Saldo final` → `Saldo contable acumulado`. (2) Vista cuenta corriente · Summary: `Saldo final` → `Saldo contable acumulado`; nota al pie consistente (`Saldo contable acumulado = Total debe − Total haber`). (3) Vista cuenta corriente · Footer: `SALDO {sym}` → `SALDO CONTABLE {sym}`. (4) Nuevo `<ViewModeBadge />` (slate "Lectura contable histórica" para cuenta corriente; emerald soft "Deuda operativa actual" para por factura) integrado al `<ContextHint />`. (5) `ContextHint` reescrito con los copies pedidos: cuenta corriente "Calcula debe/haber con los movimientos sincronizados disponibles. Puede diferir del saldo pendiente por factura si Zeta no expone cobranzas, notas de crédito o imputaciones completas en esta lectura."; por factura "Usa el saldo pendiente informado por Zeta en cada factura. Esta es la lectura recomendada para analizar deuda real actual." Vista por factura sin cambios estructurales: ya usaba "Total facturado" / "Cobrado según Zeta" / "Pendiente según Zeta" — sólo se le agrega el badge contextual. Tabs `Cuenta corriente` / `Por factura` se mantienen. Aplica a todos los clientes (no se hardcodean nombres). 617 tests, tsc limpio, build OK.
- 2026-05-10: **UX — eliminado botón manual "Sincronizar Zeta" en bloque Contactos de `/copilot/datos`**. Sincronización ya es automática vía `/api/cron/zeta-sync-contacts` (diario 02:00 UTC) + `zeta_pipeline_runs`; el botón manual generaba ruido visual y posibles ejecuciones redundantes. `app/copilot/datos/datos-client.tsx`: removidas dos zonas (vista colapsada y vista expandida del tab) — el tab Contactos ahora cae al ramal genérico `DATOS_EXPAND_HINT`/`DATOS_EXPAND_CTA` igual que los demás (mismo botón "Ver contactos", carga al expandir). Eliminado handler `handleContactSync`, los 3 estados `contactSyncState/Count/Error`, y los imports `CheckCircle2` / `RefreshCw` (`Loader2` permanece por uso en otras zonas). `Ver contactos` y la carga automática se mantienen intactos. Verificado: no existen botones manuales equivalentes para saldos/vouchers/recibos en `/copilot/datos` (única dependencia con `/api/zeta/sync-contacts` era esta UI; el endpoint sigue disponible para tooling/cron). 617 tests, `tsc --noEmit` limpio, `npm run build` OK.
- 2026-05-10: **UX — eliminado botón manual "Actualizar" del Pipeline Health panel**. Refresh ya cubierto por realtime trigger-only + fallback polling 60s/15s. `pipeline-health-panel.tsx` sin prop `onRefresh`, sin import `RefreshCw`. `app/copilot/page.tsx` deja de pasar `loadPipelineHealth` al panel. Loading states automáticos preservados.
- 2026-05-10: **FASE 7 — Observability Logger Unificado**: `lib/observability/sanitize.ts` (redactSensitiveKeys, sanitizeLogPayload, sanitizeError — 28 claves sensibles). `lib/observability/logger.ts` (createLogger factory, LogContext/LogPayload/LogLevel types, debug gated por COPILOT_LOG_DEBUG, emitError helper). `lib/observability/cron-logger.ts` (createCronLogger drop-in para los 3 cron routes). Migrados internamente: pipelineEmit (zeta-saldos-pipeline.ts), pipelineReconcileLog (zeta-saldos-reconciliation.ts), diagLog ahora usa sanitizeLogPayload. Migrados: zeta-sync-saldos/vouchers/contacts cron routes. Gateado console.log en dataset/route.ts. copilot-structured-logger.ts y zeta-log/logger.ts sin cambios (backward compat total). 29 tests nuevos en lib/observability/logger.test.ts. 617 tests, tsc limpio, build OK.
- 2026-05-10: **FASE 6 — Pipeline Health Realtime + Fallback Polling + Alerting mínimo**: Hook `usePipelineHealthRealtime` (`lib/copilot-pipeline-health-realtime.ts`) con Supabase Realtime como trigger-only (nunca lee datos sensibles directo del cliente), fallback polling 60s/15s según estado de conexión, estado `RealtimeConnectionStatus`. `ConnectionStatusBar` y `AlertsBar` en panel (realtime activo/fallback/desconectado, última actualización, alertas operativas por pipeline: stale/consecutive_failures/rows_failed/error). 7 estados UX cubiertos (loading/empty/error/degraded/stale/refresh/healthy). SQL `zeta-06-02-pipeline-runs-realtime.sql` habilita RLS + política SELECT authenticated + `ALTER PUBLICATION supabase_realtime`. 20 tests nuevos en `lib/copilot-pipeline-health-realtime.test.ts`. 588 tests totales, tsc limpio, build OK.
- 2026-05-10: **FASE 4+5 — ROW_CAP no silencioso + Pipeline Health Dashboard**: FASE 4: `lib/copilot-data-completeness.ts` (`DataCompletenessWarning`, `buildCapWarning`); `loadFinancialFactsBundle` detecta `isTruncated`/`tables_at_cap`; propagación completa hasta `FinancialSnapshotApiV1.diagnostics.dataset_caps`; selectores `snapshotIsTruncated/snapshotTablesAtCap`; banner amber en home. FASE 5: `GET /api/copilot/pipeline-health` (solo lectura, autenticado); `PipelineHealthSummary` extendido con métricas del último run; `pipeline-health-panel.tsx` (cards status/métricas/tiempo relativo/botón refresh); sección "Estado de pipelines Zeta" en home. 568 tests, tsc limpio.
- 2026-05-10: **FASE 1–3 auditoría y hardening**: FASE 1 (R-02): `workspaceCompanyId` obligatorio (`string`, no `string?`) en las 7 funciones de `proto-analytics-read-repository.ts` + `getFinancialSnapshotForApi`, `getClientPortfolio`, `computeCopilotRealInsights` — TypeScript fuerza tenant en todos los call sites. Runtime guard con throw explícito. FASE 2 (R-01): removidos ~38 `console.log("🔥 ...")` de `zeta-customer-vouchers-pipeline.ts`, `zeta-customer-vouchers-fetch.ts`, `route.ts`; reemplazados por logs estructurados `console.info(JSON.stringify(...))`. FASE 3 (Q-01+Q-02): índice SQL parcial `ZETA:CCV1:%` creado (`supabase/zeta-07-01-proto-invoices-ccv1-index.sql`); `preloadClientZetaInvoiceCache` pre-carga todas las facturas CCV1 del cliente antes del loop paginado (1 query → N invoices), `persistZetaInvoice` usa Maps en-memoria para los 3 lookups principales (RegistroId, invoice_number, metadata) y heurístico in-memory; paths 1/2/3 ya no emiten queries DB por fila. 563 tests, tsc limpio.
- 2026-05-10: **ZETA-06 validación end-to-end completa en producción** (`copilot-pro.vercel.app`). 3 fixes multi-tenant aplicados (7bebb30, 6cce033): `selectZetaSyncStateByResource` sin `companyId` fallaba con `.maybeSingle()` cuando había 2+ workspaces. Root cause del `succeed=false`: `DesarrolladorClaveNoValida` (clave incorrecta en Vercel, resuelta por usuario). Resultados finales: saldos `succeeded` (10 clientes, 4 upserted), vouchers `succeeded` (2 ws, 2 meses, 386 procesados, 118 actualizados), contacts `partial` (195 synced ws principal; ws secundario sin match `proto_companies` — problema de datos, no de código). Anti-overlap validado: `skipped:true` con run activo falso. `zeta_pipeline_runs` registra correctamente estado, duración, métricas y metadata JSON por corrida. Health layer: saldos/vouchers=healthy, contacts=degraded (ws sin datos).
- 2026-05-09: **Automatización enterprise pipelines Zeta (ZETA-06)**. 5 fases implementadas:
  - **Fase 1 — Cron orchestration**: 3 nuevos cron routes autenticados con `CRON_SECRET`. `/api/cron/zeta-sync-saldos` (cada 3h), `/api/cron/zeta-sync-vouchers` (cada 6h, meses actual+anterior), `/api/cron/zeta-sync-contacts` (diario 02:00 UTC). `vercel.json` actualizado. El route anterior `/api/cron/sync-saldos` queda como legacy.
  - **Fase 2 — Pipeline Run Registry**: tabla `zeta_pipeline_runs` (`supabase/zeta-06-01-pipeline-runs-schema.sql`), tipos (`lib/data/zeta-pipeline-run-types.ts`) y repositorio (`lib/data/zeta-pipeline-run-repository.ts`) con `createPipelineRun`, `updatePipelineRun`, `findActivePipelineRun`, `getRecentPipelineRuns`.
  - **Fase 3 — Anti-overlap**: cada cron consulta `findActivePipelineRun` antes de arrancar; si hay un run activo en la ventana temporal → skip con log estructurado.
  - **Fase 4 — Retry conservador**: `lib/integrations/zeta/zeta-retry.ts` — `withZetaRetry<T>` + `isTransientZetaError`. Max 3 retries, backoff exponencial 1s→2s→4s (cap 30s), solo errores de red/5xx/429. Sin retry en 4xx.
  - **Fase 5 — Health layer**: `lib/data/zeta-pipeline-health.ts` — tipos `PipelineHealthStatus`, `PipelineHealthSummary`, función pura `derivePipelineHealth`, `getAllPipelineHealth`. Listo para dashboard.
  - Reconciliación orphans integrada en el cron saldos (`syncMode: "reconciliation_cleanup"`). 563 tests, 41 archivos, tsc limpio.
- 2026-05-09: **Auditoría y reactivación controlada abril 2026 (62 facturas)**. FASE 2 completada: causa raíz confirmada (pipeline viejo del 22/04 con `is_active=false, archived_at=null` — estado imposible con código actual). Script `scripts/reactivate-abril-2026.ts` ejecutado: 62/62 filas reactivadas (is_active=true), re-sync vouchers abril 2026 procesó 129 filas, actualizó 62, 0 errores. Post-sync: active_vouchers=62 (antes 0), inactive_vouchers=0. Efecto colateral esperado: las 44 facturas previamente pagadas ahora muestran `status=issued` (VentasDetalladas sin saldo real-time). El próximo run de saldos pipeline corregirá balances vía `zeroCcV1BalancesWithoutSaldoRow`. Reporte en `temp-audits/audit-abril-2026-fase2-report.md`. 521 tests, tsc limpio.
- 2026-05-09: **Pipeline Contactos Zeta (DIV-003)**: `contracts/zeta-contacts.contract.ts` soporta `QueryOut.Response[]` (Postman/tenant real) + retrocompat `QueryOut.Contactos.Contacto[]`. `zeta-contacts-pipeline.ts`: sync incremental paginado con upsert por Codigo/Documento/email, resolución FK a proto_companies, trazabilidad en zeta_sync_*, retry. API `POST /api/zeta/sync-contacts`. DIV-003 + NOTE-001 documentados en `KNOWN-DIVERGENCES.md`. 521 tests, tsc limpio.

- 2026-05-09: **Collection Operations Layer — Fase 1 implementada**. Capa operativa de cobranza independiente de datos financieros Zeta. Tabla nueva `collection_actions` (SQL: `supabase/collection-operations.sql`) con RLS completa por workspace, soft delete, enums pg para status/type/priority. Tipos TypeScript: `lib/copilot-collection-types.ts` (CollectionAction, CollectionActionInput, CollectionActionPatch, ClientCollectionSummary, ClientCollectionTimeline, labels UI). Servicio `lib/copilot-collection-service.ts`: create/update/archive/getByCompany/getByWorkspace, pattern idéntico a proto-crud. Repository puro `lib/data/collection-read-repository.ts`: deriveClientCollectionSummary, buildClientCollectionTimeline, rankClientsByCollectionPriority (priorización: overdue promise → escalated → priority score → stale → no_action). API routes: POST create, PATCH [id]/update, POST [id]/archive, GET collection-actions (por company o workspace). Hook `hooks/use-collection-actions.ts` (useReducer + AbortController, sin race conditions). Drawer `components/copilot/collection-action-drawer.tsx` (form completo con campos de promesa de pago condicionales). ClientDebtExplorer actualizado: columna "Cobranza" con estado + botón "Registrar acción" (+), timeline de acciones en expand row. `tsc --noEmit` limpio, 22/22 tests nuevos verdes.

- 2026-05-09: **Bloque 5 — Aging Analytics + Client Debt Explorer + Explainability Panel**. Backend extendido con `agingByCurrency` (4 buckets 0-30/31-60/61-90/90+ por moneda: amount, invoiceCount, clientCount, percentage), `dominantAgingRange` por cliente y `pre2026InvoiceCount` en gaps — todo en O(n) en el mismo pass de facturas. Nuevos componentes: `AgingAnalytics` (tabs UYU/USD, barras horizontales animadas con framer-motion, colores fintech muted), `ClientDebtExplorer` (tabla con search debounced, sort, 7 filter chips, expand rows, paginación 25/50/100, derivación de risk level render-only), `ExplainabilityPanel` (auditoría financiera: period exclusion, unknown currency, stale pending, pre-2026, orphan warnings, voided). Corrección definitiva de "Efectividad cobranza global": reemplazado el card ponderado multi-moneda por dos cards separados "Efectividad UYU" y "Efectividad USD". `CarteraShell` actualizado: 4 secciones activas + skeleton de aging. 416 tests verdes (41 nuevos de aging), `tsc --noEmit` limpio.


- 2026-05-08: **Salud financiera evolucionada a centro de decisiones**. El dashboard de `/copilot` ahora prioriza interpretación y acción antes de métricas: 1) `FinancialPriorityAlerts` ("Atención requerida hoy"), 2) `FinancialActionPriorities` ("Prioridades de acción"), 3) `FinancialRiskSummary` ("Riesgo financiero"), 4) KPIs financieros, 5) aging, 6) top deudores detallado. Nuevo motor puro `lib/copilot-financial-priority-engine.ts` genera alertas, scores de prioridad y semáforos usando reglas determinísticas/auditables sobre `FinancialDashboardMetrics` (sin IA): high por deuda >90 días, concentración >60%, efectividad <80%, cliente >25% deuda; medium por efectividad 80-90, aging 61-90 significativo y muchos clientes con deuda. `priorityScore` combina peso de monto, aging dominante y concentración (0-100). `TopDebtor` se enriqueció con `invoiceCount`, `dominantAgingLabel`, `oldestAgeDays`, `totalInvoiced`, `totalCollected` y `collectionEffectiveness` para acciones y tabla analítica, manteniendo campos previos. Tests nuevos `copilot-financial-priority-engine.test.ts` (6 casos). Validado con `tsc --noEmit`, vitest métricas+priority+deuda actual y eslint de componentes.
- 2026-05-08: **Salud financiera ejecutiva en `/copilot` (Hoy)**. El bloque ejecutivo ahora vive en la home — no en el sidebar de cliente. `app/copilot/page.tsx` carga `getProtoInvoices("all")` + `getProtoCompanies("all")`, calcula `buildFinancialDashboardMetrics` y renderiza `CopilotFinancialDashboard` dentro de un `CopilotCard` con título "Salud financiera", ubicado entre "Liquidez y cobertura" y "Próximos vencimientos". El sidebar de cliente quedó revertido al alcance previo (deuda actual + estado de cuenta + ledger histórico + por factura + movimientos + cortes). UI del dashboard simplificada: el wrapper visual lo provee la home; el componente sólo renderiza la grilla por moneda con KPIs, aging, top deudores e insights ejecutivos en `lg:grid-cols-2`. Datos reales del tenant verificados: USD 155 facturas (135/3/17, efectividad 93.61%, pendiente U$S 6.352,99) y UYU 179 facturas (170/1/8, efectividad 95.75%, pendiente $ 148.691). Validado con `tsc --noEmit`, 79 tests verdes y eslint.
- 2026-05-08: **Executive Financial Dashboard — Fase 1 implementada**. Nueva capa reusable `lib/copilot-financial-dashboard-metrics.ts` construye métricas financieras puras desde ledger completo (`getProtoInvoices("all")`) y `balance_amount`, reutilizando utilidades de `copilot-client-current-debt-summary` para moneda, anuladas/canceladas, clasificación paid/partial/pending y montos. Métricas por moneda: facturado, cobrado, pendiente, counts, clientes deudores, efectividad de cobranza, top 5 deudores y aging 0-30/31-60/61-90/90+ basado en `issue_date`. No usa recibos ni linking inferido; no depende de filtros UI, tablas visibles ni `is_active=true`; incluye archivadas válidas y excluye anuladas/corruptas. Tests dedicados (7 casos: multi-moneda, aging, top debtors, exclusiones, archivadas, cero divisores, inmutabilidad).
- 2026-05-08: **Bloque "Deuda actual del cliente"** agregado al sidebar de detalle de cliente. Helper puro `lib/copilot-client-current-debt-summary.ts` calcula `SUM(balance_amount)` agrupado por moneda sobre el dataset ledger COMPLETO (`ledgerInvoices`), independiente de cualquier filtro del panel. No usa recibos ni Debe/Haber. Excluye comprobantes con `status` anulado/cancelado y filas con moneda indeterminable. Componente `components/copilot/copilot-client-current-debt-summary.tsx` muestra cards por moneda (Pendiente, counts cobradas/parciales/pendientes, Facturado/Cobrado histórico) + banner contextual emerald (sin deuda) / amber (con deuda). Mountado en `copilot-data-sidebar.tsx` arriba del bloque de filtros y del Estado de cuenta. 13 tests dedicados (90 tests verdes en total). Caso Álvarez verificado contra DB real: USD pendiente=0, 4 cobradas (incluye archivada A-2863), banner "no registra saldo pendiente" — aunque el estado de cuenta filtrado al 17/04 muestre USD 732, este bloque sigue mostrando USD 0 (deuda actual).
- 2026-05-07: creada carpeta `temp-audits/` aislada para artefactos de auditoría/importación/validación. Ignorada en `.gitignore` (sólo se versiona `temp-audits/README.md`). También se ignora `/tmp/`.
- 2026-05-07: auditoría reconciliación enero–abril 2026 (read-only):
  - **63 facturas faltantes en abril 2026** → causa raíz priorizada en `temp-audits/audit-abril-2026-report.md`. Hipótesis principal: `issue_date` mal parseado (fallback `new Date()` en `mapCopilotCustomerVoucherToProtoInvoiceInput`). Pendiente confirmar con queries SQL §A.2/§B.3/§E.1.
  - **1 fila Excel Nº=0 (Prestis S.A.S. 04-mar UYU 9.760)** confirmada como **borrador CFE no emitido** (`Emitida="N"`, `Estado DGI=""`). NO requiere sync. Detalle en `temp-audits/audit-prestis-numero-0.md`. Reduce el universo "Excel emitido" a **281** filas.
  - Discrepancia residual real: 62 facturas de abril, no 63.
- 2026-05-07: **fix H1 aplicado** en `lib/integrations/zeta/zeta-customer-vouchers-mapper.ts`: el mapper voucher→`ProtoInvoiceInput` ya **NO** cae a `new Date()` cuando `fecha_emision` no parsea. Ahora retorna `MapVoucherToProtoInvoiceResult`; el pipeline rechaza la fila, incrementa `errors` y emite log estructurado `kind: "row_skip_invalid_fecha_emision"`. `tsc --noEmit` y `vitest run` pasan. Idempotencia, balances, enrichment, dedupe y upsert intactos.
- 2026-05-07: **Fase 1/2 sync recibos Zeta implementada**: migración `supabase/zeta-05-01-proto-receipts-zeta-sync.sql` (currency_code, company_id nullable), `ProtoReceiptInput` extendido, mapper con normalización de moneda y rechazo de anulaciones, pipeline con métricas extra y `allowUnlinkedCompany`. Validado con `tsc --noEmit` y `vitest` 144/144.
- 2026-05-07: **DIV-001 (RESTRecibosCobranzaV2QueryComprobantes — request shape)** corregido sin tocar persistence: `buildQueryInData` en `lib/integrations/zeta/zeta-collection-receipts-fetch.ts` ahora omite filtros opcionales vacíos (causaban HTTP 400 al castear `int.Parse("")` en binder ASP.NET de Zeta) y envía `Mes` sin `padStart`. Log estructurado `kind: "zeta_receipts_payload_shape"` agregado. Tests de regresión (`zeta-collection-receipts-fetch.test.ts`, 6 casos) garantizan que no se reintroduzca la causa raíz. Documentado en `temp-audits/audit-receipts-payload-shape.md`.
- 2026-05-07: **DIV-002 (RESTRecibosCobranzaV2QueryComprobantes — response shape)** corregido: el contract `lib/integrations/zeta/contracts/zeta-collection-receipts.contract.ts` ahora soporta `QueryComprobantesOut.Response[]` (Postman oficial / forma real del tenant) además de `QueryOut.Response[]` (legacy `0134`), array raíz, y fallback defensivo (`*Out` con primer array bajo claves prioritarias `Recibos / Items / Data / Result / etc.`). Nuevo helper `summarizeZetaCollectionReceiptsResponseShape(raw)` y log estructurado `kind: "zeta_receipts_raw_response"` en cada fetch. Tests (16 casos) cubren todos los shapes. Documentado en `docs/vendors/z/KNOWN-DIVERGENCES.md` (DIV-002).
- 2026-05-07: **Sync Recibos Zeta enero–mayo 2026 ejecutada exitosamente**:
  - Endpoint real validado: `POST /APIs/RESTRecibosCobranzaV2QueryComprobantes` con envelope `QueryComprobantesIn → { Connection, Data: { Page, Filters: { Anio, Mes } } }`.
  - Shape real respuesta: `QueryComprobantesOut.Response[]` (DIV-002 documentado).
  - Resultados por mes: enero=58, febrero=55, marzo=48, abril=67, mayo=6 → **234 recibos** persistidos, 0 errores, 0 unlinked, 0 invalid date, 0 invalid amount, 0 negative.
  - Tenant: `040321ff-10fd-4da3-aeca-f1865f879986` (EASY DIGITAL AGENCY).
- 2026-05-07: **Reconciliación Recibos 2026 vs Excel maestro: paridad 100 %**:
  - Excel `temp-audits/RecibosCobranzaWWExport-67.xlsx`: **234 filas**.
  - DB `proto_receipts`: **234 filas**.
  - Coincidencias plenas: **234** (100 %).
  - 0 ghost rows · 0 missing · 0 currency mismatches · 0 amount mismatches · 0 date mismatches · 0 cliente mismatches · 0 duplicados reales.
  - Distribución por mes y por moneda (USD=111 / UYU=123): idéntica entre Excel y DB.
  - 2 recibos legítimos con `Numero=0` (sin emitir / borradores Zeta) presentes en ambos lados; el reconciliador desempata por tupla `(Numero, Fecha, Cliente normalizado, Total, Currency)`.
  - Outputs: `temp-audits/receipts-reconciliation-2026.md`, `temp-audits/receipts-reconciliation-diff.csv` (vacío, solo header), `scripts/audit-receipts-reconciliation-2026.ts` reusable.

## Decisiones técnicas
- **Archivos temporales fuera de carpetas estructurales.** CSVs, Excels, diffs, reportes de auditoría y exports de validación viven exclusivamente en `temp-audits/` (o `tmp/`). Prohibido ubicarlos en `app/`, `lib/`, `services/`, `supabase/`, `scripts/`, `public/`, `mocks/` o `components/`.
- **No acoplar lógica productiva a `temp-audits/`.** El código en producción no importa nada desde esa ruta. Lógica reutilizable (parsers, normalizadores, comparadores) vive en `lib/`, `services/` o `scripts/` según corresponda.
- **Reconciliador Excel↔Copilot debe filtrar borradores CFE** antes de comparar (`Numero <= 0` o `Emitida = "N"`). Zeta `VentasExport.xlsx` incluye borradores; la API `RESTComprobantesClienteV1Query` correctamente no los devuelve. Documentado en `temp-audits/audit-prestis-numero-0.md` §6.3 (propuesta NOTE-001 para `KNOWN-DIVERGENCES.md`).
- **Recibos Zeta unlinked se persisten sin cliente sintético.** Si `ClienteCodigo`/RUT del recibo no resuelve a `proto_companies`, el sync guarda el recibo con `company_id = NULL` y `workspace_company_id` poblado. No se crea `Consumidor Final` ni otro cliente artificial. No se vinculan facturas por heurística en esta iteración.
- **`Filters` Zeta sólo lleva claves con valor real.** Convención del proyecto: las claves `<integer>` opcionales (`ComprobanteCodigo`, `MonedaCodigo`, `LocalCodigo`, etc.) se omiten cuando no hay filtro; nunca se envían como `""`. Aplica a todos los métodos REST V2 con shape `Data → Page + Filters`.
- **Recibos Zeta con `Numero=0` son válidos.** Convención Zeta: recibos sin emitir / borradores comparten `Numero=0`. El identificador único es `RegistroId` (→ `receipt_number = "ZETA:COB:<RegistroId>"`). Reconciliadores deben desempatar por tupla completa `(Numero, Fecha, Cliente, Total, Currency)`, no solo por `Numero`.

## Pendientes
- **ZETA-10 — bloqueo de negocio · confirmar fuente real de egresos del cliente**. Audit Zeta concluido: la empresa no registra egresos en Zeta para este tenant (0 filas en pagos a proveedores, compras, cuotas proveedor, saldos pendientes proveedor, movimientos de caja, débitos bancarios y asientos contables, sobre ventanas abr-2026 / dic-2025 / "now" según corresponda; el único endpoint con datos es `RESTMovimientosBancariosV1Query` con 168 filas pero todas créditos = ingresos). Antes de reanudar ZETA-10 hace falta validar con el cliente/contador dónde se registran efectivamente: (1) gastos operativos, (2) pagos a proveedores, (3) débitos bancarios / transferencias de salida, (4) impuestos (DGI/IRAE/IVA/BPS), (5) sueldos y cargas sociales, (6) transferencias internas entre cuentas, (7) egresos por caja chica / efectivo, (8) cualquier otro flujo monetario de salida. Posibles escenarios y caminos: (a) si los registran en Zeta pero el rol API no los ve → revisar `RolCodigo` y pedir permiso sobre `RESTMovimientosBancariosV1Query` con `ComprobanteTipo=52` y/o `RESTRecibosPagosV1*`; (b) si los registran en Zeta UI pero la API no expone esos comprobantes → pedir a soporte Zeta endpoint dedicado; (c) si los registran en otro sistema (Excel/banco directo/otro ERP/SIIGO/etc.) → diseñar ingestión alternativa (upload manual, conector banco, integración aparte); (d) si no los registran en ninguna parte → módulo "Egresos" del Copilot necesita captura nativa propia. Sin esa confirmación, NO implementar `proto_bank_movements` / `proto_cash_movements` / "Tesorería" para evitar repetir el escenario ZETA-09-01 (0 filas persistidas).
- **ZETA-10 — reanudación técnica (cuando se confirme fuente)**. Si la confirmación llega como escenario (a) o (b) con datos disponibles, retomar con la recomendación arquitectónica ya registrada en "Últimos cambios" (tablas nuevas independientes de `proto_payments`, no mezclar semánticas). Si llega como (c) o (d), abrir un frente separado fuera del paraguas Zeta.
- **ZETA-09-01 validación real**: re-correr smoke contra ventanas con actividad real cuando se identifique un mes con ≥1 recibo de pago a proveedor cargado en Zeta. Smoke runner disponible en `temp-audits/smoke-sync-vendor-payments.mts <mes> <anio>`. Si tras varias ventanas sigue vacío, evaluar: (a) `RolCodigo` con permisos a `RESTRecibosPagos*`, (b) variante V2 del endpoint, (c) si la empresa efectivamente usa el módulo "Recibos de Pago" Zeta o gestiona pagos por otro circuito.
- **ZETA-09-01 commit `e716097` no pushed**: queda local hasta que se confirme push explícito.
- **Ejecutar saldos pipeline abril 2026**: las 62 facturas reactivadas quedaron con `status=issued` tras el re-sync de vouchers (VentasDetalladas no tiene balance real-time). El próximo run de saldos corregirá balances y status de las 44 previamente pagadas vía `zeroCcV1BalancesWithoutSaldoRow`.
- Evaluar cierre de período: revisar si conviene ampliar el alcance reconciliador a mayo–junio 2026.
- **UI Recibos refinements** (no bloqueante): orden visual por `receipt_date` DESC (hoy ordena por `created_at` DESC con `receipt_date` como secundario), agregar columna de moneda al lado del importe (similar a invoices), exponer cliente (lookup `company_id → proto_companies.name` o `notes.raw_payload.ClienteNombre` cuando `company_id` es NULL).
- **Linking factura↔recibo** (opcional, no implementado): hoy todos los recibos quedan registrados como recibos puros sin `invoice_id`. Si se decide vincular, hace falta heurística confiable o reglas explícitas (ej.: matching por `Serie+Numero` con `proto_invoices`); requiere análisis aparte para no afectar `balance_amount`.
- **Automatizar sync mensual de recibos** (cron / scheduler) tras el cierre actual.

## FASE 5C — Executive Decision Layer + UX Consolidation (2026-05-18)

**Entregados:**
- `lib/decision-engine/executive-decision-engine.ts` — motor determinístico que combina señales (portfolio_score, analytics, predictive, ranked_clients, automation_runs, pipeline_signals) en `ExecutiveDecisionBrief` con 8 reglas de negocio.
- `lib/decision-engine/executive-timeline-builder.ts` — línea de tiempo ejecutiva desde acciones, follow-ups, automatización y estados.
- `lib/data/decision-executive-briefing-repository.ts` — repositorio con TTL 15 min.
- `supabase/de-11-executive-briefings.sql` — tabla `decision_executive_briefings` (RLS tenant-safe, pendiente aplicar en Supabase).
- `app/api/copilot/decision-engine/executive-brief/route.ts` — `GET /api/copilot/decision-engine/executive-brief` con cache-first + `?force=true`.
- `components/copilot/decision-engine/executive-command-center.tsx` — UI ejecutiva con top 3 decisiones, evidencia expandible, CTAs, timeline.
- `components/copilot/decision-engine/executive-timeline.tsx` — timeline compacto de 5 eventos recientes.
- `lib/decision-engine/executive-decision-engine.test.ts` — 27 tests cubriendo 8 reglas + fallbacks + ordering + confidence.
- Modificaciones `daily-briefing-card.tsx` — paneles reordenados (score → exec command center → intel colapsable → forecast colapsable → analytics → automation colapsado → cola), collapsible wrapper.
- Modificaciones `client-operational-summary-card.tsx` — UX polish: border por prioridad, sin bloques de color internos, detalles expandibles.

**Validado:** `tsc --noEmit` exit 0 · `vitest run` 1392/1392 verdes · `npm run build` limpio.

**Pendiente operativo:** aplicar `supabase/de-11-executive-briefings.sql` en Supabase para activar persistencia del executive brief.

## TREASURY-NOTIFICATIONS-001 (2026-06-10)

**Tesorería / Movimientos:** orden descendente (fecha → `created_at` → `id`) en `sortManualCashMovementsNewestFirst` + repo query; botón "Nuevo movimiento" removido del tab Movimientos (lógica create/edit interna intacta).

**Campana (NOTIFICATIONS-FINANCIAL-EVENTS-001):** generación ampliada en `generate-operational-notifications.ts` — `new_debtor` (actividad reciente + lifecycle), `collection_received` parcial, `client_debt_settled`, `client_overdue` por factura (`invoice_overdue:*` dedup). Sin realtime; dedup por sync. Fuera de alcance: `cash_risk_detected`, `sync_changes_detected`.

**Validado:** suite completa + `tsc --noEmit` OK.

## Próximo paso recomendado
- **Ejecutar saldos pipeline para abril 2026** para corregir los 44 status=issued que deberían estar paid (invocar `POST /api/zeta/sync-saldos-all-clients` o equivalente con mes=4, anio=2026). Confirmar post-run: paid_active debe subir de 18 a ~62, issued_active bajar a los reales emitidos.
