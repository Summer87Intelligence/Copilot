# Project Context

## Resumen del proyecto
- Pendiente de completar.

## Estado actual
- Proyecto inicializado con Claude Skills.
- Convención de archivos temporales establecida (`temp-audits/`).
- **Pipeline Recibos Zeta operativo** (enero–mayo 2026 sincronizado, reconciliación 100 %).

## Últimos cambios
- 2026-05-09: **Automatización enterprise pipelines Zeta (ZETA-06)**. 5 fases implementadas:
  - **Fase 1 — Cron orchestration**: 3 nuevos cron routes autenticados con `CRON_SECRET`. `/api/cron/zeta-sync-saldos` (cada 3h), `/api/cron/zeta-sync-vouchers` (cada 6h, meses actual+anterior), `/api/cron/zeta-sync-contacts` (diario 02:00 UTC). `vercel.json` actualizado. El route anterior `/api/cron/sync-saldos` queda como legacy.
  - **Fase 2 — Pipeline Run Registry**: tabla `zeta_pipeline_runs` (`supabase/zeta-06-01-pipeline-runs-schema.sql`), tipos (`lib/data/zeta-pipeline-run-types.ts`) y repositorio (`lib/data/zeta-pipeline-run-repository.ts`) con `createPipelineRun`, `updatePipelineRun`, `findActivePipelineRun`, `getRecentPipelineRuns`.
  - **Fase 3 — Anti-overlap**: cada cron consulta `findActivePipelineRun` antes de arrancar; si hay un run activo en la ventana temporal → skip con log estructurado.
  - **Fase 4 — Retry conservador**: `lib/integrations/zeta/zeta-retry.ts` — `withZetaRetry<T>` + `isTransientZetaError`. Max 3 retries, backoff exponencial 1s→2s→4s (cap 30s), solo errores de red/5xx/429. Sin retry en 4xx.
  - **Fase 5 — Health layer**: `lib/data/zeta-pipeline-health.ts` — tipos `PipelineHealthStatus`, `PipelineHealthSummary`, función pura `derivePipelineHealth`, `getAllPipelineHealth`. Listo para dashboard.
  - Reconciliación orphans integrada en el cron saldos (`syncMode: "reconciliation_cleanup"`). 563 tests, 41 archivos, tsc limpio.
- 2026-05-09: **Auditoría y reactivación controlada abril 2026 (62 facturas)**. FASE 2 completada: causa raíz confirmada (pipeline viejo del 22/04 con `is_active=false, archived_at=null` — estado imposible con código actual). Script `scripts/reactivate-abril-2026.ts` ejecutado: 62/62 filas reactivadas (is_active=true), re-sync vouchers abril 2026 procesó 129 filas, actualizó 62, 0 errores. Post-sync: active_vouchers=62 (antes 0), inactive_vouchers=0. Efecto colateral esperado: las 44 facturas previamente pagadas ahora muestran `status=issued` (VentasDetalladas sin saldo real-time). El próximo run de saldos pipeline corregirá balances vía `zeroCcV1BalancesWithoutSaldoRow`. Reporte en `temp-audits/audit-abril-2026-fase2-report.md`. 521 tests, tsc limpio.
- 2026-05-09: **Pipeline Contactos Zeta (DIV-003)**: `contracts/zeta-contacts.contract.ts` soporta `QueryOut.Response[]` (Postman/tenant real) + retrocompat `QueryOut.Contactos.Contacto[]`. `zeta-contacts-pipeline.ts`: sync incremental paginado con upsert por Codigo/Documento/email, resolución FK a proto_companies, trazabilidad en zeta_sync_*, retry. API `POST /api/zeta/sync-contacts`. DIV-003 + NOTE-001 documentados en `KNOWN-DIVERGENCES.md`. 521 tests, tsc limpio.

- 2026-05-09: **Collection Operations Layer — Fase 1 implementada**. Capa operativa de cobranza independiente de datos financieros Zeta. Tabla nueva `collection_actions` (SQL: `supabase/collection-operations.sql`) con RLS completa por workspace, soft delete, enums pg para status/type/priority. Tipos TypeScript: `lib/copilot-collection-types.ts` (CollectionAction, CollectionActionInput, CollectionActionPatch, ClientCollectionSummary, ClientCollectionTimeline, labels UI). Servicio `lib/copilot-collection-service.ts`: create/update/archive/getByCompany/getByWorkspace, pattern idéntico a proto-crud. Repository puro `lib/data/collection-read-repository.ts`: deriveClientCollectionSummary, buildClientCollectionTimeline, rankClientsByCollectionPriority (priorización: overdue promise → escalated → priority score → stale → no_action). API routes: POST create, PATCH [id]/update, POST [id]/archive, GET collection-actions (por company o workspace). Hook `hooks/use-collection-actions.ts` (useReducer + AbortController, sin race conditions). Drawer `components/copilot/collection-action-drawer.tsx` (form completo con campos de promesa de pago condicionales). ClientDebtExplorer actualizado: columna "Cobranza" con estado + botón "Registrar acción" (+), timeline de acciones en expand row. `tsc --noEmit` limpio, 22/22 tests nuevos verdes.

- 2026-05-09: **Bloque 5 — Aging Analytics + Client Debt Explorer + Explainability Panel**. Backend extendido con `agingByCurrency` (4 buckets 0-30/31-60/61-90/90+ por moneda: amount, invoiceCount, clientCount, percentage), `dominantAgingRange` por cliente y `pre2026InvoiceCount` en gaps — todo en O(n) en el mismo pass de facturas. Nuevos componentes: `AgingAnalytics` (tabs UYU/USD, barras horizontales animadas con framer-motion, colores fintech muted), `ClientDebtExplorer` (tabla con search debounced, sort, 7 filter chips, expand rows, paginación 25/50/100, derivación de risk level render-only), `ExplainabilityPanel` (auditoría financiera: period exclusion, unknown currency, stale pending, pre-2026, orphan warnings, voided). Corrección definitiva de "Efectividad cobranza global": reemplazado el card ponderado multi-moneda por dos cards separados "Efectividad UYU" y "Efectividad USD". `CarteraShell` actualizado: 4 secciones activas + skeleton de aging. 416 tests verdes (41 nuevos de aging), `tsc --noEmit` limpio.


- 2026-05-08: **Salud financiera evolucionada a centro de decisiones**. El dashboard de `/copilot` ahora prioriza interpretación y acción antes de métricas: 1) `FinancialPriorityAlerts` ("Atención requerida hoy"), 2) `FinancialActionPriorities` ("Prioridades de acción"), 3) `FinancialRiskSummary` ("Riesgo financiero"), 4) KPIs financieros, 5) aging, 6) top deudores detallado. Nuevo motor puro `lib/copilot-financial-priority-engine.ts` genera alertas, scores de prioridad y semáforos usando reglas determinísticas/auditables sobre `FinancialDashboardMetrics` (sin IA): high por deuda >90 días, concentración >60%, efectividad <80%, cliente >25% deuda; medium por efectividad 80-90, aging 61-90 significativo y muchos clientes con deuda. `priorityScore` combina peso de monto, aging dominante y concentración (0-100). `TopDebtor` se enriqueció con `invoiceCount`, `dominantAgingLabel`, `oldestAgeDays`, `totalInvoiced`, `totalCollected` y `collectionEffectiveness` para acciones y tabla analítica, manteniendo campos previos. Tests nuevos `copilot-financial-priority-engine.test.ts` (6 casos). Validado con `tsc --noEmit`, vitest métricas+priority+deuda actual y eslint de componentes.
- 2026-05-08: **Salud financiera ejecutiva en `/copilot` (Hoy)**. El bloque ejecutivo ahora vive en la home — no en el sidebar de cliente. `app/copilot/page.tsx` carga `getProtoInvoices("all")` + `getProtoCompanies("all")`, calcula `buildFinancialDashboardMetrics` y renderiza `CopilotFinancialDashboard` dentro de un `CopilotCard` con título "Salud financiera", ubicado entre "Liquidez y cobertura" y "Próximos vencimientos". El sidebar de cliente quedó revertido al alcance previo (deuda actual + estado de cuenta + ledger histórico + por factura + movimientos + cortes). UI del dashboard simplificada: el wrapper visual lo provee la home; el componente sólo renderiza la grilla por moneda con KPIs, aging, top deudores e insights ejecutivos en `lg:grid-cols-2`. Datos reales del tenant verificados: USD 155 facturas (135/3/17, efectividad 93.61%, pendiente U$S 6.352,99) y UYU 179 facturas (170/1/8, efectividad 95.75%, pendiente $ 148.691). Validado con `tsc --noEmit`, 79 tests verdes y eslint.
- 2026-05-08: **Executive Financial Dashboard — Fase 1 implementada**. Nueva capa reusable `lib/copilot-financial-dashboard-metrics.ts` construye métricas financieras puras desde ledger completo (`getProtoInvoices("all")`) y `balance_amount`, reutilizando utilidades de `copilot-client-current-debt-summary` para moneda, anuladas/canceladas, clasificación paid/partial/pending y montos. Métricas por moneda: facturado, cobrado, pendiente, counts, clientes deudores, efectividad de cobranza, top 5 deudores y aging 0-30/31-60/61-90/90+ basado en `issue_date`. No usa recibos ni linking inferido; no depende de filtros UI, tablas visibles ni `is_active=true`; incluye archivadas válidas y excluye anuladas/corruptas. Tests dedicados (7 casos: multi-moneda, aging, top debtors, exclusiones, archivadas, cero divisores, inmutabilidad).
- 2026-05-08: **Bloque "Deuda actual del cliente"** agregado al sidebar de detalle de cliente. Helper puro `lib/copilot-client-current-debt-summary.ts` calcula `SUM(balance_amount)` agrupado por moneda sobre el dataset ledger COMPLETO (`ledgerInvoices`), independiente de cualquier filtro del panel. No usa recibos ni Debe/Haber. Excluye comprobantes con `status` anulado/cancelado y filas con moneda indeterminable. Componente `components/copilot/copilot-client-current-debt-summary.tsx` muestra cards por moneda (Pendiente, counts cobradas/parciales/pendientes, Facturado/Cobrado histórico) + banner contextual emerald (sin deuda) / amber (con deuda). Mountado en `copilot-data-sidebar.tsx` arriba del bloque de filtros y del Estado de cuenta. 13 tests dedicados (90 tests verdes en total). Caso Álvarez verificado contra DB real: USD pendiente=0, 4 cobradas (incluye archivada A-2863), banner "no registra saldo pendiente" — aunque el estado de cuenta filtrado al 17/04 muestre USD 732, este bloque sigue mostrando USD 0 (deuda actual).
- 2026-05-07: creada carpeta `temp-audits/` aislada para artefactos de auditoría/importación/validación. Ignorada en `.gitignore` (sólo se versiona `temp-audits/README.md`). También se ignora `/tmp/`.
- 2026-05-07: auditoría reconciliación enero–abril 2026 (read-only):
  - **63 facturas faltantes en abril 2026** → causa raíz priorizada en `temp-audits/audit-abril-2026-report.md`. Hipótesis principal: `issue_date` mal parseado (fallback `new Date()` en `mapCopilotCustomerVoucherToProtoInvoiceInput`). Pendiente confirmar con queries SQL §A.2/§B.3/§E.1.
  - **1 fila Excel Nº=0 (Prestis S.A.S. 04-mar UYU 9.760)** confirmada como **borrador CFE no emitido** (`Emitida="N"`, `Estado DGI=""`). NO requiere sync. Detalle en `temp-audits/audit-prestis-numero-0.md`. Reduce el universo "Excel emitido" a **281** filas.
  - Discrepancia residual real: 62 facturas de abril, no 63.
- 2026-05-07: **fix H1 aplicado** en `lib/integrations/zeta/zeta-customer-vouchers-mapper.ts`: el mapper voucher→`ProtoInvoiceInput` ya **NO** cae a `new Date()` cuando `fecha_emision` no parsea. Ahora retorna `MapVoucherToProtoInvoiceResult`; el pipeline rechaza la fila, incrementa `errors` y emite log estructurado `kind: "row_skip_invalid_fecha_emision"`. `tsc --noEmit` y `vitest run` pasan. Idempotencia, balances, enrichment, dedupe y upsert intactos.
- 2026-05-07: **Fase 1/2 sync recibos Zeta implementada**: migración `supabase/zeta-05-01-proto-receipts-zeta-sync.sql` (currency_code, company_id nullable), `ProtoReceiptInput` extendido, mapper con normalización de moneda y rechazo de anulaciones, pipeline con métricas extra y `allowUnlinkedCompany`. Validado con `tsc --noEmit` y `vitest` 144/144.
- 2026-05-07: **DIV-001 (RESTRecibosCobranzaV2QueryComprobantes — request shape)** corregido sin tocar persistence: `buildQueryInData` en `lib/integrations/zeta/zeta-collection-receipts-fetch.ts` ahora omite filtros opcionales vacíos (causaban HTTP 400 al castear `int.Parse("")` en binder ASP.NET de Zeta) y envía `Mes` sin `padStart`. Log estructurado `kind: "zeta_receipts_payload_shape"` agregado. Tests de regresión (`zeta-collection-receipts-fetch.test.ts`, 6 casos) garantizan que no se reintroduzca la causa raíz. Documentado en `temp-audits/audit-receipts-payload-shape.md`.
- 2026-05-07: **DIV-002 (RESTRecibosCobranzaV2QueryComprobantes — response shape)** corregido: el contract `lib/integrations/zeta/contracts/zeta-collection-receipts.contract.ts` ahora soporta `QueryComprobantesOut.Response[]` (Postman oficial / forma real del tenant) además de `QueryOut.Response[]` (legacy `0134`), array raíz, y fallback defensivo (`*Out` con primer array bajo claves prioritarias `Recibos / Items / Data / Result / etc.`). Nuevo helper `summarizeZetaCollectionReceiptsResponseShape(raw)` y log estructurado `kind: "zeta_receipts_raw_response"` en cada fetch. Tests (16 casos) cubren todos los shapes. Documentado en `docs/vendors/z/KNOWN-DIVERGENCES.md` (DIV-002).
- 2026-05-07: **Sync Recibos Zeta enero–mayo 2026 ejecutada exitosamente**:
  - Endpoint real validado: `POST /APIs/RESTRecibosCobranzaV2QueryComprobantes` con envelope `QueryComprobantesIn → { Connection, Data: { Page, Filters: { Anio, Mes } } }`.
  - Shape real respuesta: `QueryComprobantesOut.Response[]` (DIV-002 documentado).
  - Resultados por mes: enero=58, febrero=55, marzo=48, abril=67, mayo=6 → **234 recibos** persistidos, 0 errores, 0 unlinked, 0 invalid date, 0 invalid amount, 0 negative.
  - Tenant: `040321ff-10fd-4da3-aeca-f1865f879986` (EASY DIGITAL AGENCY).
- 2026-05-07: **Reconciliación Recibos 2026 vs Excel maestro: paridad 100 %**:
  - Excel `temp-audits/RecibosCobranzaWWExport-67.xlsx`: **234 filas**.
  - DB `proto_receipts`: **234 filas**.
  - Coincidencias plenas: **234** (100 %).
  - 0 ghost rows · 0 missing · 0 currency mismatches · 0 amount mismatches · 0 date mismatches · 0 cliente mismatches · 0 duplicados reales.
  - Distribución por mes y por moneda (USD=111 / UYU=123): idéntica entre Excel y DB.
  - 2 recibos legítimos con `Numero=0` (sin emitir / borradores Zeta) presentes en ambos lados; el reconciliador desempata por tupla `(Numero, Fecha, Cliente normalizado, Total, Currency)`.
  - Outputs: `temp-audits/receipts-reconciliation-2026.md`, `temp-audits/receipts-reconciliation-diff.csv` (vacío, solo header), `scripts/audit-receipts-reconciliation-2026.ts` reusable.

## Decisiones técnicas
- **Archivos temporales fuera de carpetas estructurales.** CSVs, Excels, diffs, reportes de auditoría y exports de validación viven exclusivamente en `temp-audits/` (o `tmp/`). Prohibido ubicarlos en `app/`, `lib/`, `services/`, `supabase/`, `scripts/`, `public/`, `mocks/` o `components/`.
- **No acoplar lógica productiva a `temp-audits/`.** El código en producción no importa nada desde esa ruta. Lógica reutilizable (parsers, normalizadores, comparadores) vive en `lib/`, `services/` o `scripts/` según corresponda.
- **Reconciliador Excel↔Copilot debe filtrar borradores CFE** antes de comparar (`Numero <= 0` o `Emitida = "N"`). Zeta `VentasExport.xlsx` incluye borradores; la API `RESTComprobantesClienteV1Query` correctamente no los devuelve. Documentado en `temp-audits/audit-prestis-numero-0.md` §6.3 (propuesta NOTE-001 para `KNOWN-DIVERGENCES.md`).
- **Recibos Zeta unlinked se persisten sin cliente sintético.** Si `ClienteCodigo`/RUT del recibo no resuelve a `proto_companies`, el sync guarda el recibo con `company_id = NULL` y `workspace_company_id` poblado. No se crea `Consumidor Final` ni otro cliente artificial. No se vinculan facturas por heurística en esta iteración.
- **`Filters` Zeta sólo lleva claves con valor real.** Convención del proyecto: las claves `<integer>` opcionales (`ComprobanteCodigo`, `MonedaCodigo`, `LocalCodigo`, etc.) se omiten cuando no hay filtro; nunca se envían como `""`. Aplica a todos los métodos REST V2 con shape `Data → Page + Filters`.
- **Recibos Zeta con `Numero=0` son válidos.** Convención Zeta: recibos sin emitir / borradores comparten `Numero=0`. El identificador único es `RegistroId` (→ `receipt_number = "ZETA:COB:<RegistroId>"`). Reconciliadores deben desempatar por tupla completa `(Numero, Fecha, Cliente, Total, Currency)`, no solo por `Numero`.

## Pendientes
- **Ejecutar saldos pipeline abril 2026**: las 62 facturas reactivadas quedaron con `status=issued` tras el re-sync de vouchers (VentasDetalladas no tiene balance real-time). El próximo run de saldos corregirá balances y status de las 44 previamente pagadas vía `zeroCcV1BalancesWithoutSaldoRow`.
- Evaluar cierre de período: revisar si conviene ampliar el alcance reconciliador a mayo–junio 2026.
- **UI Recibos refinements** (no bloqueante): orden visual por `receipt_date` DESC (hoy ordena por `created_at` DESC con `receipt_date` como secundario), agregar columna de moneda al lado del importe (similar a invoices), exponer cliente (lookup `company_id → proto_companies.name` o `notes.raw_payload.ClienteNombre` cuando `company_id` es NULL).
- **Linking factura↔recibo** (opcional, no implementado): hoy todos los recibos quedan registrados como recibos puros sin `invoice_id`. Si se decide vincular, hace falta heurística confiable o reglas explícitas (ej.: matching por `Serie+Numero` con `proto_invoices`); requiere análisis aparte para no afectar `balance_amount`.
- **Automatizar sync mensual de recibos** (cron / scheduler) tras el cierre actual.

## Próximo paso recomendado
- **Ejecutar saldos pipeline para abril 2026** para corregir los 44 status=issued que deberían estar paid (invocar `POST /api/zeta/sync-saldos-all-clients` o equivalente con mes=4, anio=2026). Confirmar post-run: paid_active debe subir de 18 a ~62, issued_active bajar a los reales emitidos.
