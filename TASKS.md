# Tasks

## Recently Done
- 2026-07-15 — **FASE 8 · Bandeja única de tareas inteligentes cerrada localmente**. `/copilot/tareas-diarias` quedó como una sola bandeja `Tareas`, sin selector visible Cuaderno/Todas, combinando tareas persistidas y recomendaciones virtuales por `stableKey`. Nuevo feed `/api/copilot/tasks/feed`, acciones de recomendaciones, scoring determinístico, reglas Cobranza/Clientes/Alertas/Datos con permisos server-side y sin migraciones/DML/cambios financieros. Validaciones: `tsc`, ESLint dirigido FASE 8, Vitest completo 2 veces, build y diff-check OK; `npx eslint` global conserva fallas preexistentes en artefactos ajenos.
- 2026-07-15 — **FASE 6A Increment 5 · Global visual consistency sweep cerrado localmente**. Pulido DS-Core/tokens/a11y en Finanzas, Reportes, Tareas, Alertas, Admin, Cliente 360, Dashboard, Banco, Tesorería, Manual y Mesa de ayuda. Sin nuevas funcionalidades, sin cambios de fórmulas financieras, scopes, RLS, RBAC ni permisos. Validaciones: `tsc`, ESLint dirigido, guardrail visual, suite Vitest completa, build, smoke Playwright y QA visual local. Banco tuvo retries aislados por latencia headless local; terminó 200 en combinaciones revisadas.

## Now
- **BANK-RECONCILIATION-CANONICAL-ENGINE-001 — cerrada en código 2026-07-20 (commit local, sin push)**. Decisión de arquitectura: Motor D (`bank_reconciliation_suggestions`+payer identities+RPC `confirm_/reverse_bank_reconciliation_v1`) es el único motor canónico de conciliación de ingresos. Matriz exhaustiva de writers en `docs/architecture/bank-reconciliation-canonical-engine.md`. Motor C (drawer "Conciliación detallada") retirado como escritor (bypaseaba la RPC, sin allocations/eventos) — POST/DELETE de `reconciliation-links` devuelven 410 server-side, no solo ocultos en UI. Motor A (Tesorería, `planned_cash_obligations`) renombrado/reubicado dentro de Movimientos, ya no es "Conciliar" en el flujo de cobros. Nueva lectura 100% read-only `lib/bank/canonical/canonical-suggestion-evidence.ts` + endpoint + panel, montada en la pestaña Conciliación (reordenada, destacada). Cero escrituras nuevas ejecutadas contra producción. Gap real encontrado: no existe código que escriba a `bank_payer_identities`/`client_payer_links` al confirmar (aprendizaje de pagador es trabajo nuevo, Fase 3). Próximo: Fase 2 BANK-CANONICAL-CONFIRM-UI (requiere antes corregir inconsistencia `bank_movements.status='reversed'` no válido en el CHECK).
- **SALES-SELLER-DOCUMENT-LEVEL-CONFIRMATION-001 — cerrada en código 2026-07-20 (commit local, sin push)**. Confirmación definitiva de negocio: vendedor = comprobante completo, nunca por línea. Cierra `DIV-CONT-011` como no requerida (no bloquea nada porque no se modela vendedor por línea). Sin cambios de modelo (`sales_document_salespersons` UNIQUE(workspace_id, document_id) ya en prod); descartado explícitamente crear tabla/endpoint/auditoría por línea. Único cambio de código: texto "↳ mismo comprobante" → "Incluido en este comprobante" en Detalle + test agregado en `seller-sales-summary.test.ts` (multi-línea 5 líneas / $68.320 → 1 operación, nunca $341.600). Docs actualizados: `docs/domains/ventas.md` (sección Ejecutivo vs Vendedor), `KNOWN-DIVERGENCES.md`. tsc 0 errores, vitest 4780 passed. Próximo: ninguno pendiente sobre este tema salvo que el negocio reabra vendedor-por-línea (requeriría antes confirmar con soporte Zeta la pregunta ya preparada en DIV-CONT-011).
- **BANK-SIMPLE-RECONCILIATION-RESTORE-001 — cerrada en código 2026-07-19 (commit local, sin push)**. Se retira la pantalla independiente `/copilot/revision-bancaria` + APIs `/api/copilot/bank-review/*` + componentes `bank-review/*` + `lib/bank/review/*` + item nav + entrada RBAC; el flujo vuelve al módulo **Banco** (movimientos-bancarios: import Excel/PDF, conciliación N:M, matching ingreso→cliente). **Sin rollback DB**: tablas/suggestion_scope/RPC/migraciones/eventos/las 11 suggestions+13 events quedan inactivos (no borrados). Motor `lib/bank/intelligence/**` conservado inactivo. Test `bank-review-retired` (7); tsc/eslint/387 bank+intelligence/RBAC coverage verdes. Próximo: QA simple de Banco.
- **BANK-HISTORICAL-REVIEW-ACTIONS-001 — cerrada en código 2026-07-19 (commit local, sin push)**. Acciones humanas no financieras: marcar revisada (solo historical), agregar nota, rechazar (op/hist). Modelo A (reviewed_at/reviewed_by; sin status='reviewed'); rechazo=status'rejected'+reason. Migración `20260721120000_bank_review_actions.sql` **CREADA NO APLICADA** (event types + 3 RPC transaccionales service_role-only + índice). API POST review/reject/notes con write-RBAC; UI drawer wired + badges Pendiente/Revisada/Rechazada + filtro revisión; Pendientes=activo+reviewed_at IS NULL. Atomicidad/idempotencia/concurrencia; sin confirm/reverse/links/allocations. Tests review+schema-contract verdes. **Requiere autorización para aplicar migración.** Confirmar conciliación operativa: fuera de alcance.
- **BANK-HISTORICAL-SHADOW-PERSIST-POLICY-001 — contrato cerrado en código 2026-07-19 (commit local, sin push)**. Columna canónica `suggestion_scope` (`operational`/`historical_review`/`matched_audit`) + migración `20260720120000_bank_suggestion_scope.sql` **CREADA, NO APLICADA** (aditiva; reemplaza índice idempotencia por uno con scope). Idempotencia por ámbito, `listOperationalSuggestions()`/`listHistoricalReviewSuggestions()`, eventos con `metadata.suggestionScope`. Runner `persistHistoricalForReview` (flag default false; requiere includeHistorical+persist+IDs; solo historical_review; omite post-corte/matched; nunca AUTO/cron). Las 5 sugerencias→operational; 2 matched no reclasificadas. Tests bank intelligence 134. Doc `bank-historical-review-contract.md`. **Requiere autorización para aplicar migración en prod.** Cero escrituras/DDL remoto.
- **BANK-SHADOW-HISTORICAL-SCOPE-001 — cerrada en código 2026-07-19 (commit local, sin push)**. Modo `includeHistoricalForShadow=true` (server-side, default false): analiza `[2026-01-01, 2026-07-01)` como historical-audit (`historicalAudit=true`, `auditOnly=true`, warning `HISTORICAL_SHADOW_AUDIT`, nunca AUTO). Dry-run only (`persist=true`→`HISTORICAL_PERSIST_NOT_ALLOWED`), exige IDs explícitos (sin IDs→`HISTORICAL_SCOPE_REQUIRES_IDS`), `< 2026-01-01`→`MOVEMENT_BEFORE_GLOBAL_FLOOR`. Cortes global (2026-01-01) y bancario (2026-07-01) intactos. 424 pending históricos no se procesan automáticamente. Flujos operativo/matched/historical separados. Tests eligibility 19 + runner 12. Cero escrituras. Próximo: dry-run histórico controlado por IDs.
- **BANK-SHADOW-PERSIST-EXPANSION-001 — NO_GO 2026-07-19 (sin escrituras)**. Inventario elegible operativo = 1 movimiento (resto post-corte ya `matched`; 424 pending son pre-corte). No se pudo armar muestra 12–18; halt en selección, sin dry-run ni persistencia. Motiva BANK-SHADOW-HISTORICAL-SCOPE-001.
- **BANK-SHADOW-MATCHED-POLICY-001 — cerrada en código 2026-07-17 (commit local, sin push)**. Política única `isShadowEligibleMovement()`: excluye por defecto `matched`/`ignored`/`reversed`/egreso/link-activo/fuera-de-workspace/pre-corte `2026-07-01`, idéntica en todos los caminos. `matched` solo `includeMatchedForAudit=true` (server-side, default false) → audit-only (`auditOnly=true`, warning `MATCHED_MOVEMENT_AUDIT`, nunca AUTO, no persiste). Runner integra el gate; audit-only nunca persiste. Las 2 sugerencias `matched` históricas (`49bd2ddc…`/`1c611fcd…`) se conservan. Tests eligibility (11) + runner matched (3); bank intelligence 109 verdes. Docs actualizados. Próximo: shadow persist en lote ampliado solo con movimientos elegibles.
- **BANK-CONTROLLED-SHADOW-PERSIST-REVIEW-001 — primera persistencia shadow 2026-07-17 (prod, sin push)**. 5 suggestions + 5 events (`suggestion_created` 1:1), 6º UNIDENTIFIED skip por evidencia insuficiente; idempotencia verificada (2ª corrida 0 writes); cero escrituras financieras; sin datos sensibles. 2 movimientos ya `matched` procesados por ID explícito → motiva BANK-SHADOW-MATCHED-POLICY-001.
- **BANK-SHADOW-CORRECTION-001 — cerrada en código 2026-07-17 (sin push)**. Empate/colisión de recibos: sin selección arbitraria; warnings `MULTIPLE_STRONG_CANDIDATES` / `RECEIPT_CANDIDATE_COLLISION`. Próximo: dry-run revalidation controlado.
- **BANK-SHADOW-SERVER-IMPLEMENTATION-001 — cerrada en código 2026-07-17 (sin push)**. Capa `lib/bank/intelligence/server/` (repositories/loaders/mappers/suggestion-service/shadow-persistence/runner). Dry-run default; persist opcional solo suggestions+events. Sin RPC financiera / links / allocations / cron / UI / runner prod.- **BANK inteligencia — post-migración cerrada 2026-07-17 (producción aplicada, sin push)**. Migraciones `20260719120000`/`120100`/`120200` aplicadas en `erzdifkvvailxnwdukzf` e inmutables. Validación read-only: sin fixtures persistidos; datos reales intactos; baterías C1–C9 y D1–D8 evidencia en logs con rollback; concurrencia 25–28 pendiente en Postgres local.
- **BANK-MOVEMENTS + DAILY-TASKS Sprint B — CRUD manual usable 2026-07-09, sin push**. Migración ya aplicada (version `20260709120938`). APIs POST/PATCH/DELETE para bank-movements y daily-tasks (Zod + write-access + workspace-scoped). UI operativa: Banco (crear/editar/conciliar/ignorar/eliminar movimientos) y Tareas (crear/completar/reabrir/editar/eliminar + filtros). Fuera de scope: parser de extractos, matching automático, cron, generación automática de tareas. Sprint C: upload+parser (reusar `lib/treasury/santander-*`) + motor de sugerencias + generación de tareas.
- **BANK-MOVEMENTS + DAILY-TASKS Sprint A — scaffold entregado 2026-07-09, sin push**. Migración `20260709000000_bank_movements_daily_tasks.sql` en preview: **NO aplicada, esperando aprobación de Andrés**. Módulos `/copilot/movimientos-bancarios` y `/copilot/tareas-diarias` con RBAC, APIs GET base (fallback controlado sin tablas), sidebar, manual y tests. Importador bancario viejo removido visualmente de Tesorería (lógica intacta para reusar). Sprint B: aplicar migración + upload/parser (reusar `lib/treasury/santander-*`) + sugerencias de matching. Sprint C: confirmación de conciliación + generación de tareas.
- **ZETA-12 — Motor financiero tesorería · Fase 4 en repo**. Motores `lib/treasury/treasury-{alert-engine,cash-projection,recurring-obligations,insights}.ts`; APIs `projection` / `alerts` / `insights` / `recurring-obligations`; SQL `zeta-12-01` pendiente de aplicar en Supabase; dashboard `/copilot/tesoreria` con KPIs, runway, proyección 30d, riesgos e insights. `zetaCollections` vacío hasta cablear cartera. Sin commit/push hasta cierre de scope.
- **ZETA-11 — Tesorería manual Summer87 · Fases 1–3 entregadas en repo**. SQL `zeta-11-00` … `zeta-11-05` aplicado en Supabase; APIs `app/api/copilot/treasury/*`; UI `/copilot/tesoreria` consumiendo solo treasury APIs. Pendiente operativo: datos reales de egresos en Zeta (ZETA-10 pausado) y ampliaciones opcionales (import extracto bancario masivo, cuentas desde UI). No commit/push; `stash@{0}` intacto.
- **ZETA-10 — Audit discovery tesorería/egresos · PAUSADO 2026-05-12 (Opción D)**. Audit técnico de descubrimiento de la operación real de egresos/caja/bancos/CxP en Zeta para `EmpresaCodigo=250218923`. Read-only end-to-end: no migraciones, no pipelines, no UI, no commit, `stash@{0}` intacto.
  - **Discovery colección 10-2025**: 262 endpoints totales → 57 matches → 9 operacionales (`RESTFacturaProveedorV1QuerySaldosPendientes`, `RESTCuotasV1QueryProveedor`, `RESTRecibosPagosV1QueryPendientes`, `RESTFacturaProveedorV1QueryCompras`, `RESTFacturaProveedorV1Compras`, `RESTFacturaProveedorV1ComprasDetalladas`, `RESTMovimientosCajaV1Query`, `RESTMovimientosBancariosV1Query`, `RESTAsientoV1Lista`) + ~10 catálogos (`RESTBancosV1Query`, `RESTCajasV1Query`, `RESTCuentasV1Query`, `RESTProveedorV2Query`, `RESTFormasPagoV1Query`, `RESTCondicionesPagoV1Query`, `RESTRetencionesPercepcionesV1Query`, `RESTTiposAsientosV1Query`, `RESTCategoriasProveedoresV1Query`, `RESTBandejaEntradaAsientosV1Query`). No existen en la colección 10-2025 endpoints REST para "Libro de Ingresos y Egresos", "Resumen de Documentos de Pago", "Totales por Concepto" ni "Flujo de Efectivo" como métodos dedicados — esas vistas de UI se construyen sobre los 9 operacionales.
  - **Live probe (todos HTTP 200, envelopes correctos)**: `RESTFacturaProveedorV1QuerySaldosPendientes` 0 filas / `RESTCuotasV1QueryProveedor` 0 / `RESTRecibosPagosV1QueryPendientes` 0 / `RESTFacturaProveedorV1QueryCompras` 0 (abr-2026 y dic-2025) / `RESTFacturaProveedorV1Compras` 0 (mismas ventanas) / `RESTFacturaProveedorV1ComprasDetalladas` 0 (mismas ventanas) / `RESTMovimientosCajaV1Query` 0 (ene/feb/mar/abr 2026 y dic-2025) / `RESTAsientoV1Lista` 0 (ejercicio 2026 y dic-2025). **Único endpoint con datos: `RESTMovimientosBancariosV1Query` con 168 filas ene–abr 2026 ($2.673.088,60 agregado)**.
  - **Verificación específica de débitos (egresos bancarios)**: barrido por `ComprobanteTipo` 51/52/53/54/55 + barrido sin filtro mensual con `ComprobanteTipo=52` para todo 2026. **0 débitos bancarios en cualquier configuración**. Los 168 movimientos son 100% `ComprobanteTipo=51` "Crédito de Cuenta Bancaria" (ingreso), 100% Banco Santander, 98 en cta UYU + 70 en cta USD, `Descripcion="Recibo de Cobro"` predominante, `ConceptoCodigo`/`ConceptoNombre`/`ClienteProveedor`/`ProveedorNombre` 100% vacíos, `Conciliado="N"`. Son el espejo bancario de los Recibos de Cobro ya cubiertos por ZETA-08.
  - **Conclusión técnica**: no hay endpoint Zeta que represente egresos reales para este tenant. Implementar Tesorería hoy sincronizaría 0 filas de egreso (escenario idéntico a ZETA-09-01).
  - **Decisión**: Opción D — pausar implementación y devolver descubrimiento al stakeholder. NO se crearon tablas, NO se crearon pipelines, NO se tocó UI, NO commit. `stash@{0}` (ZETA-08-live bucket B) intacto. Artefactos read-only en `temp-audits/`: scripts `zeta-10-enumerate-endpoints.mjs`, `zeta-10-extract-bodies.mjs`, `zeta-10-audit-tesoreria.mjs`, `zeta-10-audit-deep-bancos.mjs`, `zeta-10-audit-debits.mjs`; outputs JSON en `temp-audits/output/zeta-10-audit-tesoreria-*.json`, `zeta-10-audit-deep-bancos-*.json`, `zeta-10-audit-debits-*.json`.
  - **Pendiente de negocio (bloqueante para reanudar)**: confirmar con cliente/contador dónde se registran efectivamente: (1) gastos operativos, (2) pagos a proveedores, (3) débitos bancarios / transferencias de salida, (4) impuestos (DGI / IRAE / IVA / BPS), (5) sueldos y cargas sociales, (6) transferencias internas entre cuentas, (7) egresos por caja chica / efectivo, (8) cualquier otro flujo monetario de salida. Escenarios posibles: (a) registran en Zeta pero el rol API no los ve → revisar `RolCodigo` y pedir permiso sobre `RESTMovimientosBancariosV1Query` con `ComprobanteTipo=52` y/o `RESTRecibosPagosV1*`; (b) registran en Zeta UI pero la API no los expone → pedir a soporte Zeta endpoint dedicado; (c) registran en otro sistema (Excel / banco directo / otro ERP / SIIGO / etc.) → diseñar ingestión alternativa fuera del paraguas Zeta; (d) no se registran en ninguna parte → módulo "Egresos" del Copilot necesita captura nativa propia.
  - **Recomendación arquitectónica para cuando se reanude (post-confirmación)**: NO mapear a `proto_payments` (mezcla semánticas con CxP / recibos de pago a proveedores). Crear `proto_bank_movements` y `proto_cash_movements` independientes, ambas con `workspace_company_id`, `company_id NULL`, `source='zeta'`, `zeta_metadata jsonb`, `direction='credit'|'debit'` derivado de `ComprobanteTipo`, `account_external_id` (CuentaCodigo), `external_registro_id` (RegistroId), `concepto_code/nombre`, `is_conciliated` (Conciliado='S/N'). Mantener `proto_payments` como hoy. Si se requiere "Flujo de Caja Realizado" unificado, crear vista SQL `proto_treasury_flow_v1` con UNION ALL de receipts + bank/cash movements + payments liquidados.
- **ZETA-09-01 — Sync Vendor Payments Zeta CIERRE TÉCNICO 2026-05-12**. Implementación completa del pipeline `RESTRecibosPagosV1QueryComprobantes → proto_payments` siguiendo 1:1 el patrón de Recibos. Entregables: migración SQL idempotente (`supabase/zeta-09-01-proto-payments-zeta-sync.sql` — aplicada en Supabase via SQL Editor, verificada post-aplicación: `company_id` nullable, `currency_code`/`source default 'manual'`/`zeta_metadata`, 2 CHECK constraints, 3 índices nuevos, backfill no-op por tabla vacía); pipeline (`lib/integrations/zeta/zeta-vendor-payments-{rest-method,contract,fetch,mapper,pipeline}.ts` + endpoint `app/api/zeta/sync-vendor-payments/route.ts`); UI enriquecida (`app/copilot/datos/datos-client.tsx` columnas nuevas `vendor_name` / `currency_code` / `caja_nombre` + `enrichPaymentRows` desde `zeta_metadata`); types/service extendidos (`ProtoPaymentInput.company_id: string|null`, `currency_code`/`source`/`zeta_metadata`, `PROTO_PAYMENT_STATUSES` agrega `"void"`, `protoCreate/UpdatePayment(options.allowUnlinkedCompany)` + `validatePaymentIntegrity` condicional). Reglas de mapeo: `payment_number="ZETA:PAG:${RegistroId}"`, `source='zeta'`, `company_id=null`, status `paid|void` desde `Emitido`, `currency_code` USD/UYU normalizada, `category=ComprobanteNombre`, `vendor_name=ProveedorRazonSocial||ProveedorNombre`, `reference=Serie+Numero`, `notes.zeta_vendor_payment_v1.raw_payload` completo, `obligation_id=null`. Upsert idempotente por `(workspace_company_id, payment_number)`. Validaciones: ignora sin `RegistroId`, sin fecha, pre-`COPILOT_OPERATIONAL_START_DATE`, montos negativos/inválidos. **Decisiones arquitectónicas**: `company_id` nullable para pagos Zeta (no se crea `proto_vendors` en este pase, replica Recibos unlinked), `allowUnlinkedCompany` como opt-in centralizado en CRUD service (mantiene integridad de pagos manuales), sin impacto en tesorería/bancos/movimientos de caja, sin impacto en reconciliación/cartera/facturas/recibos/saldos/cuotas/dashboards/escenarios/insights. **Smoke estructural ejecutado vía `temp-audits/smoke-sync-vendor-payments.mts <mes> <anio>`** (importa el mismo `syncZetaVendorPayments` que usa el endpoint con `SUPABASE_SERVICE_ROLE_KEY` + `WORKSPACE_COMPANY_ID` del `.env.local`, equivalente al POST sin pasar por auth Next): 3 ventanas probadas (`5/2026`, `4/2026`, `12/2025`) → todas HTTP 200 con `QueryComprobantesOut.Response[]` vacío (`payload_size_bytes:73`). 3 `zeta_sync_runs` registrados (1× bootstrap + 2× incremental), todos `succeeded`. `zeta_sync_state.zeta_vendor_payments_v1` con `bootstrap_completed=true`, watermark `{"mes":"12","anio":"2025"}`. **Revisión UI**: `GET /copilot/datos` HTTP 200 ~48.9 KB; `GET /api/copilot/dataset` HTTP 200 con `payments=[]` sano; lazy-load del acordeón confirmado (sin fetch hasta expandir); empty state OK; las 3 columnas nuevas declaradas y `listProtoPayments` usa `select("*")` (las columnas viajarán cuando existan filas). **Validaciones**: `npx tsc --noEmit` exit 0; `npx vitest run` **836/836** verdes (incluye 3 nuevos suites contract/fetch/mapper); `npx eslint` sobre los 13 archivos tocados → 0 errores nuevos. **Limitación conocida**: empresa actual (`EmpresaCodigo=250218923`) devuelve `Response[]` para las 3 ventanas probadas — persistencia real pendiente hasta que Zeta exponga ≥1 recibo de pago a proveedor para este tenant. Mapper + persistencia cubiertos por unit tests; cuando lleguen filas, las reglas (`ZETA:PAG:*`, `source='zeta'`, `company_id=null`, `zeta_metadata`, `notes.raw_payload`) aplicarán por contrato testeado. Commit `e716097` creado (NO push). `stash@{0}` (ZETA-08-live bucket B) intacto. **Próximos pasos**: re-correr smoke contra ventanas con actividad real cuando se identifique mes con pagos a proveedores Zeta; si tras varias ventanas sigue vacío, evaluar `RolCodigo`/permisos a `RESTRecibosPagos*`, variante V2 del endpoint, o si la empresa efectivamente usa el módulo Zeta "Recibos de Pago". Sin cron por ahora; sync solo manual vía `POST /api/zeta/sync-vendor-payments`.
- **ZETA-08 — Pipeline Cuotas end-to-end ENTREGADO 2026-05-12**. 9 fases completas: tabla `proto_invoice_installments` (`supabase/zeta-08-01-…sql`) + `proto_invoices.due_date_source` (`supabase/zeta-08-02-…sql`); linker batch (`lib/integrations/zeta/zeta-installments-link.ts`); pipeline orquestador (`lib/integrations/zeta/zeta-installments-pipeline.ts`) con linkage idempotente, upsert `onConflict='workspace_company_id,zeta_registro_id,cuota_numero'` y update de `proto_invoices.due_date` con `due_date_source='zeta_cuotas_v1'` usando `min(cuota_vencimiento de cuotas con saldo>0)`; cron 6h (`app/api/cron/zeta-sync-cuotas/route.ts` + `vercel.json`); backfill manual (`app/api/zeta/sync-installments-backfill/route.ts`) + script CLI (`scripts/zeta-backfill-installments.mjs`); mappers (voucher + saldos pipeline) marcan `due_date_source='synthetic_30d'` al crear y el saldos pipeline NO pisa `due_date` real (guard `buildSaldosDueDatePatch`); motor `lib/copilot-financial-reconciliation.ts` con aging real (semántica days-past-due) + fallback sintético + `agingSource` per-currency (`real`|`synthetic`|`mixed`|`none`) + `AgingBucket.realDueDateCount`/`syntheticDueDateCount`; 19 tests nuevos (7 pipeline, 12 aging real) sobre 808/808 totales. `tsc --noEmit` exit 0; `eslint` 0 errores nuevos. Docs sincronizados: DIV-CONT-001 → MITIGADO POR ZETA-08, DIV-CONT-006 → IMPLEMENTADO. **Validación live pendiente** (próximo turno): correr migraciones en Supabase, correr backfill via `scripts/zeta-backfill-installments.mjs` para workspace Summer87, comparar con PDFs Zeta "Comprobantes Pendientes" + "Vencimiento de Cuotas" para Acquagarden, El País y al menos un cliente con cuotas vencidas.
- **Aplicar migraciones zeta-08-* en Supabase**: ejecutar `supabase/zeta-08-01-proto-invoice-installments.sql` y `supabase/zeta-08-02-proto-invoices-due-date-source.sql` en orden. Ambas son idempotentes (no fallan si la columna/tabla ya existe). Backfill de `due_date_source='synthetic_30d'` se aplica automáticamente en la 08-02 sobre filas existentes con `due_date` no nulo.
- **Validar pipeline cuotas live** (post-migración):
  - `BASE_URL=… CRON_SECRET=… WORKSPACE_COMPANY_ID=… node scripts/zeta-backfill-installments.mjs --clientes 2 --dry-run` (Acquagarden, listado de elegibles).
  - `… --clientes 2,17` (Acquagarden + El País, processing real). Esperado: cuotas con vencimiento real coincidiendo con PDF, `due_date` migrado a `zeta_cuotas_v1`.
  - Verificar `/copilot/cartera` muestra `agingSource: 'mixed'` mientras el cron no haya recorrido a todos los clientes; debería pasar a `'real'` luego del primer ciclo completo.
- **Correr 4 audit scripts entregados en turnos anteriores** (algunos ya corridos):
  - ✅ `audit-zeta-receipt-data-shape.mjs` — corrido. Confirma aplicaciones recibo↔factura NO expuestas en `Data`. **2026-05-11 tarde:** re-validado con `audit-zeta-2025-reprobe.mjs` que `Load` (RegistroId=1911 real) tampoco las expone. DIV-CONT-005.
  - ✅ `audit-zeta-due-date-shape.mjs` — corrido (con bug de ruta), reemplazado por `audit-zeta-cuotas-path-reprobe.mjs` que probó AMBAS variantes y confirmó HTTP 200 con `/rest/APIs/`.
  - ⏳ `audit-zeta-account-statement-endpoints.mjs` — pendiente correr live para evaluar POC `RESTAsientoV1Lista`.
  - ⏳ `audit-zeta-exchange-differences-shape.mjs` — pendiente correr live para auditar `Cotizacion` en factura/recibo.
- **Validar caso El País con NC ahora detectada (Fase B entregada)**. Correr `node --env-file=.env.local temp-audits/audit-zeta-financial-coverage.mjs` para confirmar conteo de NCs y comparar el saldo del cliente El País contra el Estado de Cuenta Zeta. Esperado: con `is_credit_note=true` activo en motor + route, saldo coincide con 67.222 UYU (antes 75.884). Si no coincide, capturar `zeta_metadata.zeta_customer_voucher_v1` de A391 y validar el `cfe_tipo`.
- **Validar `/copilot/cartera` contra PDFs Zeta (Fase A entregada)**. Correr los 4 scripts de auditoría con un período conocido y comparar contra los PDFs oficiales:
  - `node --env-file=.env.local temp-audits/audit-zeta-comprobantes-pendientes.mjs --cutoff 2026-05-11`
  - `node --env-file=.env.local temp-audits/audit-zeta-analisis-saldos.mjs --cutoff 2026-05-11`
  - `node --env-file=.env.local temp-audits/audit-zeta-vencimiento-cuotas.mjs --cutoff 2026-05-11`
  - `node --env-file=.env.local temp-audits/audit-zeta-estado-cuenta.mjs --from 2026-05-01 --to 2026-05-11 --client "ACQUAGARDEN"`
  - `… --client "El País"` para validar caso UYU con NC ahora contabilizada.
  Esperado: cliente, comprobante, total, saldo coinciden con Zeta. Divergencias remanentes esperadas y aceptadas: vencimiento real ≠ +30d (DIV-CONT-001), recibos posteriores al `Hasta` que cambian `balance_amount` (DIV-CONT-003), opening balance dependiente de cobertura de recibos pre-período (DIV-CONT-004).
- **Endpoints Zeta certificados — 2026-05-11 (revalidado con colección 10-2025 en la tarde)**. Discovery exhaustivo en los 262 endpoints REST de `docs/zeta/reference/ZetaSoftware-REST-10-2025.json` (variable oficial `baseUrl = "https://api.zetasoftware.com/rest"`) + re-prueba live de los críticos:
  - ✅ `RESTCuotasV1QueryCliente` → DUE_DATE REAL DISPONIBLE. **Validado live con `audit-zeta-cuotas-path-reprobe.mjs` (Acquagarden Codigo=2): HTTP 200, `RegistroId=2527, CuotaVencimiento="2026-04-10", CuotaSaldo="368.26"` que coincide 1:1 con PDF "Comprobantes Pendientes" A2874**. DIV-CONT-009 RESUELTO (era bug del audit script anterior, no bloqueo Zeta). Backend (contract+fetch+mapper+60 tests) listo, pendiente decisión del usuario sobre pipeline.
  - ✅ `RESTFacturaClienteV4QuerySaldosPendientes` → canónico, ya en uso. **Validado live**: HTTP 200, 25 sample keys incluyendo `CondicionCodigo`/`CondicionNombre` (info no aprovechada).
  - ✅ `RESTRecibosCobranzaV2QueryPendientes` (nuevo en catálogo 2025) → endpoint funcional. **Validado live**: HTTP 200. Lista recibos con saldo a favor del cliente. Listo para futuro módulo "Anticipos / Saldos a favor".
  - ✅ `RESTCondicionesPagoV1Query` → catálogo disponible (no urgente; Cuotas ya da vencimiento directo).
  - ✅ `RESTMonedasCotizacionesV1Query` → cotizaciones por fecha (habilita FX futuro).
  - ❌ Aplicaciones recibo↔factura → **bloqueado por Zeta** (DIV-CONT-005). **Re-confirmado 2026-05-11 tarde**: `RESTRecibosCobranzaV2Load` con RegistroId=1911 real solo devuelve cabecera (15 campos, sin arrays de aplicaciones). Pedir a soporte Zeta endpoint dedicado.
  - ❌ `RESTQuerySaldoPendienteCliente` (alias del PDF) → NO existe como endpoint REST. **Validado live**: HTTP 404 con JSON `{"error":{"code":404,"message":"Not Found"}}`. Es referencia conceptual del PDF, el canónico es `RESTFacturaClienteV4QuerySaldosPendientes`. DIV-CONT-010.
  - ❌ Estado de cuenta oficial → **bloqueado por Zeta** (DIV-CONT-007). Alternativa: POC con `RESTAsientoV1Lista` + `RESTPlanCuentasV2Query`.
  - ❌ Saldos a fecha histórica → no acepta `FechaCorte`. Mitigar con `proto_invoice_balance_snapshots`.
  - 📋 Doc completa en `docs/zeta-missing-financial-endpoints.md` (rev. 2026-05-11 tarde) + `docs/vendors/z/KNOWN-DIVERGENCES.md` (DIV-CONT-001 a 010).
- **Fix data workspace secundario (`5e4de8f3`)**: contacts devuelve 195 filas Zeta sin match en `proto_companies`. Verificar que los `Codigo` Zeta del segundo workspace estén cargados en `proto_companies.Codigo`.
- **Investigar gap voucher pipeline (A-2919 USD 2026-05-04)**: factura mencionada por el negocio con saldo U$S 610 NO existe en `proto_invoices`. Probable falta de ingesta en customer-vouchers pipeline (no es bug de saldos — ese ya quedó cubierto). Confirmar contra `RESTFacturaClienteV4VentasDetalladas` Serie=A Numero=2919 (cliente con Codigo a determinar). Si Zeta la expone, re-correr vouchers para mayo 2026.

## Later — Cartera contable Fase B/C (cuando aplique)
- **Fase B (NCs) — ENTREGADA 2026-05-11**. Motor + route activos con detector desde `zeta_metadata.zeta_customer_voucher_v1.cfe_tipo` (catálogo DGI). Falta: validar caso El País end-to-end; ampliar `lib/copilot-client-account-statement.ts` con renglón `kind: "credit_note"` separado (hoy ya se contabiliza pero se muestra como factura); UI explícita para NC en explorador y aging.
- **Fase C — Vencimiento real (ENTREGADA 2026-05-12 · ZETA-08)**. Tabla `proto_invoice_installments` + pipeline + cron 6h + backfill + due_date_source + aging real + tests + docs. DIV-CONT-001 mitigada (vigente solo hasta primer ciclo del cron por factura), DIV-CONT-006 IMPLEMENTADA. Solo falta correr migraciones + backfill + validación live contra PDFs (ítems en sección "Now").
- **Fase D — Vínculo factura↔recibo (BLOQUEADO Zeta)**: confirmado 2026-05-11 que **ningún endpoint REST oficial** (sobre 260 revisados) expone aplicaciones de recibo. Doc oficial 0165 confirma. Acción: pedir a Zeta soporte endpoint dedicado. Mientras tanto, mantener `proto_receipts.invoice_id = null`. Ver DIV-CONT-005.
- **Estado de Cuenta oficial (BLOQUEADO Zeta)**: 2026-05-11 confirmado que no existe `RESTEstadoCuenta*`. Evaluar futuro POC con `RESTAsientoV1Lista` + `RESTPlanCuentasV2Query` mapeando cliente → cuenta contable. Ver DIV-CONT-007.
- **Snapshot histórico de `balance_amount`**: tabla `proto_invoice_balance_snapshots(invoice_id, snapshot_date, balance_amount)` diaria. Permite cartera fiel a cualquier corte histórico. Ver DIV-CONT-003.
- **Diferencias de cambio (módulo aparte, bloqueado por Fase D)**: cálculo derivable usando `RESTMonedasCotizacionesV1Query` + `Cotizacion` en `RESTRecibosCobranzaV2Load` + `RESTFacturaClienteV4VentaDetallada`. Audit script ya entregado (`audit-zeta-exchange-differences-shape.mjs`). **Bloqueado por DIV-CONT-005** porque el cálculo factura-por-factura requiere aplicaciones. Hasta resolver eso, FX agregado por cliente/período no es matemáticamente equivalente al de Zeta. Ver DIV-CONT-008.

## Next — Pipeline Health Dashboard UI

**Objetivo:** visualizar estado de los 3 pipelines Zeta automatizados en producción.

**Fuentes de datos (ya disponibles, no tocar):**
- `zeta_pipeline_runs` — tabla Supabase con una fila por corrida
- `lib/data/zeta-pipeline-health.ts` — `getAllPipelineHealth()` + `derivePipelineHealth()` (función pura)
- `lib/data/zeta-pipeline-run-repository.ts` — `getLatestRunPerPipeline()`, `getRecentPipelineRuns()`

**Mostrar por pipeline:**
- Estado: `healthy` / `degraded` / `stalled` / `unknown` (con color/icono)
- Última corrida: timestamp relativo + duración (`duration_ms`)
- Métricas: `rows_processed`, `rows_updated`, `rows_failed`
- `consecutive_failures` y `error_summary` cuando `status ≠ succeeded`
- `is_overdue` si superó el intervalo esperado (3h/6h/24h)

**Restricciones:**
- No modificar pipelines, cron routes, reconciliación ni `zeta_pipeline_runs` schema
- Solo lectura: API route GET que llame `getAllPipelineHealth()` + componente UI

## Later
- **UI Recibos refinements** (no bloqueante; pipeline ya funcional):
  - Ordenar por `receipt_date` DESC en la grilla (hoy ordena por `created_at` DESC, con `receipt_date` como tie-breaker).
  - Mostrar `currency_code` (USD / UYU) junto al importe, alineado al patrón de la columna de facturas.
  - Mostrar nombre del cliente: lookup `company_id → proto_companies.name` o, cuando `company_id IS NULL`, fallback a `notes → zeta_collection_receipt_v1.raw_payload.ClienteNombre`.
- **Linking factura↔recibo** (opcional): hoy los recibos se persisten sin `invoice_id`. Si se decide vincular, definir heurística confiable o reglas explícitas (ej.: match por `Serie+Numero`) antes de tocar `proto_receipts.invoice_id` y los balances.
- **Automatizar sync mensual de recibos**: cron diario/mensual que invoque `POST /api/zeta/sync-collection-receipts` con el mes corriente y el anterior, con retención de logs.
- Evaluar cierre de período en facturas: una vez resuelto re-sync abril, revisar si conviene ampliar el alcance reconciliador a mayo–junio.

## Done
- 2026-05-11 — **Cards `/copilot/cartera` · normalizador resiliente de `report.currencies` (helper testeable)**. Síntoma reportado tras restart: cards muestran `Pendiente UYU $0 / Emitido UYU $0 / Cobranza efectiva 0%` PERO el microcopy de la card de efectividad mostraba `Cobrado $235.326,38 / Emitido` — prueba de que `totalCollected` SÍ llegaba mientras `totalInvoiced/totalPending` no eran leídos consistentemente por el frontend. Auditoría server-side (log `report_debug` rev. 2026-05-12T00:21:25Z) confirmó que el backend envía valores correctos: USD `{invoiced 8639.8, pending 5824.22, collected 2815.58, effectiveness 0.3259}` y UYU `{invoiced 659992.38, pending 424666, collected 235326.38, effectiveness 0.3566}`. Adicionalmente se observó en server logs un error CRÍTICO previo de HMR (`Rendered more hooks than during the previous render` en `use-financial-reconciliation.ts:174`, secuencia `1-7 useState · 8 useReducer · 9 useCallback · 10 useRef · 11 useEffect → useRef`) provocado por el commit anterior que agregó `lastParamsKeyRef` — Fast Refresh rompió el orden de hooks y dejó el state del componente desincronizado. El restart limpia esto, pero el shape parsing de las cards seguía siendo frágil. Fix definitivo (sin tocar sync/cron/DB/RLS/mappers/balance_amount/Aging/Explorer):
  - **Nuevo helper puro y testeable `lib/copilot-cartera-cards-source.ts`** (260 LOC). Exporta tres funciones:
    1. `readCurrencyMetric(record, ...keys)` — lee primer key resoluble a number finito, distinguiendo "campo presente con 0" de "campo ausente" (devuelve `null` solo en el segundo caso). Tolera `numeric` Postgres serializado como string, formato es-UY (`"659.992,38"`), rechaza NaN/Infinity. Es la base de todo lo demás.
    2. `buildCurrencyIndex(currencies: unknown)` — toma `report.currencies` y devuelve `Map<ReconciliationCurrencyCode, NormalizedCurrencyMetrics>` resistente a: **array vs objeto indexado** (`{USD:{...}, UYU:{...}}`), **camelCase vs snake_case** (`totalInvoiced` ↔ `total_invoiced`, `collectionEffectiveness` ↔ `collection_effectiveness`, etc.), **numéricos number vs string** (`8639.8` o `"8639.80"`), **claves objeto en lowercase** (`{usd:{...}}`), **códigos inválidos** (ignorados), **items malformados** (null/primitivo → ignorados). Deriva `totalCollected` desde `max(0, invoiced − pending)` y `collectionEffectiveness` desde `clamp(collected/invoiced, 0, 1)` cuando faltan, SIEMPRE del mismo bucket (nunca cross-source).
    3. `findCurrencyMetrics(report, code)` — atajo para componentes.
  - **30 tests** en `lib/copilot-cartera-cards-source.test.ts` cubriendo cada variabilidad real: `readCurrencyMetric` (number directo / cae a snake / parsea string JS / parsea string es-UY / distingue 0 de null / rechaza NaN / handle null record), array shape (camel / snake / strings / inválidos / malformados), object shape (`{USD:{}}` / lowercase / snake interno), derivaciones (collected ausente / clamp ≥0 / effectiveness ausente / null cuando invoiced=0 / clamp [0..1] / counts trunc), inputs degenerados (null / primitivo / vacío), y un **test de regresión que replica literal el shape del log `report_debug` 2026-05-11 21:21:25Z** (USD/UYU con valores exactos del audit DB).
  - **`components/copilot/executive-summary-cards.tsx`** refactorizado para consumir el helper:
    - Eliminado `readPath/readNumberField/readCurrencyCode/readEffectivenessField/findCurrency` (~110 LOC reemplazadas por una llamada al helper).
    - `getCurrencyCardMetrics(index, report, code)` ahora recibe el `Map` ya construido (no recalcula).
    - `pendingCollectionCard/issuedCard/effectivenessCard` reciben el index como parámetro.
    - Nuevo `useMemo` en el componente principal construye `currencyIndex` una sola vez por render.
    - Microcopy de efectividad ahora muestra TAMBIÉN el emitido: `Cobrado X / Emitido Y` (antes solo "/ emitido" literal).
    - Aging/staleClients siguen siendo red de seguridad última (con warning dev si aging tiene deuda pero el índice no expone la moneda).
    - Log temporal `[cards currencies runtime]` (solo dev, solo browser) que imprime `currenciesType`, `currenciesLength`, `currenciesKeys`, `firstBucketKeys`, `normalizedUSD`, `normalizedUYU` — permite confirmar shape en 1 vistazo desde DevTools sin filtrar payload completo. Marcado `TEMP[2026-05-11]` para futura remoción.
  - Validación: `npx tsc --noEmit` exit 0 · `npx vitest run` **683/683** (cards source 30/30 nuevos, motor 96/96, cartera otros 33/33, total +30) · `npx eslint` 0 issues en archivos tocados.
- 2026-05-11 — **Fuente única financiera en motor + cards lectoras puras (`/copilot/cartera`)**. Auditoría + refactor que consolida la verdad financiera en `generateFinancialConsistencyReport()` y deja a las cards como lectoras. Cero cambios en sync/cron/DB/RLS/mappers/balance_amount/fórmulas de deuda actual cliente.
  - **Audit Supabase (read-only)**: `temp-audits/audit-cartera-period-may-2026.mjs`. Confirma para período 2026-05-01→11 en workspace `040321ff…`: 58 facturas (USD 23, UYU 35), shape limpio (0 strings/nulls), 0 inactivas, 0 `issue_date` inválidos. Valores reales: **USD** invoiced $8.639,80 / pending $5.824,22 / collected $2.815,58 / eff 32,6% (14 pendientes de 23); **UYU** invoiced $659.992,38 / pending $424.666,00 / collected $235.326,50 / eff 35,7% (23 pendientes de 35). Coincide con lo que el usuario veía en aging + cobranza efectiva, lo que probó que el motor calculaba bien `totalInvoiced` pero las cards leían inconsistentemente.
  - **Motor (`lib/copilot-financial-reconciliation.ts`)**: `CurrencyReconciliation` ahora incluye **`totalCollected`** (`round2(max(0, totalInvoiced − totalPending))`) y **`collectionEffectiveness`** (`clamp(collected/invoiced, 0, 1)` o `null` si `invoiced ≤ 0`) calculados en el mismo pase. Estos campos pasan a ser parte del contrato del reporte: cards, dashboards y futuros consumidores leen el ratio del motor en lugar de rederivar. JSDoc explícito en cada campo (incl. advertencia "aging no puede ser fuente de emitido porque excluye facturas cobradas 100%").
  - **Route (`app/api/copilot/financial-reconciliation/route.ts`)**: log estructurado nuevo `kind: "report_debug"` con `from/to`, `invoices_loaded`, `currencies[]` (code, invoiced, pending, collected, effectiveness, counts), `aging_keys`, `aging_sums.{UYU,USD}` y `stale_client_count`. Sin secretos. Permite cruzar la respuesta del reporte contra los queries de Supabase directamente desde server logs.
  - **Cards (`components/copilot/executive-summary-cards.tsx`)**: `getCurrencyCardMetrics` simplificado a leer DIRECTO del bucket `report.currencies[code]` cuando existe (fuente primaria sin recálculo). Aging + staleClients quedan como **fallback defensivo SOLO** si el bucket no aparece (red de seguridad ante regresiones futuras del motor). El fallback marca `usedFallback: true` y, en `NODE_ENV !== "production"`, emite `console.warn` con instrucción de revisar el motor cuando aging muestra deuda pero `currencies` no expone la moneda. `findCurrency` ahora también acepta `totalCollected`/`collectionEffectiveness` con keys alternativos (camel/snake/`totals.*`) y deriva los faltantes desde el MISMO bucket (nunca cross-source). Card "Cobranza efectiva" gana microcopy `Cobrado X / emitido` (lee `collectedAmount` del motor, sin rederivar).
  - **Tests motor (6 nuevos en `lib/copilot-financial-reconciliation.test.ts`, suite "currencies output: paid + partial + pending invoices")**:
    1. `period report includes paid invoices in totalInvoiced` — 3 facturas USD (100 paid + 200 pending + 300 partial) → invoiced 600, pending 300, collected 300, eff 0.5, aging sum 300 (excluye paga).
    2. `aging does not define totalInvoiced` — 2 UYU cobradas + 1 pendiente → invoiced > aging sum, aging refleja sólo pendiente.
    3. `multi-currency: USD y UYU se calculan en buckets independientes` — verifica buckets no se contaminan, effectiveness por moneda.
    4. `collectionEffectiveness es null cuando totalInvoiced=0` — no inventa 0% engañoso.
    5. `totalCollected nunca es negativo` — caso patológico balance > total → clamp >=0.
    6. `matches DB audit values for mayo 2026` — réplica del subset USD del audit (23 facturas) → estructura coherente (`invoiced > pending`, `collected = invoiced − pending`, `pendingInvoiceCount < invoiceCount`, `0 < eff < 1`).
  - Validación: `npx tsc --noEmit` exit 0; `npx vitest run lib/copilot-financial-reconciliation.test.ts` 96/96 (90 previos + 6 nuevos); `npx vitest run` 653/653; `npx eslint app/api/copilot/financial-reconciliation/route.ts lib/copilot-financial-reconciliation.ts components/copilot/executive-summary-cards.tsx lib/copilot-financial-reconciliation.test.ts` → 0 errores, 1 warning preexistente (`getCopilotOperationalEndDate` no usado, fuera de scope).
- 2026-05-11 — **Fix rangos independientes `/copilot/cartera` · cards consistentes desde la primera carga**. Bug: cargar directamente 01/05/2026 → 11/05/2026 mostraba counts correctos pero montos mal calculados; al cargar antes 01/01/2026 → 11/05/2026 y volver, los montos quedaban correctos. Causa raíz combinada (varias capas, todas resueltas sin tocar sync/DB/cron/balance_amount/fórmulas core):
  1. **`components/copilot/executive-summary-cards.tsx · getCurrencyCardMetrics`**: el helper priorizaba `agingByCurrency[code]` por sobre `report.currencies[code]` para derivar pending. `agingByCurrency` solo agrega facturas con `pendingAmount > 0` (excluye facturas cobradas 100%), mientras `currencies.totalInvoiced` agrega TODAS las facturas del período. Cuando el primer fetch traía un rango con cobros parciales/totales (típico mayo), aging.totalPending < currencies.totalPending y eso disparaba `emittedAmount = pendingAmount`, subestimando "Emitido" frente a "Pendiente". Fix: invertir prioridad → `currencies` es fuente primaria, `aging`/`staleClients` son fallback defensivo SOLO si currencies viene vacío.
  2. **`hooks/use-financial-reconciliation.ts`**: el reducer mantenía `report` previo en `"request"` para evitar flicker en refetch, pero al confirmar un rango NUEVO el usuario veía el reporte anterior durante el fetch. Si el rango anterior era amplio y el nuevo corto, había un instante donde "counts del nuevo rango + montos del rango viejo" se mezclaban — visualmente indistinguible de un bug de cálculo. Fix: nuevo action `params_changed` en el reducer + `lastParamsKeyRef` que detecta cambio de `mode|periodStart|periodEnd` y limpia el reporte previo antes del nuevo fetch (mantiene el comportamiento "no flicker" para refetch puro del mismo rango).
  3. **`app/api/copilot/financial-reconciliation/route.ts`**: ausencia de `dynamic = "force-dynamic"` + `revalidate = 0` permitía al Data Cache de Next servir respuestas stale entre rangos. Se agrega ambos. Validación con regex `^\d{4}-\d{2}-\d{2}$` para `period_start`/`period_end`: strings basura ya no caen a default operativo silenciosamente — se devuelve `400 INVALID_PERIOD`. Validación adicional `period_start > period_end → 400 INVALID_PERIOD_RANGE`. Query Supabase agrega `.order("id", { ascending: true })` para que ante eventual `LIMIT 5000` el subset sea reproducible y no varíe entre requests. Log estructurado nuevo `kind: "range_received"` con `from`, `to`, `mode`, `raw_from`, `raw_to`, `workspace_id` (sin secretos) para diagnóstico de parámetros recibidos.
  4. **`lib/copilot-financial-reconciliation.test.ts`**: 4 tests de regresión "independent period ranges on same dataset" con dataset realista (enero–mayo, mezcla UYU/USD, cobros parciales + totales). Verifica que: (a) rango corto 01/05→11/05 incluye solo mayo con totales correctos y aging consistente; (b) rango amplio 01/01→11/05 incluye todas las facturas; (c) dos corridas del mismo rango en cualquier orden dan resultados idénticos (motor puro, sin memoria entre invocaciones); (d) rango sin facturas devuelve totales en cero sin throw.
  - Validación: `npx tsc --noEmit` exit 0; `npx vitest run` 647/647 (motor: 90/90 con 4 nuevos); `npx eslint app/copilot/cartera/page.tsx components/copilot/cartera-shell.tsx components/copilot/executive-summary-cards.tsx app/api/copilot/financial-reconciliation/route.ts hooks/use-financial-reconciliation.ts lib/copilot-financial-reconciliation.test.ts` sin issues.
- 2026-05-11 — **Indicador auto-sync Zeta en barra de cartera (`/copilot/cartera`)**. UI-only, cero cambios en cron, sync, DB, balance_amount, motor financiero ni fórmulas. Objetivo: explicar al usuario que los datos se actualizan automáticamente cada 3 h (cron Vercel `0 */3 * * *` → `/api/cron/zeta-sync-saldos`) y diferenciar eso del botón manual "Refrescar". Nuevos archivos: `lib/copilot-auto-sync.ts` con tres helpers puros — `ZETA_SALDOS_SYNC_INTERVAL_HOURS=3`, `pickZetaSaldosSyncState(syncStates)` (busca substring "saldo" case-insensitive en `resource_flow`, fallback al `pickMostRecentSync` existente, soporta tanto el flujo legacy `saldos_pendientes` como el actual `factura_cliente_saldos_pendientes`), `computeNextZetaSyncAt(now, intervalHours=3)` (calcula próximo múltiplo entero UTC ≥ `now` con semántica "estrictamente futura": si `now` cae exactamente en el múltiplo devuelve el siguiente bloque), `formatHHmm(date)` (formato HH:mm 24h es-UY con fallback `—` para fechas inválidas). 16 tests nuevos en `lib/copilot-auto-sync.test.ts`. Componente `<AutoSyncIndicator />` agregado a `components/copilot/financial-control-bar.tsx` (oculto en mobile <sm para no apretar la fila, visible y alineado a la derecha junto al botón Refrescar a partir de sm): muestra tres líneas microcopy — "Auto-sync Zeta · cada 3 h" con icono `Clock3`, "Última sync: hace Xh" (de `formatRelativeAgeHours` aplicado al `ageHours` del sync state de saldos) o "Última sync: no disponible" cuando no hay timestamp, "Próxima aprox.: HH:mm" en hora local del usuario. El componente no usa `useState`/`useEffect`: instancia `new Date()` en render; cada fetch del reporte refresca el componente y por ende recalcula la "próxima". `FinancialControlBar` recibe nueva prop opcional `syncStates?: readonly SyncStateSummary[]`; si está ausente/vacía el indicador cae al copy estático. `cartera-shell.tsx` pasa `report?.syncStates` (sigue siendo `undefined` mientras `!hasConfirmedRange` o durante el primer loading → fallback estático mostrado, que es el comportamiento esperado). Tooltip del botón Refrescar refuerza que es manual y no reemplaza al cron. Validación: `npx tsc --noEmit` exit 0; `npx vitest run` 643/643 (627 previos + 16 nuevos); `npx eslint components/copilot/financial-control-bar.tsx components/copilot/cartera-shell.tsx lib/copilot-auto-sync.ts lib/copilot-auto-sync.test.ts` sin issues.
- 2026-05-11 — **Fix cards superiores `/copilot/cartera` · fuente canónica unificada con Aging/Explorer**. Síntoma: con rango 01/05/2026 → 11/05/2026 confirmado, Aging mostraba "UYU $ 424.666 · 23 facturas" y "USD U$S 5.824 · 14 facturas", Explorer mostraba clientes y saldos correctos, pero las cards superiores quedaban en `$ 0,00 / U$S 0,00` y `Cobranza efectiva 0%`. Las cards leían exclusivamente `report.currencies` vía `findCurrency()`, mientras que Aging consume `report.agingByCurrency[code]` y Explorer `report.staleClients[].pendingByCurrency[code]`. Si `report.currencies` venía delgado por cualquier shape mismatch, las cards quedaban a 0 aunque el resto de la página tuviera datos. Fix (solo `components/copilot/executive-summary-cards.tsx`, cero cambios en motor, sync, DB, cron, balance_amount, fórmulas, mappers, Aging ni Explorer): nuevo helper local `getCurrencyCardMetrics(report, code)` que prioriza las MISMAS fuentes que ya funcionan: `pendingAmount = Σ agingByCurrency[code].amount` (fallback a `staleClients.pendingByCurrency[code]`, fallback a `currencies.totalPending`); `pendingInvoiceCount = Σ agingByCurrency[code].invoiceCount` (fallback a `currencies.pendingInvoiceCount`); `emittedAmount` y `emittedInvoiceCount` desde `currencies.totalInvoiced/invoiceCount` con fallback al pending si el bucket está vacío; `collectionEffectiveness = clamp((emitted − pending) / emitted, 0, 1)`. `pendingCollectionCard`, `issuedCard` y `effectivenessCard` ahora consumen el helper en vez de `findCurrency` directo. Removido el `console.debug("[summary cards]", ...)` temporal que se agregó para diagnosticar; removido import sin uso `deriveCollectionEffectiveness`. Resultado: las cards quedan consistentes con Aging y Explorer en todos los casos. Validado: `npx tsc --noEmit` exit 0, `npx vitest run` 627/627, `npx eslint components/copilot/executive-summary-cards.tsx components/copilot/cartera-shell.tsx` sin issues.
- 2026-05-11 — **Cambio de carga inicial `/copilot/cartera` · sin fetch hasta confirmar fechas + sin año/moneda en barra**. Frontend/wiring únicamente. `CarteraShell`: `periodStart/periodEnd` arrancan `null`, `hasConfirmedRange=false`, y `useFinancialReconciliation({ enabled: hasConfirmedRange })` evita el fetch automático al montar; `handleConfirmDraft()` exige `Desde` + `Hasta` válidas y recién ahí setea rango efectivo. Se removió el default visual y efectivo basado en período operativo para la carga inicial (ya no hay snapshot automático). La barra superior quedó sólo con `Desde`, `Hasta`, `Confirmar`, `Refrescar`; `Refrescar` queda disabled hasta confirmar un rango. Se eliminó el selector de año y el selector global de moneda de UI/lógica del control bar; cartera queda en modo multi-moneda (`selectedCurrency="all"`) y los componentes internos conservan soporte multi-currency. Estado inicial profesional: `EmptyPeriodState` ("Seleccione un período"), `EmptySummaryPlaceholders` con valores `—`, placeholders de aging, tabla y reconciliación; durante loading posterior se reutilizan placeholders con shimmer. Validado: `npx tsc --noEmit`, `npx vitest run` 627/627, ESLint cartera sin errores (3 warnings preexistentes en `client-debt-explorer`/`explainability-panel`).
- 2026-05-11 — **Fix UI/state `/copilot/cartera` · cards tolerantes a shape + Confirmar realmente pendiente**. Alcance solo frontend/wiring (no sync, DB, cron, balance_amount ni fórmulas core). `ExecutiveSummaryCards`: `findCurrency()` ahora normaliza `report.currencies` antes de renderizar, aceptando `currencyCode/currency_code/currency/code` y montos camel/snake/aliases (`totalPending/total_pending/pendingAmount`, `totalInvoiced/total_invoiced/invoicedAmount`, counts equivalentes), parseados con `toSafeNumber`. Esto cubre el caso donde el report sí trae USD/UYU pero la UI lee keys distintas o montos string y caía a `?? 0`. Agregado `console.debug("[summary cards]", { selectedCurrency, currencies, report })` sólo en dev para verificar shape real sin secretos. `CarteraShell`: inputs de fecha visualmente vacíos al abrir, aunque el reporte inicial sigue usando período operativo default para no dejar la página sin datos; agrega `confirmedDraftStart/End`, `draftCurrency` y `hasPendingChanges` normalizado `YYYY-MM-DD`. `FinancialControlBar`: moneda y año pasan a draft, no se aplican hasta Confirmar; botón Confirmar se habilita con cualquier cambio pendiente válido (fechas, moneda o año), se deshabilita si no hay cambios / loading / fechas incompletas / `desde > hasta`; selector Año sólo rellena draft; badge dot marca moneda pendiente. Validado: `npx tsc --noEmit`, `npx vitest run` 627/627, `npx eslint app/copilot/cartera/page.tsx components/copilot/cartera-shell.tsx components/copilot/executive-summary-cards.tsx components/copilot/financial-control-bar.tsx` sin issues.
- 2026-05-11 — **Fix cartera cards en cero · Supabase numeric serializado como string**. Causa raíz: `app/api/copilot/financial-reconciliation/route.ts` líneas 116–117 usaba `typeof r.total_amount === "number" ? r.total_amount : null` para mapear filas Supabase a `InvoiceInput`. PostgreSQL serializa columnas `numeric`/`decimal` como STRING en el driver JS de Supabase (para preservar precisión decimal). Con todos los `total_amount` siendo strings (ej. `"3624185.05"`), el cast los convertía a `null` → en el motor `safeNum(null) = 0` → `if (!(totalAmount > 0)) continue` saltaba la factura → `buckets[code]` queda vacío → `report.currencies = []` → `findCurrency()` en `executive-summary-cards.tsx` retorna `undefined` → cards renderizan `$ 0,00 / U$S 0,00` para "Pendiente de cobro", "Emitido" y "Cobranza efectiva: 0,0%". Fix mínimo, capa de wiring (cero cambios en motor, sync, cron, DB, balance_amount, fórmulas, formatters, mappers): nuevo helper `lib/copilot-numeric-parse.ts → toSafeNumber(v: unknown): number | null` (acepta number finito, string JS-native como `"1234.56"` y string es-UY como `"3.624.185,05"`, rechaza NaN/Infinity/boolean/object/empty). Aplicado en el route y en `scripts/run-post-fase3-validation.ts`. Agregado log estructurado `kind: "report_amounts_all_zero"` (warn) cuando hay facturas cargadas pero totales en cero, con hint que apunta al cast como sospecha primaria. Tests: 10 nuevos en `lib/copilot-numeric-parse.test.ts` (627/627 totales). `npx tsc --noEmit` exit 0. `npx eslint` sobre route + helper + tests + componentes cartera → 0 issues.
- 2026-05-11 — **UX cartera (`/copilot/cartera`) · confirmación de fechas + filtro de moneda global + remoción de "Todo el historial"**. Cero cambios en backend financiero / sync / pipelines / RLS. `cartera-shell.tsx`: agrega estado `draftStart/draftEnd` (separado del rango efectivo `periodStart/periodEnd`), state `selectedCurrency: "all" | "USD" | "UYU"`, modo fijo `period_only`. `financial-control-bar.tsx`: control 100% controlado (sin `useEffect` ni `useState` interno de fechas), selector "Todas / USD / UYU" reemplaza el legacy de modos; inputs Desde/Hasta editan draft, botón verde "Confirmar" promueve a estado real (deshabilitado si `!draftDirty` o `desde > hasta`); selector de Año aplica inmediato y sincroniza draft. `executive-summary-cards.tsx`: 4 cards cuando moneda activa (Pendiente de cobro / Emitido / Cobranza efectiva / Clientes en riesgo `<MONEDA>`); 8 cards en "Todas"; nueva variante `staleCardForCurrency` deriva clientes en riesgo desde `report.staleClients` filtrando por `pendingByCurrency[currency] > 0`. `aging-analytics.tsx`: oculta tabs UYU/USD cuando hay moneda forzada, muestra solo esa moneda con badge. `client-debt-explorer.tsx`: pre-filter `pendingByCurrency[currency] > 0`, oculta columna de saldo opuesta, `effectiveSort` deriva en `useMemo` (sin sync de state en render) cuando el sort apunta a columna oculta, expanded row solo muestra moneda activa, copys ajustados. `explainability-panel.tsx`: `buildItems` recibe `selectedCurrency` y filtra listas de monedas (`stalePendingByCurrency`, `historical.pendingByCurrency`, `warnedPendingByCurrency`). 617 tests verdes, tsc limpio, ESLint 0 errores nuevos (3 warnings preexistentes).
- 2026-05-11 — **Backfill saldos pendientes 2026-01-01 → 2026-05-11 validado + wording ejecutivo + Cash conversion**. Validación read-only sobre el período operacional completo (`temp-audits/audit-backfill-saldos-coverage-2026.mjs`): 348 facturas en rango (USD=163, UYU=185); **0 facturas `issued` con `balance_amount=0`** → estado de DB consistente. Cobertura por moneda: USD 32 pendientes / U$S 11.514,08 abiertos; UYU 29 pendientes / $ 561.218 abiertos. Cron Vercel `0 */3 * * *` confirmado, cap efectivo 200 (env `ZETA_SALDOS_CRON_MAX_CLIENTS_PER_WORKSPACE` opcional) cubre los 183 clientes activos con `Codigo`. UI: `components/copilot/copilot-financial-dashboard.tsx` recibe wording ejecutivo (`Facturado → Emitido`, `Pendiente → Pendiente de cobro`, `Efectividad → Cobranza efectiva`), `<KpiGlossary>` con definiciones canónicas, y nueva `<CashConversionSection>` con frase ejecutiva por moneda (Emitiste / cobraste / queda pendiente) que reusa exactamente `totalInvoiced / totalCollected / totalPending` del motor existente. Cero cambios en fórmulas / motor / aging / prioridades. 617 tests, tsc limpio, eslint OK.
- 2026-05-11 — **Fix cobertura cron saldos pendientes Zeta (`balance_amount=0` en facturas USD pendientes mayo 2026)**. Causa raíz: cron `/api/cron/zeta-sync-saldos` con cap `MAX_CLIENTS_PER_WORKSPACE = 10` sin `.order()` no determinístico sobre 183 clientes activos → 22/23 facturas USD del rango 2026-05-01→10 sin saldo aplicado (mapper voucher escribe `balance_amount=0` por diseño; única fuente real es `RESTFacturaClienteV4QuerySaldosPendientes`). Fix mínimo solo en pipeline: cron cap default 200 (env-tunable `ZETA_SALDOS_CRON_MAX_CLIENTS_PER_WORKSPACE`), `.order("id")` determinístico, log `clients_cap_reached` cuando universo > cap; endpoint manual `/api/zeta/sync-saldos-all-clients` cap 50→500, default 20→100, mismo `.order("id")`. Cero cambios en UI, dashboard, fórmulas, reconciliación, RLS. Validación antes/después con `temp-audits/audit-may-usd-balance-mismatch.mjs` (read-only): 1 → 14 facturas USD con saldo real; 22 → 9 con balance=0 (todas status=paid por zero-pass de Zeta). A-2926/2925/2923/2922 correctas. A-2919 ausente → gap separado del voucher pipeline (movido a Now). 1 cliente con `fetch failed` transitorio al staging payload (no bug del fix). Re-sync producción ejecutado: 183 clientes procesados, 52 saldos upserted, 17 min. 617 tests, tsc limpio. Reporte: `temp-audits/audit-may-usd-balance-mismatch-report.md`.
- 2026-05-10 — **UX detalle de cliente · diferenciación Cuenta corriente vs Por factura** (`components/copilot/copilot-client-account-statement.tsx`). Cero cálculos tocados; solo copy/labels/badges. Header y Summary cuenta corriente: `Saldo final` → `Saldo contable acumulado`; footer reporte: `SALDO` → `SALDO CONTABLE`. Nuevo `<ViewModeBadge>` (slate "Lectura contable histórica" / emerald soft "Deuda operativa actual") embebido en `<ContextHint>` con descripciones nuevas. Vista por factura ya usaba "Total facturado" / "Cobrado según Zeta" / "Pendiente según Zeta" — sólo se incorpora el badge. 617 tests, tsc limpio, build OK.
- 2026-05-10 — **UX cleanup `/copilot/datos` · botón Sincronizar Zeta de Contactos**: eliminado botón manual + handler `handleContactSync` + estados `contactSyncState/Count/Error` + imports muertos `CheckCircle2`/`RefreshCw` en `app/copilot/datos/datos-client.tsx`. Sincronización automática vía cron `/api/cron/zeta-sync-contacts`. Botón "Ver contactos" y carga automática preservados; tab Contactos ahora usa el ramal genérico de `DATOS_EXPAND_HINT/CTA`. Verificado: no hay botones manuales equivalentes para saldos/vouchers/recibos. 617 tests, tsc limpio, build OK.
- 2026-05-10 — **UX cleanup Pipeline Health Panel · botón Actualizar**: eliminado botón manual + prop `onRefresh` + import `RefreshCw` en `pipeline-health-panel.tsx`. `app/copilot/page.tsx` no pasa más `loadPipelineHealth`. Realtime + fallback polling cubren el refresh. 617 tests, tsc limpio, build OK.
- 2026-05-10 — **FASE 5 — Pipeline Health Dashboard**: `GET /api/copilot/pipeline-health` (auth + `getAllPipelineHealth()`). `PipelineHealthSummary` extendido con `last_run_duration_ms`, `last_run_rows_processed/updated/failed`. Componente `pipeline-health-panel.tsx`: cards por pipeline con ícono de status, métricas reales, tiempo relativo + duración, errores consecutivos, is_overdue, botón Actualizar. Sección "Estado de pipelines Zeta" agregada a `app/copilot/page.tsx`. 568 tests, tsc limpio.
- 2026-05-10 — **FASE 4 — R-03+U-03 (ROW_CAP no silencioso)**: `lib/copilot-data-completeness.ts` (`DataCompletenessWarning`, `buildCapWarning`). `loadFinancialFactsBundle` detecta qué tablas alcanzan cap y puebla `isTruncated`/`tables_at_cap` en meta. `FinancialSnapshotApiV1.diagnostics.dataset_caps` refleja detección real (antes `truncation_unknown: true` hardcoded). Selectores `snapshotIsTruncated`/`snapshotTablesAtCap` en `copilot-financial-snapshot-selectors.ts`. Banner amber en `app/copilot/page.tsx` con copy de `FINANCIAL_UX_COPY.rowCapWarningTitle/Body`. 568 tests, 41 archivos, tsc limpio.
- 2026-05-10 — **ZETA-06 validación end-to-end en producción**: saldos succeeded, vouchers succeeded, contacts partial (ok). Anti-overlap, `zeta_pipeline_runs`, health layer y logs estructurados validados. Fixes: 7bebb30 (saldos multitenancy), 6cce033 (vouchers multitenancy), credencial Vercel `ZETA_DESARROLLADOR_CLAVE` corregida.
- 2026-05-09 — **Pipeline Contactos Zeta (DIV-003 + incremental sync)**: `contracts/zeta-contacts.contract.ts` soporta shape primario `QueryOut.Response[]` (Postman + tenant real) con retrocompat para `QueryOut.Contactos.Contacto[]` y variantes defensivas. `zeta-contact-mapper.ts` mapea a `ZetaContactProtoShape` (external_id, name, document, email/email2, telefono/celular, es_cliente, es_proveedor, raw_payload). `zeta-contacts-fetch.ts` con log estructurado de shape. `zeta-contacts-pipeline.ts`: sync incremental paginado con upsert (por Codigo/Documento/email), resolución de FK a proto_companies, trazabilidad en zeta_sync_*, retry simple 3 intentos, fallback de esquema en primer sync. API route `POST /api/zeta/sync-contacts`. Documentado en `KNOWN-DIVERGENCES.md` (DIV-003) y `NOTE-001`. 521 tests, tsc limpio.
- 2026-05-09 — **Hard Cutoff Pre-2026 (Fases 1–6)**: 2026-01-01 convertido en límite mínimo absoluto del sistema. Fase 2: `zeta-factura-cliente.ts` elimina fallback `new Date()` + rechaza fechas pre-2026 con log `pre_operational_cutoff`; `zeta-saldos-pipeline.ts` skip hard en `persistZetaInvoice`. Fase 3: script `scripts/remove-pre-2026-financial-data.ts` con dry-run/execute, cuenta+montos, respeta FKs. Fase 4: `proto-operational-read-repository.ts` y `proto-analytics-read-repository.ts` tienen `.gte("issue_date"/"receipt_date", COPILOT_OPERATIONAL_START_DATE)` en todas las queries; `buildFinancialDashboardMetrics` filtro in-memory defense-in-depth. Fase 5: `assertOperationalDate()` en `copilot-operational-period.ts`. Fase 6: 8 nuevos tests assertOperationalDate, 8 tests saldos-mapper hard cutoff, 4 tests dashboard-metrics pre-2026. `PROVENANCE_HOME_DASHBOARD` → `operational_2026`. 495 tests, 38 archivos, tsc limpio.
- 2026-05-09 — **Financial Semantic Unification (Fases 2–4+7)**: `lib/copilot-financial-terminology.ts` (vocabulario canónico: FT, AGING_DISPLAY_LABELS, DATA_SOURCES, PERIOD_SCOPES, RECORD_SCOPES, DataProvenanceConfig, 3 provenance presets). `components/copilot/financial-data-provenance.tsx` (`<DataProvenanceBadge>` Fuente · Período · Alcance + tooltip expandible). `copilot-financial-dashboard.tsx`: eliminados `CURRENCY_SYMBOL`/`CURRENCY_LABEL` locales, usa `currencySymbolFor` + `CURRENCY_SHORT_LABELS` del sistema central, badge `PROVENANCE_HOME_DASHBOARD` visible. `app/copilot/page.tsx`: locale `es-AR` → `es-UY`. 23 tests terminología, 475 total, tsc limpio.
- 2026-05-09 — **Período operativo 2026**: constante central `COPILOT_OPERATIONAL_START_DATE = "2026-01-01"` en `lib/copilot-operational-period.ts`. Reconciliación y cartera default a `period_only` 2026-01-01→hoy. Mappers Zeta (vouchers + recibos) rechazan fechas pre-2026 con `reason: "pre_operational_date"`. Reporte agrega `operationalPeriod` y `excludedHistorical`. UI: ControlBar renombrado a "Período operativo" con badge "2026", ExplainabilityPanel diferencia modo operativo vs historial. 452 tests, tsc limpio.
- 2026-05-09 — **Bloque 5**: `agingByCurrency` backend, `AgingAnalytics`, `ClientDebtExplorer`, `ExplainabilityPanel`, efectividad UYU/USD separadas. 416 tests, tsc limpio. Discrepancia de valores investigada y cerrada: 148.691/6.352 son valores del home (`buildFinancialDashboardMetrics`, `getProtoInvoices("all")`); 764.218/15.366 son los valores correctos de cartera (`is_active=true`, modo `all_outstanding`). Sin bug de código.
- 2026-05-08 — **Salud financiera como centro de decisiones**: nuevo motor `lib/copilot-financial-priority-engine.ts`, componentes `FinancialPriorityAlerts`, `FinancialActionPriorities`, `FinancialRiskSummary` y dashboard reordenado para empezar por atención/acción/riesgo antes de KPIs. Priority score determinístico 0-100 y semáforos auditables.
- 2026-05-08 — **Salud financiera en Hoy**: dashboard ejecutivo movido del sidebar de cliente a `app/copilot/page.tsx` (home), entre "Liquidez y cobertura" y "Próximos vencimientos". Sidebar revertido al alcance client-specific. Datos reales del tenant validados (USD efectividad 93.61%, UYU 95.75%).
- 2026-05-08 — **Executive Financial Dashboard Fase 1**: helper puro `lib/copilot-financial-dashboard-metrics.ts`, tests dedicados, UI `components/copilot/copilot-financial-dashboard.tsx`. Métricas reales por moneda: facturado, cobrado, pendiente, efectividad, counts, top deudores y aging. Reutiliza reglas de deuda actual; no usa recibos ni linking inferido.
- 2026-05-07 — `temp-audits/` creada, ignorada en `.gitignore` (excepto `README.md`); convención de archivos temporales documentada.
- 2026-05-07 — Auditoría reconciliación enero–abril 2026 (read-only): identificada hipótesis H1 (`issue_date` fallback `new Date()`); confirmada fila Excel `Prestis Nº=0` como borrador CFE válidamente excluido del sync.
- 2026-05-07 — **Fix H1**: `mapCopilotCustomerVoucherToProtoInvoiceInput` y pipeline customer-vouchers rechazan fechas no parseables sin caer a fecha actual; tests verdes.
- 2026-05-07 — **Migración `supabase/zeta-05-01-proto-receipts-zeta-sync.sql`** ejecutada (agrega `currency_code`, hace `company_id` nullable, mantiene RLS por `workspace_company_id`).
- 2026-05-07 — **Pipeline Recibos Zeta — Fase 1 y 2** implementadas: tipos, mapper (currency normalizada, prioridad de `payment_method`, rechazo de anulaciones / fechas inválidas / importes ≤ 0), pipeline con métricas extra y `allowUnlinkedCompany`, ruta API `/api/zeta/sync-collection-receipts`. `tsc --noEmit` y `vitest` 144/144 verdes.
- 2026-05-07 — **DIV-001 (request shape)** corregido: `buildQueryInData` omite filtros opcionales vacíos, `Mes` sin `padStart`; log estructurado `kind: "zeta_receipts_payload_shape"`; tests de regresión (6 casos). HTTP 400 resuelto.
- 2026-05-07 — **DIV-002 (response shape)** corregido: contract acepta `QueryComprobantesOut.Response[]` (Postman / real tenant), `QueryOut.Response[]` (legacy), root array y fallback defensivo. `summarizeZetaCollectionReceiptsResponseShape` + log `kind: "zeta_receipts_raw_response"`. Tests (16 casos). Documentado en `KNOWN-DIVERGENCES.md`.
- 2026-05-07 — **Sync recibos enero–mayo 2026** ejecutada: 58 + 55 + 48 + 67 + 6 = **234 recibos** persistidos, 0 errores, 0 unlinked, 0 invalid, 0 negative.
- 2026-05-07 — **Reconciliación Recibos vs Excel maestro `RecibosCobranzaWWExport-67.xlsx`**: paridad 100 % (234 = 234, monedas exactas USD=111 / UYU=123, distribución mensual idéntica, 0 diffs reales). Outputs: `temp-audits/receipts-reconciliation-2026.md` y `temp-audits/receipts-reconciliation-diff.csv` (vacío, solo header). Script reusable: `scripts/audit-receipts-reconciliation-2026.ts`.
